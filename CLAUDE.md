# このプロジェクトの絶対ルール

## 【最重要】コード変更前の必須確認手順

**コード変更・追加・提案を行う前に、毎回必ず以下を実行すること。省略厳禁。**

### ステップ1：対象ファイルを実際に読む
- 変更対象ファイルをReadツールで必ず読み込む
- 記憶・前回の読み込み結果に頼らない
- 毎回・毎ターン・必ずディスクから読み直す

### ステップ2：参照シンボルの存在確認
- 提案コードが参照する関数・変数・定数・importが実際に存在するか確認
- 存在確認はGrep/Readで実際に確認する（推測しない）
- 挿入位置の前後数行を実際に読んで確認する

### ステップ3：自己検証チェックリスト
提案前に以下を全て確認すること：
- [ ] 参照する全シンボルが実際に存在する（行番号も確認）
- [ ] 挿入位置の前後コンテキストが正確（実際の行番号）
- [ ] 新規追加関数と既存コードで変数スコープが正しい
- [ ] 重複定義がない（同名関数・変数が既に存在しないか）
- [ ] import/依存関係が揃っている

### ステップ4：提案の形式
変更前・変更後・行番号を省略なく提示し、確認を待つ。

---

## プロジェクト基本情報

- GCPプロジェクト: crypto-alert-479023
- Cloud Run: crypto-alert（asia-northeast1）
- GitHub: kenji888knymec/ocho
- メインコード: main.py（約5755行）

## 開発方針

- V1改善は保留。主戦場はV2のみ
- 時間帯フィルターは今は入れない
- LONG側はまだ観察段階（SHORT優先）
- SHORT と LONG は最終的に別々のAIモデルで判定させる
- 今はSHORTデータが多いのでSHORT先行で進める。LONGは同じ流れを後追いで実施

## pushルール

- 私が「OK」と言うまでcommit・pushは絶対にしない
- pushブランチ: `claude/crypto-bot-assistant-QlA5G`
- mainブランチへの直接pushは禁止

---

## 【セッション開始時に毎回自動でやること】

**新しいセッション開始時、または「最新データを確認して」と言われたとき、必ず以下を実行する。**

### 手順1：v2_shadow_ai の最新データ取得
- `/v2_report` エンドポイント呼び出し、またはスプレッドシートから `v2_shadow_ai` シートを直接読み込む
- 総行数・OPEN中・DONE件数を把握する

### 手順2：ロードマップ第4段階に従って分析
以下の軸で集計・比較する：
1. **Symbol別** — 勝率・平均PnL・件数（上位10件）
2. **Direction別** — SHORT / LONG それぞれの勝率・PnL
3. **P3_VolumeScore別** — プラス / ゼロ / マイナスで勝率・PnL比較
4. **Hour_JST別** — 時間帯別勝率（可視化、ただしまだフィルターは入れない）

### 手順3：3分類を日本語でまとめて提示
以下の形式で出力する：

**切る候補**（今の時点で弱く、次の修正で除外候補にしてよい群）
- 対象・理由・具体的なコード修正案

**残す候補**（相対的に良く、現在のV2に残してよい群）
- 対象・理由

**まだ観察**（件数不足または判断が早い群）
- 対象・理由・次に再判断するタイミング

### 手順4：確認を待つ
- 賢治さんの「OK」が出るまで push・commit しない
- 修正は1〜2個の条件に絞る（一気に多くを切らない）

---

## ロードマップ現在地（厳守）

| 段階 | 内容 | 状態 |
|---|---|---|
| 第1段階 | V1は保留・比較対象のみ | 完了 |
| 第2段階 | V2 Shadow 土台 | 完了 |
| 第3段階 | 観察基盤の正常化 | 完了 |
| **第4段階** | **V2ルールの成績レビュー・3分類（方向A: ブロックリスト整理）** | **← 今ここ** |
| 第5段階（SHORT先行） | **SHORT選抜構造の実装（方向B: 良いものだけ通す絞り込みフィルタ）** | 次 |
| 第6段階（SHORT先行） | SHORT選抜構造を2〜3週間 Shadow で回す | まだ |
| 第7段階（SHORT先行） | SHORTデータでAIモデルを作成・検証 | まだ |
| 第8段階（SHORT先行） | QualityScore（現行スコア）をAIスコアに置き換え | まだ |
| 第9段階（SHORT先行） | AIを最終判定に使用・v2_live昇格 | まだ |
| LONG対応 | 第5〜9段階と同じ流れをLONGで繰り返す | SHORT完了後 |

### 第5段階（SHORT選抜構造）の目標
- 1日2〜3件に絞り込む
- 目標勝率：70%以上
- 「確実に勝てる」と判断できるシグナルだけを通す
- 実装後は2〜3週間 Shadow で回し、そのデータでAIモデルを育てる

### 今やらないこと（絶対厳守）
- V1改善への回帰
- 時間帯フィルターの実装（観察はするが入れない）
- LONG側への積極的な修正（SHORT完了後に着手）
- 最小修正を超える変更
- 第5段階より先の作業（SHORTコード変更指示が出るまで待つ）

---

## 【スプレッドシート分析の厳守ルール】

### 1. 列名ベースで読む
- 列の位置（A列、B列など）ではなく列名で識別する
- 列の順番が変わっても正しく読める実装にする

### 2. 空白セルの扱い
- 空白セルを0や平均値などで補完しない
- 空白セルは「データなし」として扱う
- 推測値で埋めることは禁止

### 3. 行のずれ防止
- 空白セルが原因で行がずれる実装は禁止
- 1行1行を独立したレコードとして扱う
- 空白セルがあっても他の列のデータがずれないこと

### 4. 空白行の扱い
- 完全空白行は行ごとスキップする
- 部分的に空白のある行は空白列だけスキップして他の列は正常に読む
- 行を詰めて処理することは禁止

### 5. 分析の分離
- 完了データ（EvalStatus=DONE）と未評価データは必ず分けて扱う
- 主な判断は完了データのみで行う
- 未評価データを完了データと混ぜて集計しない

### 6. 確認事項
- 分析前に必ず以下を報告する
  - 使用した列名一覧
  - 完全空白列の有無
  - 完全空白行の有無
  - 除外した行数と理由
