# このプロジェクトの絶対ルール

## 【最重要】コード変更前の必須確認手順

**コード変更・追加・提案を行う前に、毎回必ず以下を実行すること。省略厳禁。**

### ステップ1：対象ファイルを実際に読む
- 変更対象ファイルをReadツールで必ず読み込む
- 記憶・前回の読み込み結果に頼らない
- 毎回・毎ターン・必ずディスクから読み直す

### ステップ2：参照シンボルの存在確認
- 提案コードが参照する関数・変数・定数・importが実際に存在するか確認
- 存在確認はGrep/Readで実際に確認する（推測しない）
- 挿入位置の前後数行を実際に読んで確認する

### ステップ3：自己検証チェックリスト
提案前に以下を全て確認すること：
- [ ] 参照する全シンボルが実際に存在する（行番号も確認）
- [ ] 挿入位置の前後コンテキストが正確（実際の行番号）
- [ ] 新規追加関数と既存コードで変数スコープが正しい
- [ ] 重複定義がない（同名関数・変数が既に存在しないか）
- [ ] import/依存関係が揃っている

### ステップ4：提案の形式
変更前・変更後・行番号を省略なく提示し、確認を待つ。

---

## プロジェクト基本情報

- GCPプロジェクト: crypto-alert-479023
- Cloud Run: crypto-alert（asia-northeast1）
- GitHub: kenji888knymec/ocho
- メインコード: main.py（約5755行）

## 開発方針

- V1改善は保留。主戦場はV2のみ
- 時間帯フィルターは今は入れない
- LONG側はまだ観察段階（SHORT優先）
- SHORT と LONG は最終的に別々のAIモデルで判定させる
- 今はSHORTデータが多いのでSHORT先行で進める。LONGは同じ流れを後追いで実施

## 【絶対禁止】欠損値・NaN の補完

**コード変更・追加時、以下を必ず確認すること。違反は厳禁。**

- `_safe_float(x, 50)` `_safe_float(x, 0)` のような「都合のよい中間値・ゼロ補完」は禁止
- NaN / 欠損の場合は `np.nan` を返し、後続の判定で `np.isfinite()` を使ってスキップする
- スプレッドシートの空白セルを 0・平均・中央値・推測値で埋めることは禁止
- 欠損があった場合は「加点も減点もしない（無視する）」が原則
- 提案コードに `default=0` `fillna(0)` `or 0` `or 50` が含まれていたら必ず立ち止まって見直す

**チェックリスト（コード提案前に毎回確認）：**
- [ ] `_safe_float` のデフォルト引数が `np.nan` 以外になっていないか
- [ ] `fillna()` に数値が入っていないか（`fillna(np.nan)` は OK）
- [ ] `or 0` `or 50` のような暗黙補完がないか
- [ ] RSI・スコア・価格系の欠損を推測値で補っていないか

**修正依頼を受けたときの対応ルール（厳守）：**
- 修正内容に上記の欠損補完が含まれている場合、実装前に必ず確認を取る
- 例：「この修正、RSI=50 補完が入っていますが本当に実装してよいですか？」
- 指示書・依頼文そのままでも、欠損補完の疑いがあれば確認してから進む
- 確認なしに禁止パターンを実装することは絶対にしない

## 【分析・推奨時のバイアス注意】

**AIは「変えない方向」「安全側に止める方向」に寄りがちなバイアスがある。毎回意識して補正すること。変更を避けること自体を安全と決めつけない。**

### 判断時に必ず比較する3点
1. 変更した場合のリスク
2. 変更しない場合のリスク
3. 現行条件を維持した場合の実損・機会損失

### 正しい比較軸
- **NG**：「候補 vs. 理想の閾値」だけで判断する
- **OK**：「候補 vs. 現行条件の実績」で比較する
- 例：現 SHORT_C が直近10日 0%/-0.885 のとき、候補が 57.9%/+0.188 なら候補の方が優れている

### 「維持」もリスクとして扱うケース
現行条件が以下のいずれかに該当する場合、「維持する」こと自体をリスクとして評価する：
- 直近で負けが続いている
- 発火していない（データが取れていない）
- 他条件と重複していて独立した検証価値が低い

このとき、データ上「導入した方が検証価値または期待値が高い」と判断できるなら、**1枠だけ・停止基準付きで導入する判断を積極的に検討する。**

### 導入判断の原則
- 導入するものは導入する。導入しないものは導入しない。フラットに判断する。
- 複数変更を同時に入れて原因が混ざることは避ける
- 原則として1回の変更は1profile または1論点に限定する
- 変更後の評価は `D = notify_sent=1 かつ DONE` の件数・勝率・PnL で行う

### チェックリスト（分析・推奨前に毎回確認）
- [ ] 3点比較（変更リスク・維持リスク・現状の実損）を全て出したか
- [ ] 比較対象は「理想」ではなく「現行条件の実績」か
- [ ] 「変えない」理由と「変える」理由を両方フラットに列挙したか
- [ ] 「まだ様子見」の場合、期限（件数・日数）が具体的に定義されているか
- [ ] 現状維持した場合のコスト（損失・機会損失・検証機会の喪失）を明示したか

---

## 【最重要】「待つ」と「変える」のフラット評価

**過去に2種類の失敗があった。両方を回避すること。**

- **失敗パターンA**: 変更を急ぎすぎて、十分な検証データが集まる前に条件を変え続け、原因が混ざって負けた
- **失敗パターンB**: 明らかにシグナルが出ていないのに「待て」を繰り返し、ユーザーの時間と機会を大量に失わせた

「待つ」も「変える」もデフォルトにしない。**毎回フラットに評価する。**

### どちらを選ぶかの判断フローチャート

```
Step 1: A候補（条件マッチ行）の件数を確認
        ↓
   ┌────┴────┐
   A候補=0   A候補>0
   ↓         ↓
   Step 2A   Step 2B
```

#### Step 2A: A候補=0 の場合

これは「市場が来ていない」のではなく「**条件が今の市場と合っていない**」可能性が高い。

確認項目:
- [ ] 直近の市場状況（BTC mode、上下トレンド、レンジ）は条件想定と整合しているか
- [ ] 条件想定の市場が直近1週間内に来ているか（例: UPモード条件なのにレンジが続いている）
- [ ] 同じ profile が過去に同様の市場で発火していたか

判断:
- 想定市場が来ていない（例: UP条件で、過去1週間レンジ） → **待つ妥当**（ただし期間と再評価条件を明示）
- 想定市場が来ているのに発火0 → **条件破綻、変える方向で検討**
- ユーザーから2回以上の懸念表明 → **待つを選ばない**

#### Step 2B: A候補がある場合

A候補は出ているが、route通過 / notify_sent / D が伸びない。

確認項目:
- [ ] A候補 → route通過 → notify_sent の各層で何件落ちているか
- [ ] 直前の変更から何時間経ったか
- [ ] サンプル数は統計的判断に十分か

判断:
- 直前変更から24時間未満 → **待つ妥当**（浸透時間）
- A候補があり route も通っているが件数が薄い → **待つ妥当**（サンプル蓄積）
- どこかの層で落ちすぎている → **その層が原因、変える方向で検討**

### 「変える」を選ぶ目安

以下のいずれかなら、変えることを優先的に検討:
- A候補=0 が3日以上続いていて、想定市場は来ている
- 直近7日の通知発火数が目標下限を大きく下回る
- 現条件の実績が明確に弱い（負け続き、PnL累計マイナス、active_days極小）
- 候補に明確に優れたデータがある（候補 vs 現行で全指標勝ち）

### 「待つ」を選ぶ目安

以下のいずれかなら、待つことを優先的に検討:
- 直前の変更から24時間未満
- A候補・route通過があり、ただ件数が統計的に薄いだけ
- 想定市場が直近来ていない（条件側でなく市場側の理由）
- 変更しても判定材料が変わらない（=判断を保留する価値がある）

### 「待つ」を提案するときの必須項目

- [ ] 何件のA候補が出ているか（数値）
- [ ] 何件がrouteに乗っているか（数値）
- [ ] 待つ期間の終了条件（具体的件数 or 日数）
- [ ] 待っている間の機会損失の見積もり
- [ ] **想定市場が来ているかどうかの判定**

これらを書けないなら、「待つ」を提案しない。

### 「変える」を提案するときの必須項目

- [ ] 候補と現行条件の数値比較表（n件/勝率/avgPnL/medPnL/active_days）
- [ ] 候補のtrain/test時系列分割結果（崩れていないか）
- [ ] 候補と他profileの重複率
- [ ] 1profileのみ変更を厳守（複数同時変更は禁止）
- [ ] 停止基準（数値で）

これらを書けないなら、「変える」を提案しない。

### システム健全性 > Profile健全性

個別profile（LONG_E等）の評価をする前に、必ず以下を先に確認する：

1. Bot全体の通知発火数（直近7日）が1件/日を下回っていないか
2. もし下回っていれば、profile単位の議論を停止し、システム横断のファネル分析を最優先

**個別profileのD件数を待つ前に、システム全体の発火が止まっていないか確認しろ。**

### ユーザーの不安は警告として扱う

- 「シグナル出ない」「大丈夫か」を **2回以上** 言われたら、自分のフレームが間違っている可能性が高い
- 「説明」して安心させようとせず、**ズームアウトして「そもそも今のフレームで判断していいのか」を再検討する**
- ユーザーの懸念を「市場が来ていない」「条件が来ていない」で吸収するのは禁止

### AIの構造的な失敗パターンと対策（原因 → 対策の対応表）

過去に発生した失敗の根本原因と、それぞれに対する具体的な対策：

#### 原因1: フレーム固着
- **症状**: 直前の話題の枠でしか答えを探さない（例: 「LONG_E評価」と言われたら、システム全体の発火数を確認しないまま LONG_E 単体を見る）
- **対策（毎回実施）**:
  - [ ] 個別profile・個別条件の議論を始める前に、**最初に「Bot全体の直近7日通知発火数」を必ず確認する**
  - [ ] 1件/日を下回っていたら、個別議論を**自動的に保留**してシステム横断ファネル分析に切り替える
  - [ ] ユーザーが個別profileの話題を出しても、システム全体が機能していなければ「先にこちらを確認させてください」と必ず横断視点を提示する

#### 原因2: 「待つ」が安全側に見える錯覚
- **症状**: 行動の失敗 > 待ちの機会損失、と無意識に評価する。実際は逆のことが多い
- **対策（毎回実施）**:
  - [ ] 「待つ」を提案する前に、必ず**「待たない場合の損失」と「待つ場合の機会損失」を数値で並べて比較**する
  - [ ] 機会損失の数値（発火0件/日 × N日 = 機会損失数）を必ず明示する
  - [ ] 比較を書けないなら「待つ」を提案しない（書けない=判断材料がない=待つ判断もできない）
  - [ ] 「待つ」のデフォルト選択を禁止する。**何もしないという選択肢は「変える」と同じ重さで評価する**

#### 原因3: データ不足を行動しない理由に使う
- **症状**: 「データ集まるまで待つ」と言うが、データ不足自体が「条件が機能していない」というデータであることを見落とす
- **対策（毎回実施）**:
  - [ ] 「データが少ない/まだ判断できない」と言いそうになったら、**先に「データが集まらない理由」を確認する**
  - [ ] 確認すべき項目:
    - A候補（条件マッチ行）は出ているか
    - routeに乗っているか
    - notify_sent前で止まっているか
  - [ ] これらが0なら「データ不足」ではなく「**条件が壊れているか厳しすぎる**」と判定する
  - [ ] 「データが集まらないこと自体が判定材料」として扱う

#### 原因4: 自分が書いた指示書を自分の判断には適用しない
- **症状**: 保守バイアス補正セクションを書きながら、自分の応答では「変えない=安全」を選び続ける
- **対策（毎回実施）**:
  - [ ] 提案を出す前に、CLAUDE.md の該当セクション（保守バイアス・待ち禁止条件）を**必ず再読する**
  - [ ] 「待つ」「様子見」「もう少し」と書きそうになったら、CLAUDE.md の禁止条件チェックリストに照らす
  - [ ] チェックリストに違反していたら**提案を撤回する**
  - [ ] 自分の応答に対して「もし他人が書いたものとして読んだら、CLAUDE.mdの原則に違反していないか」を自問する

### この失敗が起きたときの対応

ユーザーから「待たせて時間を無駄にした」と指摘されたら：

1. 弁明・正当化しない
2. なぜなぜ分析（Why 5以上）を実施する
3. 防止ルールを CLAUDE.md に書き込む
4. 直近の判断を即座に見直す

---

## pushルール

- コード変更が完了したら、確認を待たずにそのままcommit・pushまで進めてよい
- pushブランチ: `claude/crypto-bot-assistant-QlA5G`
- mainブランチへの直接pushは禁止

---

## 【セッション開始時に毎回自動でやること】

**新しいセッション開始時、または「最新データを確認して」と言われたとき、必ず以下を実行する。**

### 手順1：v2_shadow_ai の最新データ取得
- `/v2_report` エンドポイント呼び出し、またはスプレッドシートから `v2_shadow_ai` シートを直接読み込む
- 総行数・OPEN中・DONE件数を把握する

### 手順2：ロードマップ第4段階に従って分析
以下の軸で集計・比較する：
1. **Symbol別** — 勝率・平均PnL・件数（上位10件）
2. **Direction別** — SHORT / LONG それぞれの勝率・PnL
3. **P3_VolumeScore別** — プラス / ゼロ / マイナスで勝率・PnL比較
4. **Hour_JST別** — 時間帯別勝率（可視化、ただしまだフィルターは入れない）

### 手順3：3分類を日本語でまとめて提示
以下の形式で出力する：

**切る候補**（今の時点で弱く、次の修正で除外候補にしてよい群）
- 対象・理由・具体的なコード修正案

**残す候補**（相対的に良く、現在のV2に残してよい群）
- 対象・理由

**まだ観察**（件数不足または判断が早い群）
- 対象・理由・次に再判断するタイミング

### 手順4：確認を待つ
- 賢治さんの「OK」が出るまで push・commit しない
- 修正は1〜2個の条件に絞る（一気に多くを切らない）

---

## ロードマップ現在地（厳守）

### 全体方針（最終形）
1. ルールで最低限の危険を落とす
2. AIが候補全体を採点する
3. AIが強いものを上に押し上げる
4. 上位だけ通知する
5. 最終的に **SHORT-AI** / **LONG-AI** を分ける
→ フィルターだけのAIではなく、**ランキング主役のAI** を目指す

---

### フェーズ一覧

| フェーズ | 内容 | 対象 | 状態 |
|---|---|---|---|
| Phase 1 | SHORT選抜構造をShadowで安定化（defensive filter + QualityScore + 上位N件） | SHORT | **← 今ここ** |
| Phase 2 | SHORTのShadowデータを2〜3週間ためる（毎日v2_shadow_aiを確認） | SHORT | 次 |
| Phase 3 | SHORTの仮スコアを本物のAIスコアに置き換える準備（外部でモデル作成） | SHORT | まだ |
| Phase 4 | SHORTランキングAIを導入（calc_quality_scoreをAIスコアに差し替え） | SHORT | まだ |
| Phase 5 | SHORTでAIあり/なし比較（ルールのみ / +手作りQS / +AIランキング 等） | SHORT | まだ |
| Phase 6 | LONGを同じ構造で開始（母数が固まったら着手） | LONG | SHORT完了後 |
| Phase 7 | LONG-AI / SHORT-AI の分離（別物として設計） | BOTH | まだ |
| Phase 8 | 最終出力ルール確定（確信度しきい値 + 上位N件 ハイブリッド） | BOTH | まだ |

---

### Phase 1 詳細（今ここ）
**やること（SHORTのみ）：**
- defensive filter
- QualityScore
- 上位N件選抜

**v2_shadow_ai に記録：**
- `AI_Prob_Win` / `AI_Pass` / `AI_Band`
- `AI_Model_Version` / `AI_Model_Type` / `AI_Note`
- REJECT / RANK_REJECT / RULE_QS を後で分析できるよう記録

**目的：**
- 「どの候補が落ちたか」「通したらどうなったか」「上位N件だけにすると勝率がどう変わるか」を可視化

---

### Phase 2 詳細（次）
**毎日確認する軸：**
- 通過件数 / 勝率 / 平均PnL
- rank別 / REJECT理由別

**確認したいこと：**
- TOP_N=2 が多すぎるか少なすぎるか
- RSI 40〜60 が広すぎるか（40〜55に絞るべきか）
- BTCstr >= 0.7 が強すぎるか
- P1 >= 2.0 が適切か

**注意：この段階では細かく最適化しすぎない**

---

### Phase 3 詳細（まだ）
**入力候補（今ある列だけで開始）：**
- Symbol / TotalScore / P1_TrendScore / P3_VolumeScore
- RSI / BTC_HTF_Dir / BTC_HTF_Strength / BTC_Mode_Compat / Hour_JST

**教師ラベル：** WinLose（シンプルに）

**分け方：** 時系列で train / test を分ける

---

### Phase 4 詳細（まだ）
**現行のQualityScoreを置き換える：**
```
# 現行
P1 * 1.0 + RSI_bonus * 0.8 + BTCstr * 0.5 + TotalScore * 0.3

# 置き換え後
model.predict_proba() または model.predict() ベース
```
**AIの役割：** 危険なものを低スコア / 良いものを高スコア / 候補全体の中で順位づけ

**重要：** この段階でも最低限のdefensive filterは残してよい。ただし主役はAIランキング。

---

### Phase 5 詳細（まだ）
**比較対象：**
1. ルールのみ
2. ルール + 手作りQualityScore
3. ルール + AIランキング
4. ルール + AIランキング + 上位N件
5. ルール + AIランキング + 確信度しきい値

**確認指標：** 勝率 / 平均PnL / 件数 / 崩壊日の耐性 / 相場局面別の崩れ方

---

### Phase 6〜8 概要（まだ）
- **Phase 6（LONG）：** 母数が増えたら、SHORTと同じ流れを横展開。RSI帯・P1の効き方・BTC環境はSHORTと別物として設計
- **Phase 7（分離）：** SHORT-AI / LONG-AI それぞれ独立したモデル
- **Phase 8（最終）：** 確信度しきい値 + 上位N件のハイブリッド（弱い日は件数を出さない、強い日にだけ複数出す）

---

### 今やらないこと（絶対厳守）
- V1改善への回帰
- 時間帯フィルターの実装（観察はするが入れない）
- LONG側への積極的な修正（Phase 6まで待つ）
- 最小修正を超える変更
- Phase 1より先の作業（指示が出るまで待つ）

---

## 【スプレッドシート分析の厳守ルール】

### 1. 列名ベースで読む
- 列の位置（A列、B列など）ではなく列名で識別する
- 列の順番が変わっても正しく読める実装にする

### 2. 空白セルの扱い
- 空白セルを0や平均値などで補完しない
- 空白セルは「データなし」として扱う
- 推測値で埋めることは禁止

### 3. 行のずれ防止
- 空白セルが原因で行がずれる実装は禁止
- 1行1行を独立したレコードとして扱う
- 空白セルがあっても他の列のデータがずれないこと

### 4. 空白行の扱い
- 完全空白行は行ごとスキップする
- 部分的に空白のある行は空白列だけスキップして他の列は正常に読む
- 行を詰めて処理することは禁止

### 5. 分析の分離
- 完了データ（EvalStatus=DONE）と未評価データは必ず分けて扱う
- 主な判断は完了データのみで行う
- 未評価データを完了データと混ぜて集計しない

### 6. 確認事項
- 分析前に必ず以下を報告する
  - 使用した列名一覧
  - 完全空白列の有無
  - 完全空白行の有無
  - 除外した行数と理由

---

## 【SHORT再構築フェーズ：分析方針・再評価条件】

**記録日: 2026-06-08。bot本体・環境変数・通知設定は一切変更しない。以下はオフライン分析と再評価の方針のみ。**

---

### 主課題の再定義（重要）

SHORT再構築の主課題は**「銘柄選別・ランキング改善」ではない**。

> **「今日はSHORTを出してはいけない日（危険日）を事前に検知できるか」**

これが最優先課題。2026-06-07の崩壊（WR 10%、SL 91%）の5分位分析で、D1〜D5のWRがすべて10%前後に張りついた（全分位が横並び）。これは「モデルが逆方向に予測した」ではなく、「SHORT全体が機能しない日だった」。この状態で銘柄選別を改善しても無意味。

**分析フレームを切り替える：**
- ❌ 旧フレーム：どのSHORT銘柄を選ぶか（ランキング改善）
- ✅ 新フレーム：そもそもその日はSHORTを出してはいけなかったか（危険日ゲート）

---

### 現行regime4列の限界

現行4列（`BTC_EMA_Slope_1h` / `BTC_4h_Ret` / `BTC_Vol_1h` / `BTC_Down_Streak`）は**状態量**であり、6/7型の崩壊を事前警告できないことが検証で判明した。

- 6/7の特徴量値は訓練分布の内側（88%ile等）にあり、モデルは「知っているパターン」と認識したまま全部外れた
- 6/6 10-12h と 6/7 02-05h でほぼ同じ特徴量値が正反対の結果（WR 100% vs 5%）を示した
- 状態量だけでは「BTCが下落基調の中にいるか」と「BTCが下落末端で反転直前か」を区別できない

**次に確認すべきは「変化量」特徴量：**

| 候補特徴量 | 概要 |
|---|---|
| `BTC_1h_Ret_Change` | 1h収益の変化（加速・減速） |
| `BTC_4h_Ret_Change` | 4h収益の変化 |
| `BTC_EMA_Slope_1h` の変化量 | slope自体の傾き |
| `BTC_Vol_1h` の急低下/急上昇 | ボラ圧縮→スクイーズの前兆 |
| 直近安値からの反発 | BTCが安値から反発しているか |
| BTC短期RSI | 売られすぎからの反発シグナル |
| 上位足の陽線反転 | HTFで陽線転換が起きているか |

これらは既存の `btc_htf` / `btc_15m_df` から計算可能。ただし**今すぐ実装しない**。まず現行データで「変化量で危険日を後付け識別できるか」をオフラインで検証してから判断する。

---

### 次回 EntryRecord 分析時の確認項目

新しいデータが届いたら、ランキング精度より先に以下を確認する：

```
1. regime4列のSHORT DONEが14日以上あるか
2. 6/7型の反転・上昇崩壊日が追加で2〜3日あるか（合計3日以上が目標）
3. その日にSHORT候補全体が負けているか（全分位横並びか）
4. その日を事前に検知できる特徴量があるか（変化量での後付け検証）
5. selected=true や D5 が危険日に自然に減るか（下記注意参照）
```

---

### 再学習 GO 条件（AUC単独では不十分）

以下を**すべて**満たしたときに再学習に進む。AUCだけで判断しない。

| # | 条件 | 根拠 |
|---|---|---|
| 1 | walk-forward **最小AUC > 0.5** | ランダム以下の日が残っていない |
| 2 | D5 または selected=true の **WR ≥ 40〜45%** | 高確率帯でも取引不能水準でないか |
| 3 | D5 または selected=true の **平均PnL > 0** | 実運用で期待値プラスか |
| 4 | 6/7型の危険日に **危険日gateを仮適用したとき pass件数が減るか** | 最重要。ランキングより先に評価 |
| 5 | **SHORT_AI_RANK が RANK_REJECT を明確に上回るか** | ランキングが実際に機能しているか |

**条件4の補足（TOP_N固定の注意点）：**
現在の `V2_SHORT_TOP_N` 固定仕様では、モデルがどんな確率を出しても上位N件は必ず選ばれる。そのため「危険日に selected 件数が自然に減る」という評価は直接できない。かわりに「危険日gateを**仮適用**した場合に候補数・pass数が減るか」「高確率帯でも取引不能水準（WR<30%等）ならgate対象とみなせるか」で評価する。

---

### 現在のアクション（変更なし）

```
SHORT live通知OFF維持（V2_SHORT_AI_NOTIFY_ENABLE=0）
Step 3 再学習 保留
6/8全日 + 6/9以降のEntryRecordを待つ
次回は「SHORT禁止日を事前検知できるか」を重点確認
bot本体・環境変数・通知設定への変更は一切なし
```

---

### 【2026-06-12 合意】現モデルの診断確定と Step 3A 方針

**6/2〜6/12の11日・12,068行（SHORT DONE w/ regime）の分析で以下を確定扱いとする：**

```
現SHORTモデル（AI_Prob_Win）は逆方向に機能している。
- Walk-forward AUC 平均 0.376（6/11=0.114、6/12=0.097）
- Top20% WR 57.4% < Bottom80% WR 69.7%（逆転）
→ SHORT live通知を戻してはいけない
→ 現 AI_Prob_Win / SHORT_AI_RANK を信用してはいけない
```

**G2ゲート（ルール型危険日ゲート）が現時点の主役候補：**
```
G2条件: BTC_EMA_Slope_1h > -0.002 AND BTC_4h_Ret > -0.005 AND BTC_Vol_1h < 0.0025 → BLOCK
実績: PASSED WR=57.7%/+0.429 vs BLOCKED WR=40.0%/-0.023（baseline 54.6%/+0.351）
限界: 6/7は599件ブロック成功（block WR 8%）だが、6/8型はpass側が素通り（WR 6%）で防げない
→ AI再学習そのものより、G2のような危険日ゲートを先に評価する
```

**次の最新EntryRecord（6/12全日＋6/13以降）で確認する5項目：**
```
1. 新規日でも Walk-forward AUC 逆転（<0.5）が続くか
2. G2 gate が PASSED > BLOCKED を維持するか
3. 危険日に G2 で pass 件数が減るか
4. G2 + Top20% が Bottom / Rejected より良いか
5. selected=true の WR / avgPnL が実運用可能水準か
```

**Step 3 は「Step 3A（オフライン/branch限定）」に分割する。本番実装と混ぜない：**

OKなもの（Step 3A）:
```
ローカル/branchで検証
/train_v2_short?upload=0&hot_reload=0
G2ゲートのオフライン評価
regime4列＋変化量特徴量の検証
現モデル vs 新モデル vs G2 の比較
```

まだNGなもの（明示禁止）:
```
本番Cloud Runへpushして推論経路を変える
upload=1
hot_reload=1
SHORT live通知ON
V2_SHORT_RANK_TOP_N 変更
V2_SHORT_PAUSE_AI_RESCUE_MIN 変更
```

**進行順序：**
```
1. SHORT live通知OFF維持
2. LONG_E 停止【実施済み 2026-06-12】（V2_FEATURE_PROFILE_LONG_E=side=LONG;rsi_min=101）
3. 最新EntryRecord再取得（6/12全日＋6/13以降、最新DONE日時を確認）
4. 上記5項目を新データで確認
5. 良ければ Step 3A としてオフライン再学習・shadow比較
6. upload=1 / hot_reload=1 / live通知ON はまだしない
```

**LONG_E停止後の固定観察ルール（2026-06-12開始）：**
```
- 観察期間: 原則7日間、他のprofile・ENV・閾値は一切触らない
- 例外（即見直し）: 実通知8件以上 かつ WR35%未満 かつ 平均PnL大幅マイナスが複数日継続した場合のみ
- 毎回確認: Bot全体の通知件数/日（1件/日を下回ったらprofile議論よりシステム横断分析を優先）
- 確認項目: long_eがnotify_sent=1から消えたか / LONG全体avgPnL・medPnL改善 / LONG_F単独の件数・WR / zero_days
```

**注意：** 同一ファイルの再アップロードに注意。分析前にMD5/サイズ/最新DONE日時で「新しいデータか」を必ず確認する（2026-06-12にバイト同一ファイルを2回分析した実例あり）。

---

### このフレーム切り替えを忘れないために

次セッションで「ランキング改善」「AUCが上がったので再学習」のフレームに戻りそうになったら、この節を再読すること。

**問うべき問いは常に：「危険日ゲートが作れるか」が先。「良い銘柄を選べるか」は後。**

---

## 【2026-06-19 全件検死結果】選別失敗の解剖（EntryRecord 5/1〜6/19, DONE 83,057行）

**データ：** MD5=31c1b85f..., 5/1〜6/19, 最新DONE 6/19 09:00。SHORT DONE 42,814 / LONG DONE 40,243。損益分岐WR=42.9%（medTP0.95%/medSL0.72%）。
**結論を一言で：** SHORTの問題は「弱い」だけでなく**通知ルーティングが負ける候補を選んでいた**。ただしどの選別でも現状プラスにならない。本番凍結は正しい。

### 確定事実（数値）

1. **実通知ルートは2本だけ。QS top-N経由の実通知は0件。**
   - SHORT 実通知535件 = bypass(short_a/b/c) 354件(WR30.8%) + SHORT_AI_RANK 181件(WR27.1%)。
   - 全SHORT候補のWRは46.5%。**選別を通すほどWRが下がる**（全候補46.3%→bypass41.7%→selected34.6%→実通知29.5%）。
   - QS top-N選抜は shadow専用で、一度も実通知を出していない。→ **QSの逆選別は実害の主犯ではない。主犯はbypass＋逆AI。**

2. **QSペナルティはAI追加であり、賢治さんの元式には無い（git確定）。**
   - 初版3/21(5a9dd25): `P1×1.0 + rsi_zone×0.8 + btcstr×0.5 + total×0.3`（高P1・高btcstrを加点＝データと整合）。
   - 3/27(3f3e873)で`btc_over_penalty=-0.30(btcstr>0.90)`追加＋btcstr重み0.5→0.05、3/31(79f9f07)で`p1_high_penalty=-0.15(p1>=2.0)`追加＋P1重み→0.20。**勝ち条件にペナルティを付けて逆転させた。**
   - データ上の真実：BTC_HTF_Strength高 → SHORT WR51.4%、P1高 → WR53-54%（=高btcstr/高P1は本来勝つ）。

3. **「ペナルティ除去」は単独では無効（naive fix棄却）。**
   - OOS(train〜6/8→test6/9〜)で、現行/btc_over除外/p1_high除外/両方除外 すべて test Q5(選抜側)WR≈40%で分岐42.9%割れ。
   - 初版QSのみ test Q5 WR=44.1%でわずかに分岐超。だが実選抜(スロット毎Top-N=2)では現行43.5% vs 初版44.3%とほぼ差なし。**両方ともavgPnLマイナス。** スロット内候補が少なくランキングがほぼ効かない。

4. **どの選別でもSHORTはプラスにならない。** 最良の再現選抜(初版QS Top2)でも test WR44%/avgPnL-0.17。→ QSいじりは本筋でない。

5. **G2ゲート（既存G2_Gate列）はtest期間で逆。** test G2=PASS WR33.0%/avgPnL-0.337 < BLOCK WR47.3%/-0.089。**G2本番gate化は不可。**

6. **LONG_E停止（6/12）は成功。** 6/12以降long_e実通知=0。LONG実通知WR 停止前35.4%→停止後45.7%に改善。ただしLONG_F avgPnL-0.080でまだマイナス、active_d=3と薄い。緊急見直し条件(WR35%未満)非該当。**LONG_F現状維持で観察継続。**

7. **危険日検知の最初の手がかり（要OOS確認・未確定）：** 日次SHORT WRとの相関は `BTC_EMA_Slope_1h変化量` r=-0.52、`BTC_Vol_1h` r=+0.49 が最大。BTC slopeが上向きに転じる＋低ボラ日 = SHORT危険日の傾向。ただし18日・in-sampleのみ。**3反転列(BTC_RSI_15m等)＋日数が揃うまで確定しない。**

### 要決定（コード変更は未実施・凍結中）

- **short_a bypass が `V2_SHORT_AI_NOTIFY_ENABLE=0` を無視して発火している**（main.py:13331-13346, `feature_profile_only_selected`は`_v2_feature_live_guard_reason`のみで通過、OFFスイッチ対象外）。6/17-19に212件(WR34.9%/avgPnL-0.27)。Version=V2-Shadow/FeatureProbeなので即時の実損かは要確認。本番Cloud Runがこの経路で実送信していないかの確認が最優先。

### このデータでやらないこと（確定）
- short_a/b/c再開、SHORT_AI_RANK再開、G2本番gate化、QSペナルティ除去のコード変更、初版QS復帰の本番投入 — **いずれも期待値プラスの根拠なし。**
- 次に意味があるのは「QSいじり」でなく、**3反転列が溜まった後の危険日ゲートOOS検証**（slope変化量・低ボラ）。

### 次にデータをもらうとき欲しいもの
- 3反転列（`BTC_RSI_15m` / `BTC_Rebound_From_Low_1h` / `BTC_HTF_Green_Candle_Flag`）が**非NaNで入った**EntryRecord。現ファイルには列自体が無い。
- これが揃えば危険日ゲートのOOS検証（事実7の確定）に進める。

---

## 【2026-06-19 変化量ゲート検証】rv_chg を危険日検知の本命仮説として保持

**データ：** 28銘柄の15m/1h OHLCV（2026-03-01〜06-19、volume込み、BONK/SHIBは1000系を1/1000補正）。alt個別の価格時系列を初めて入手。検証は全てオフライン（本番・ENV・通知設定への変更なし）。TP=1.0%/SL=0.75%/手数料15bps×2（損益分岐WR=60%）、train=3/1〜5/31 / test=6/1〜6/19。as-of厳守（全特徴量は完了足のみ、未来情報・日次平均は不使用）。

### 経緯（新SHORTシグナル探索 → LONG鏡像 → 変化量ゲート）

1. **新SHORT案2つ（急騰後返り反落 / BTC急速続落型）= 不採用。** 15bps控除後で全パラメータavgPnLマイナス。「急騰直後SHORT」は反落でなくトレンド継続でSL直撃が多い（medPnLが-1.050%に張りつく）。BTC急速続落型はBTC4h-1.5%級がレアすぎて評価不能。

2. **「過熱・高値乖離 → 15m失速確認」型SHORT（3段階）= 不採用で確定。** 失速確認は裸の逆張りよりマシ（Stage1 -0.289% → Stage2/3 -0.20%）だが、全段階・全TP/SL幾何でavgPnLマイナス。TP拡大で損益分岐WRを下げても実WRが同率で下がり、優位性ゼロ。

3. **同設計をLONGで検証（C）= 不採用。** ただし損失構造がSHORTと根本的に違う：
   - **BTC日次<-2%の日：LONG合計-59.2% / BTC日次≥-2%の日：+2.9%。** 損失は暴落日に100%集中。平穏日はプラス寄り。
   - 6/5（BTC-4.43%）に1日69回発火しWR19%で-49.7%（落ちるナイフ掴み）。最悪1日除外で-56→-7%＝極端な1日スパイク依存→不採用。
   - **状態量BTCゲート（4h≤-1.5% or EMA乖離）では救えない**（6/5を捕捉できず）。CLAUDE.md既出のSHORT危険日結論「状態量だけでは6/7型を区別できない」とLONG側から独立に一致。

4. **統合結論：SHORTもLONGも「相場転換日（SHORT=反転上昇日 / LONG=暴落日）」が優位性を破壊し、BTC状態量では事前検知できない、という同一メカニズムで失敗。** → 銘柄選別でなく危険日ゲートが本筋、というロードマップ主課題を両方向から裏付けた。

### 変化量ゲート検証の核心：rv_chg（BTC実現ボラ急増比）

**EDAで唯一as-ofで暴落日を分離した特徴量：** `b_rv_chg`（BTC 1h実現ボラの急増比、完了足shiftでas-of）。暴落日中央値+10.4% vs 平穏日-2.1%。
- 注意：暴落日でもエントリー時のBTC直近リターンは**プラス**（LONGが反発を待って入るため）。だから「BTC下落中」状態量ゲートは効かず、**ボラ急増**が効いた。
- 既存 `slope`+`slope_chg1` shadow は暴落日分離力ほぼゼロ（無力と確定）。

**LONGに rv_chg≥5% ゲート（PASS=平穏側）：** TEST PASS n=74 WR66% avg+0.123% tot+9.1%、BLOCK n=124 WR31% avg-0.519% tot-64.4%（6/5の-51%は全てBLOCK側）。**TEST単体では完璧に見える。**

### B診断（leave-one-out / 閾値スイープ）= 採用判断には使わない前提で実施

| 観点 | 結果 |
|---|---|
| **検知挙動の一貫性** | ✅ **6/5専用でない。** BTC下落日6/2(100%),6/3(60%),6/5(71%),6/9(100%),6/18(70%)を遮断、平穏/上昇日6/6(0%),6/11(0%),6/16(0%)は通過 |
| leave-one-week-out | ❌ 第1週(6/1-7)除外でPASS残り7件・-2.1%に転落。PASS取引の67/74件が第1週集中 |
| leave-one-day-out | ❌ 6/6除外でPASS+9.1%→+0.3%。BLOCK損失-64.4%の-51.4%が6/5単独 |
| 閾値スイープ | ❌ LONGプラスは3〜5%の狭帯のみ。7%で-8.4%、10%で-23.5%に崩壊 |
| SHORT方向 | ❌ 全閾値でPASS<BLOCK（**逆効果**）。共通ゲート不成立、方向別ゲート候補 |

**B診断の確定見解：** rv_chgの「危険日検知ロジック」は6月の複数下落日で一貫して機能する（本物の手がかり）。だが「それで利益が出る」統計的証明は6/5-6/6の1エピソード依存で、現データでは確定できない。**検知は本物、PnL証明は不足。**

### rv_chg の扱い（Aで保持・本番化しない）

```
rv_chg（BTCボラ急増・方向別）= 危険日検知の本命仮説として保持
- 本番gate化しない（PnL証明が1エピソード依存）
- SHORT再開・LONG本番採用・ENV変更・QS修正もしない
- LONG用ゲートとSHORT用ゲートは別設計（共通ゲートは不成立＝SHORTで逆効果）
- 前向きデータで観察を継続
```

**再検証GO条件（複数月にまたがる暴落エピソードが3〜4回・別時期で揃ったとき）：**
```
1. leave-one-week-out で PASS側プラスが残る（単一週・単一エピソード依存でない）
2. rv_chg閾値 3〜7% で符号が安定する（狭帯依存でない）
3. 別月の暴落日でも BLOCK率が下落日に集中する（検知挙動の汎化）
4. LONG/SHORT それぞれで方向別に上記を満たす
```

**次にデータをもらうとき欲しいもの（追加）：**
- 別時期（6月以外）の暴落・反転エピソードを含む期間のalt個別OHLCV（15m/1h、volume込み）。現データは暴落が6月testに集中し、train側に同種の日がほぼ無いため、どんなゲートもtestの暴落に後付けで当たってしまう（train/test符号逆転がその証拠）。

### この段階でやらないこと（確定）
- 新条件探索の継続（今増やすと6月専用の過学習がほぼ確実）
- rv_chgの本番gate化 / 共通ゲート化
- SHORT再開・LONG本番採用・ENV変更・gate化・QS修正

---

## 【2026-06-20 rv_chg 再検証】長期データ（1/1〜6/19）でGO条件チェック

**データ：** 28銘柄 15m/1h OHLCV 2026-01-01〜06-19（前回3/1〜の延長版）。BTC暴落日（<-2%）が全5ヶ月に合計29日あり、独立エピソードが十分に揃った。train=1/1〜4/30（120日）、test=5/1〜6/19（50日）。as-of厳守。

### GO条件チェック結果

| GO条件 | 判定 | 詳細 |
|---|---|---|
| **1. leave-one-week-out でPASS+が残る** | ❌ クリアせず | 6/1〜7週除外でTEST PASS avg -0.267%に転落。6/5依存が残存 |
| **2. 閾値3〜7%で符号安定** | ❌ クリアせず | 7%超でマイナス転落（5%=+0.079%、7%=-0.151%）。狭帯依存解消されず |
| **3. 別月の暴落日でBLOCK率集中** | ✅ **クリア** | 1月(1/25 100%、1/29 100%、1/31 100%)、2月(2/3 100%、2/23 83%)、3月(3/6 100%、3/18 100%)、5月(5/15 100%)、6月(6/2 100%、6/5 71%)——複数月で一貫して暴落日をブロック |
| **4. LONG/SHORT方向別に上記を満たす** | ❌ クリアせず | SHORT方向は全閾値でPASS<BLOCK（逆効果）。方向別ゲートとして設計必須 |

**4条件中1条件のみクリア（条件3のみ）。**

### 前進した点・解消されない点

**前進：** GO条件3が初めて確定的にクリア。rv_chgの危険日検知ロジックは6月専用の過学習ではなく、1〜6月の複数月・複数エピソードで一貫してBTC暴落日をブロックしていることが確認された。

**解消されない点：** train(1〜4月)ではLONG-PASS側がavg -0.379%のまま（全LONGもavg -0.267%で赤字）。train/test符号逆転の主因は「現在のLONGシグナル設計が1〜4月の穏やかな相場でほとんど機能しない」こと——rv_chg以前に母集団自体がtrain期間と合っていない。新しい条件探索をすると1〜6月相場への過学習になるため、LONGシグナルの改良はしない。

### 現在の扱い（変更なし）

```
rv_chg（BTCボラ急増・方向別）= 危険日検知の本命仮説として保持継続
- 本番gate化しない（GO条件3のみクリア、PnL証明は不足）
- 新条件探索もしない（過学習リスク）
- 次のステップ: 実EntryRecord母集団でrv_chgの影響を診断
  → notify_sent=1 / LONG_E / LONG_F / SHORT候補 / SHORT_AI_RANK /
    feature route / v2_core / no_match の各ルートで
    「rv_chg高低が実通知成績の悪化日と対応するか」を確認
  → 良ければ shadow列としての記録設計を検討（gate化は別）
```

### やらないこと（確定）
- LONGシグナル設計の改良（1〜6月相場への過学習になる）
- rv_chgの本番gate化 / 共通ゲート化
- SHORT再開・LONG本番採用・ENV変更・gate化・QS修正

---

## 【2026-06-20 実EntryRecord診断＋shadow列実装】rv_chg を観察記録に追加（gate化はしない）

**データ：** EntryRecord_3.xlsx（DONE 83,426行、5/1〜6/19）に、BTC 1h からas-of計算した rv_chg を merge_asof で割り当てて各ルート別に診断。日別WRバグ（`WinLose=='WIN'` 大文字判定→実値は `'Win'`）を修正し、日次WRは `PnL_Pct>0` に統一。

### 診断結果（実通知ルート母集団・thr=5%）

| ルート | PASS(rv<5%) avg | BLOCK(rv≥5%) avg | rv_chg方向 | 備考 |
|---|---|---|---|---|
| **long_f** | **+0.135%** (n=262) | -0.167% (n=85) | ✅ 正常 | 最良日(6/16)除外後もPASS +0.096%。閾値3〜10%全幅でPASS+。**最も明確** |
| short_bypass_abc | +0.069% (n=994) | -0.021% (n=855) | ✅ 正常 | 最良日(6/18)除外でPASS -0.121%に崩れる→6/18依存 |
| feature_force | +0.050% (n=1350) | +0.003% (n=1081) | ✅ 正常 | 差が薄い |
| notify_sent(実通知) | -0.100% (n=237) | -0.362% (n=84) | ✅ 正常 | 方向は正しいが**両方マイナス**＝rv_chg以前に選別上流が弱い。leave-one-week-out全週で均一マイナス |
| short_ai_rank | -0.167% (n=141) | -0.622% (n=40) | ✅ 正常 | 両方ひどい（逆AI選別の問題） |
| **short_trend_all** | +0.084% (n=10945) | **+0.303%** (n=6771) | ❌ **逆転** | SHORT本来特性（急落局面でSHORT有利）。共通ゲート不成立を実データで再確認 |
| **long_e**(停止済) | -0.380% (n=94) | **+0.249%** (n=141) | ❌ **逆転** | リバウンド型の性質。6/12停止は正しい |
| long_recovery | -0.296% | -0.212% | △ | long_recovery自体が根本的に弱い |

### 確定見解

```
1. B診断・長期再検証の「LONG/SHORT方向別設計が必要・共通ゲート不成立」を実EntryRecord母集団で独立に裏付け
2. long_f に限り rv_chg<5% ゲートがPnLを明確に分離（最良日除外後も安定、6/18依存のshort_bypassより堅牢）
3. SHORTのrv_chg方向は逆（=SHORT本来特性と整合）。方向別設計必須
4. notify_sentが弱いのはrv_chgより上の選別問題（QS・逆AI）が先
→ ただし後付け診断。前向きに同傾向が続くかを観察するため shadow記録を開始する
```

### 実装（shadow記録のみ・本番gate化ではない）

main.py に rv_chg を **shadow列として記録だけ**追加（branch: `claude/crypto-bot-assistant-QlA5G`）。
**実通知・notify_sent・ランキング・QS・route選別・Discord送信には一切影響しない。**

```
ENV（新規・shadow専用スイッチ。実通知に無影響）:
  V2_RV_CHG_SHADOW_ENABLE (default 1)   # shadow記録のON/OFFのみ
  V2_RV_CHG_SHADOW_TH     (default 0.05)

計算（as-of厳守）:
  BTC 1h(btc_htf_df)の未確定足iloc[-1]を除外し完了足のみ。
  rv12 = pct_change(1).rolling(12).std(); rv_chg = rv12/rv12[4本前]-1
  → オフライン診断と30チェックポイントで数値完全一致を検証済み。欠損は補完せず空欄。

記録列（v2_shadow末尾に6列追加。既存列indexは不変・74列）:
  BTC_RV_Chg / BTC_RV_Chg_Bucket(neg/lo/mid/hi/xhi) /
  RV_Chg_Shadow_Pass(PASS/BLOCK) / RV_Chg_Shadow_Reason /
  LongF_RV_Chg_Shadow(long_f bypass行のみ) /
  Directional_RV_Chg_Shadow(LONG系=PASS/BLOCK, SHORT=NA_REVERSED)
```

### やらないこと（確定・変更なし）
- rv_chgの本番gate化 / 共通ゲート化 / 通知可否変更 / LONG_F停止 / route選別変更
- SHORT再開・LONG本番採用・QS修正
- ENV変更（Cloud Run）・デプロイは賢治さんの差分確認後に判断（コードはbranch pushのみ）

### 次のステップ
- デプロイ後、前向きに溜まったrv_chg shadow列で long_f の rv_chg<5% 優位が継続するかを観察
- 継続確認できても gate化はGO条件（leave-one-week-out / 閾値安定 / 方向別）を満たすまで行わない

---

## 【2026-06-20 rv_chg shadow 配線確認合格】前向き観察フェーズ開始

**確認内容（1タイミング分・勝敗判断ではなく配線確認）：**

```
BTC_RV_Chg              = -0.003150023466
BTC_RV_Chg_Bucket       = neg
RV_Chg_Shadow_Pass      = PASS
RV_Chg_Shadow_Reason    = rv_chg=-0.0032<0.05
Directional_RV_Chg_Shadow:
  LONG行  = PASS
  SHORT行 = NA_REVERSED
```

SHORT行の確認：
```
Short_Danger_Gate_Shadow = BLOCK
Directional_RV_Chg_Shadow = NA_REVERSED
notify_sent=0 / short_ai_notify_enable=0
```

**判定：**

| 確認項目 | 結果 |
|---|---|
| rv_chg列の記録 | ✅ OK |
| 値の計算 | ✅ OK（neg=ボラ平穏、as-of） |
| LONG側: Directional = PASS/BLOCK | ✅ OK |
| SHORT側: Directional = NA_REVERSED | ✅ OK（共通ゲート誤適用なし） |
| SHORT通知停止の継続 | ✅ OK（notify_sent=0継続） |

**現在の観察ルール（変更なし）：**

```
rv_chg gate化しない
LONG_Fを止めない
SHORTを再開しない
QSを修正しない
```

**次の確認タイミング：**

| 時期 | 目的 |
|---|---|
| 2〜3日後 | 初期確認（記録が継続しているか） |
| 7日後 | 軽い傾向確認 |
| 10日/20日/30日後 | 本格判断（leave-one-week-out相当） |

次に EntryRecord を貼るのは2〜3日後。ただし Discord警告・`Output length invalid` 再発時はその時点で即確認。

---

## 【2026-06-20 確定評価基準・現状認識の固定】

**記録日: 2026-06-20。本番・ENV・通知設定への変更は一切なし。以下はオフライン分析で確定した事実と以後の評価基準。**

---

### 確定事実（数値・変更禁止）

```
全DONE 83,426行・50日間の PnL_Net (= PnL_Pct - 0.15%) 分析結果:

1. 損益分岐WR (net=0): 52.0% (median TP=0.934% / SL=0.700%)
2. 実全体WR: 43.8%  →  分岐ラインに8ポイント届かず
3. net +0.20% に必要なWR: 64.3%

4. n≥30 & active_d≥5 で以下を全て満たす断面: ゼロ
   - WR ≥ 60%
   - avg PnL_Net ≥ +0.20%
   - median PnL_Net > 0
   (Symbol別/route別/時間帯別/RSI帯別/スコア帯別/方向×銘柄 全探索)

5. 緩い基準 (WR≥55% & avg>0 & med>0, n≥30, d≥5) でも該当ゼロ

6. long_f bypass全期間 (n=347, d=26): WR=47.8%, avg PnL_Net=-0.089%
   rv_chg<5% PASS (n=262, d=19): WR=50.0%, avg=-0.015%
   → どちらも手動運用基準を大きく下回る
```

**結論：「良い候補があるのに選べていない」ではなく「入口候補生成自体が手動運用水準に達していない」**

---

### 【絶対厳守】以後の評価基準（全分析で統一）

```
手動運用前提（"これに掛けていい"と判断できる基準）:

必須条件（全て満たすこと）:
  - PnL_Net で明確にプラス
  - WR 最低 60%以上、できれば 65%以上
  - avg PnL_Net 最低 +0.20〜+0.30%以上
  - median PnL_Net もプラス
  - 10日/20日/30日で大崩れなし（leave-one-week-out確認必須）

除外条件（以下に該当すれば不採用）:
  - active_days 不足（d < 5 は参考値扱い）
  - zero_days 過多
  - max/day 偏り（1日スパイク依存）
  - グロスだけプラス
  - ネット微プラス（avg < +0.10%）
  - n < 30 は統計的判断に使わない
```

---

### 現行本番状態（変更禁止）

```
SHORT停止維持 (V2_SHORT_AI_NOTIFY_ENABLE=0)
LONG_E停止維持 (V2_FEATURE_PROFILE_LONG_E=side=LONG;rsi_min=101)
LONG_F 積極採用せず観察継続
rv_chg shadow記録のみ（gate化しない）
rv_chg gate化なし / SHORT再開なし / TP/SL変更なし / QS修正なし / ENV変更なし
```

---

### 次の検証テーマ（優先順）

```
1. TP/SL幾何のオフライン再シミュレーション
   - OHLCVベース: TP/SL到達順・最大逆行(MAE)・最大順行(MFE)・保有時間
   - 固定%マトリクスで「損益分岐WRを下げれるTP/SL比率が存在するか」確認
   - 注意: TP/SL変更は単純な逆算だけでなく到達率・損失幅が変わることを考慮
   - 採用判断はOOS確認後（今すぐ変えない）

2. 入口ロジックの再設計
   - 現状: 候補生成プール全体が net 分岐割れ → ランキング・AIよりも入口が問題
   - 次に試す仮説: エントリー条件そのものを変える（今のprofile条件の根本見直し）
   - 変更は1条件1回・OOS確認後・本番shadow先行

3. 3反転列 + rv_chg shadow の前向きデータ蓄積
   - BTC_RSI_15m / BTC_Rebound_From_Low_1h / BTC_HTF_Green_Candle_Flag
   - 2026-06-19 デプロイ済み・前向き記録開始
   - OOS検証GO条件（CLAUDE.md記載）を満たすまでgate化しない
```

---

### この段階でやらないこと（確定）

```
AIランキング Phase 3〜5 の本番投入（母集団エッジなし → ランキングに意味なし）
rv_chg gate化 / 共通ゲート化
SHORT再開 / LONG_F停止 / TP/SL本番変更 / QS修正
新条件探索の乱発（6月相場への過学習リスク）
「通知を出したいから条件を探す」方向の変更
```

---

## 【2026-06-20 TP/SL再シミュレーション結果】OHLCV 15mベース（83,426行・28銘柄）

**実施内容：** 現行EntryRecord全DONEをOHLCV 15mで再現シミュ。その後固定TP/SL%マトリクス（TP=6種×SL=5種=30組）を3母集団で評価。コスト0.15%固定、最大保有24h(96本)。TP/SL到達順序・MAE・MFE・保有時間を計測。as-of厳守（エントリー時刻直後の完了足から再生）。

### 現行TP/SL 再現結果

| 対象 | n | WR | avg PnL_Net | 保有 | TP率 | SL率 | avg MAE | avg MFE |
|---|---|---|---|---|---|---|---|---|
| 全DONE | 83,426 | 44.7% | -0.102% | 8本/2h | 44.7% | 55.3% | 4.082% | 4.734% |
| **LONG全体** | 40,256 | **33.8%** | **-0.299%** | 8本/1.9h | 33.9% | **66.1%** | **7.765%** | 0.704% |
| **SHORT全体** | 43,170 | **54.8%** | **+0.081%** | 8本/2h | 54.9% | 45.1% | 0.648% | **8.492%** |
| long_f bypass | 347 | 36.0% | -0.271% | 7本/1.7h | 36.0% | 63.7% | 9.156% | 0.733% |

**非対称性の確定（重要）：**
```
LONG: SL率66.1%, SL到達時avg MAE=11.5%（価格が大きく逆方向に動いてSL到達）
SHORT: TP率54.9%, TP到達時avg MFE=15.1%（SHORT方向には大きく動く）

LONG側とSHORT側は根本的に異なる問題を持つ:
  LONG: 方向予測が間違っている（下落相場でLONGを取り続けている）
  SHORT: 方向は合っているが、TP/SLの幾何で +0.081% 止まり
```

### 代替TP/SL マトリクス（30組×3母集団）

**全DONE (n=83,426) — 全組合せで合格ゼロ:**
```
TP↑でSL率が上がり avg がさらに悪化。SL↑でWRは上がるが avg はマイナスのまま。
最も avg が高い組合せ: TP=2.00% SL=0.30% → WR=17.2%, avg=-0.061% （それでも負け）
```

**SHORT限定 (n=43,170) — 全組合せで合格ゼロ、だが全て△（プラス寄り）:**
```
SHORT全組合せで avg PnL_Net > -0.05% (TP=0.5,SL=0.3以外)
最良: TP=2.00% SL=1.25% → WR=48.3%, avg=+0.168%, med=-0.657% （medがマイナス）
次点: TP=2.00% SL=1.00% → WR=44.1%, avg=+0.166%, med=-1.150%
→ avg はプラス寄りだが med がマイナス（一部の大勝ちに引っ張られる構造）
→ 手動運用基準(WR60%+, avg+0.20%+, med>0) には全く届かない
```

**long_f bypass (n=347) — 全組合せで合格ゼロ、全てマイナス:**
```
最良: TP=2.00% SL=0.30% → WR=11.8%, avg=-0.198% （WRが極端に低い）
→ long_f はTP設定をどう変えても方向予測が弱すぎて回復しない
```

### TP/SL再シミュレーションの確定見解

```
1. TP/SL幾何の変更だけでは問題を解決できない（確定）
   - 全30組合せで合格ゼロ（3母集団とも）
   - SLを広げるとWRは上がるが avgは悪化するか微改善止まり
   - medianがマイナスになるケースも多い（1日スパイク依存構造）

2. LONGとSHORTは根本的に異なる問題:
   - LONG: 方向予測の問題（66%がSL到達, MAE=7.7%）→ TP/SL調整で解決しない
   - SHORT: 現行で avg+0.081%（分岐WR 52%を超える54.8%WR）→ 既存の方向性はある
             ただし avg+0.168%止まり, med<0 → 手動運用基準に届かない

3. SHORTは「方向」は合っているが「精度」が不足:
   - SHORT全体で WR54.8% → 既にある程度下落を捕捉している
   - 問題は精度: 上位30%だけに絞ればWRが上がる可能性はある
   - ただし現行AIランキングが逆選別していたため、改善には新しい入口設計が必要

4. 入口候補生成の再設計が本質:
   - LONG側: そもそも今の相場でLONGを取るべき条件を根本から見直す
   - SHORT側: 方向は合っている。精度向上 = より絞り込んだ入口条件
```

### このデータでやらないこと（確定）
```
TP/SL値の本番変更（全組合せで合格ゼロ・解決策にならない）
LONG本番拡大（方向予測問題が未解決）
SHORT再開（精度が手動運用水準に届いていない）
QS修正（入口ロジックが問題であり選別レイヤーではない）
```

---

## 【2026-06-20 訂正＋SHORT母集団分解】「SHORT方向性あり」は誤り。実績は暴落日依存

**重要な訂正：** 直前のTP/SL再シミュで「SHORT全体 avg+0.081% → 方向はある」と記録したが、これは2つの理由で**水増しされた誤り**だった。実績PnL_Net（PnL_Pct-0.15, ground truth）で再分析した結果を正とする。

### シミュレータのバグ（修正済み・記録）

```
バグ: BONK/SHIB に 1/1000 補正を適用していた（CLAUDE.md旧記述に従った）
事実: EntryRecord_3 + 今回のOHLCVでは、OHLCV生closeが既にEntryPriceとスケール一致
  - SHIB: OHLCV raw close中央値 0.0000060 ≈ EntryPrice中央値 0.0000056
  - 1/1000補正すると1000倍ズレて、SHIB/BONK(全体の7.3%, 6092行)のMAE=99.9%等に汚染
影響: avg MAE 11.5%(LONG SL到達)・MFE 8.5%(SHORT)は外れ値由来。実態はLONG MAE~0.7%, SHORT~0.5%
検証: 非SHIB/BONKでは PnL再現 median|sim-actual|=0.000%, ExitReason一致91.2% → シミュ本体は正しい
今後: このOHLCV＋EntryRecordでは BONK/SHIB に 1/1000 補正をしない
```

### 実績PnL_Net によるSHORT母集団分解（ground truth・シミュ不使用）

| 母集団 | n | active_d | WR | avg | **median** | 合計 | top3日集中度 | 判定 |
|---|---|---|---|---|---|---|---|---|
| short_all | 43,170 | 50 | 46.3% | -0.057% | **-0.486%** | -2468% | -107.9% | ❌ |
| short_trend | 17,716 | 49 | 48.4% | +0.018% | **-0.433%** | +315% | **603%** | ❌ |
| short_retrace | 838 | 46 | 49.4% | -0.041% | -0.359% | -34% | — | ❌ |
| short_bypass_abc | 1,849 | 21 | 41.3% | -0.123% | -0.568% | -227% | — | ❌ |
| **SHORT_AI_RANK_selected** | 181 | 5 | **26.5%** | -0.418% | -0.807% | -76% | — | ❌ |
| notify_sent | 188 | 27 | 34.6% | -0.314% | -0.545% | -59% | — | ❌ |

### 確定見解（訂正後）

```
1. SHORTに「手動運用できる方向性」は無い:
   - short_all median=-0.486%（普段は負ける）
   - short_trend の +0.018% も 6/4・6/5・6/6（暴落初動3日）だけで全利益
     → top3日集中度603%, median-0.433% → 暴落日依存・1日スパイク依存
   - 全体と同じ「暴落日だけ勝ち、平常日は負ける」構造

2. 選別レイヤーが逆選別している（賢治さんの懸念が的中）:
   short_trend +0.018% → bypass_abc -0.123% → AI_RANK_sel -0.418%(WR26.5%) → notify -0.314%
   → 通知に近づくほど悪化。SHORT_AI_RANK は明確に逆選別（WR26.5%）

3. よって「SHORTを絞り込めば精度が上がる」は不成立。
   既存の絞り込みは精度を下げている。
   → 既存profile/選別の延長では手動運用シグナルは作れない（LONG・SHORT共通）
```

### 次にやること（入口ロジック再設計・オフライン研究開始）

```
本番変更なし。OHLCVから新しい入口候補をゼロから生成しPnL_Net基準で検証する。
- 既存の負け候補に後付けANDを増やす探索は禁止（過学習）
- train/test時系列分割・別月・別相場で崩れないか確認
- median>0 かつ 暴落日依存でない（top3日集中度が過大でない）ことを必須化
- as-of厳守（完了足のみ・未来情報/日次確定後情報/欠損補完なし）
- SHORT候補全体 / selected / notify_sent相当 を必ず分けて評価
優先: SHORT入口（ただし"方向性あり"前提は捨て、暴落日非依存の入口を探す）
```

---

## 【2026-06-20 入口仮説スクリーニング結果】今回試した系統では手動運用水準のSHORT/LONG入口は見つからなかった

**表現の注意（賢治さん指摘・厳守）：** 「SHORT入口パターンは存在しない（確定）」は**言い過ぎ**。試したのは単一分位・LONG鏡像・H1/H2/H3の3仮説のみで、入口ロジックを網羅したわけではない。正しくは「**今回試した系統では手動運用水準の入口は見つからなかった。同じデータでこれ以上条件を足す探索は過学習リスクが高いので、この系統は一旦終了**」。

**データ:** 28銘柄 15m OHLCV（1/1-6/19）、BTC 1h文脈使用、as-of厳守。train=1/1-4/30、test=5/1-6/19。TP=1.0%/SL=0.7%/コスト0.15%/最大96バー/同足両ヒット→SL優先。損益分岐WR=50.0%（Win+0.85% / Loss-0.85%が対称）。

### シミュレーション仕様の明示（median=-0.850%固定の理由・検証済み）

| 項目 | 仕様 |
|---|---|
| エントリー価格 | バーiのclose、前向きはi+1から（as-of・未来混入なし） |
| 同一足でTP/SL両ヒット | **保守的にSL優先**（実測 446件=全体の0.39%のみ。影響軽微） |
| 最大保有 | 96本=24時間 |
| 未到達(EXPIRE) | 96本目のcloseで決済。実測 1,379件=1.2%のみ |
| MFE/MAE | exit（TP/SL到達バー）で打ち切り。exit後は見ていない |
| 保有時間 | median=6本（1.5h）、p75=14本、max=96本 |

**median=-0.850%が全仮説で固定される理由（実測で確認）：**
```
SL負け: 55.8% + 同足両ヒット0.4% = 56.2%（過半数）→ avg_net=-0.850%=-(SL0.7+COST0.15)
TP勝ち: 42.6% → avg_net=+0.850%
EXPIRE: 1.2%
→ SL負けが過半数(56.2%>50%)なので中央値がSL負け額(-0.850%)に張りつく。
  これは「今回試した特徴量・条件範囲ではSL率が50%を割らなかった」=その範囲では
  入口に方向エッジが無いことの裏返し（全特徴量で不可能とは言わない）。
  シミュのバグや指標の歪みではなく、自然な結果。
```

### 検証した3アプローチすべてで合格ゼロ

| アプローチ | 候補数 | 結果 |
|---|---|---|
| 単一特徴量5分位スクリーン（9特徴量×5分位=45組合せ）SHORT | 113,540 | ❌全分位avg<0 |
| LONG鏡像仮説（同45組合せ・LONG方向） | 113,540 | ❌全分位avg<0（SHORTより悪い） |
| 仮説ベース・BTC文脈使用パターントリガー（3仮説） | 合計35,801 | ❌全仮説avg<0 |

### 仮説ベース3仮説の詳細結果

| 仮説 | n(test) | WR(train) | avg(train) | WR(test) | avg(test) | ベースライン比 |
|---|---|---|---|---|---|---|
| ベースライン（1h毎サンプル） | 33,600 | 42.7% | -0.123% | 43.9% | -0.102% | — |
| H1: BTC急落+Alt偽回復 | 2,395 | 40.8% | -0.156% | 42.8% | -0.122% | **悪化** |
| H2: 高値圏トリプル過熱 | 7,506 | 39.3% | -0.183% | 40.3% | -0.161% | **最も悪い** |
| H3: 急騰後失速 | 1,149 | 40.5% | -0.161% | 40.3% | -0.164% | **悪化** |

全仮説でmedian=-0.850%固定（変化なし）。leave-one-week-outも全週一貫してマイナス。

### H2の逆説（重要な市場構造の発見）

「24h高値2%以内 + RSI>65 + BB>1.5σ」は教科書的なSHORT好機であるはずが、**ベースラインより最も悪い**（WR 39.3%）。

→ 1/1-6/19の市場は強モメンタム継続相場。「高値・高RSI」は反転でなくトレンド継続シグナルとして機能。逆張りSHORT条件を重ねるほど悪化する。

### 結論（表現を弱める・断定しない）

```
1. 今回試した系統（単一分位45 + LONG鏡像45 + H1/H2/H3）では、
   手動運用水準のSHORT/LONG入口は見つからなかった。
   「入口パターンが存在しない」とは断定しない（網羅していない）。
2. SHORTが機能するのは暴落日のみ（short_trend top3日集中度 603% と整合）
3. 暴落日を予測できるのはパターン入口でなく危険日ゲート（rv_chg等）の系統
4. 同じ1/1-6/19データでこれ以上条件を足す探索は過学習なので、この系統は一旦終了。
   次の主軸：
   - 前向きrv_chg shadow データの蓄積とGO条件達成
   - SHORT候補全体→selected→notifyで劣化する理由の診断（後述）
   - 別時期（1/1-6/19以外）の暴落エピソード含む新データでの再検証
```

### H2の逆説が手動運用に与える示唆（重要）

「24h高値圏 + RSI>65 + BB>1.5σ」でSHORTが**最も悪化**したのは、手動運用でも重要な教訓：
```
見た目に「上がりすぎ・そろそろ下がりそう」な場面ほど、実際は踏み上げ継続だった。
→ 1/1-6/19の相場では「過熱→反転」でなく「過熱→継続」。
→ 手動でも「高値圏・高RSIだからSHORT」は危険。逆張りの直感を信用しない。
```

### 一旦終了する探索（同じデータでの繰り返しは過学習）

```
1/1-6/19期間のOHLCVを使った単純逆張りSHORT/LONG入口パターン探索の継続
  → 試した系統では全滅。以降の同データ探索は暴落日への過学習になる
LONG入口条件の改良
  → SHORTより悪い（WR 38-39%台）。TP/SL調整でも回復しない
H1/H2/H3の閾値微調整による再探索
  → 同じデータでの後付け条件足しは過学習
```

### 次の研究テーマ（単純な新条件探索ではなく、こちらに絞る）

```
1. rv_chg shadow + 3反転列の前向き観察（GO条件達成待ち・gate化しない）
2. SHORT候補全体から selected / notify で劣化する理由の診断
   （候補全体は相対的にマシなのに通知に近づくほど悪化する構造の特定）
3. 暴落日・危険日だけを事前に見分けられるか（パターン入口でなく危険日ゲート系統）
4. 入口を作るなら、同じ期間の後付け条件でなく、別期間・前向きデータで検証する
```

### 次のデータが来たときの確認事項

```
別時期（6月以外）の暴落エピソードを含む延長データ（or 別時期データ）が来たとき：
1. 暴落日にH1（BTC急落+Alt偽回復）がプラスに転じるか
2. rv_chg gate（BLOCK=暴落日）とH1/H2/H3候補の重複率
3. 暴落日限定でのH1 WRが60%以上になるか
→ 「暴落日だけエントリー」という戦略の検討（非暴落日は全エントリーゼロ）
```

---

## 【2026-06-20 SHORT選別ファネル検死】劣化は入口でなく「ランク選抜の瞬間」で起きている

**データ:** EntryRecord_3（SHORT DONE 43,170行・5/1〜6/19）。実績 PnL_Net (= PnL_Pct - 0.15, ground truth)。シミュ不使用。全指標: n/WR/avg/median/p25/p75/合計/active_days/zero_days/max_day/top3日集中度。

### ファネル本線（候補全体 → AI → ランク → 通知）

| 段階 | n | d | WR | avg | median | 動き |
|---|---|---|---|---|---|---|
| 1. SHORT候補全体 | 43,170 | 50 | 46.3% | -0.057% | -0.486% | — |
| 2. AI_Pass=1 | 5,540 | 46 | 47.2% | -0.027% | -0.471% | **改善** |
| 3. AI_Band=SHORT_AI_RANK（選抜前プール） | 4,059 | 44 | 48.6% | -0.006% | -0.402% | **さらに改善（ほぼ分岐）** |
| 4. **short_ai_rank_selected（上位N選抜）** | 181 | 5 | **26.5%** | **-0.418%** | -0.807% | **崩壊** |
| 5. Notify_Pass=1 | 549 | 21 | 30.1% | -0.305% | -0.656% | 低い |
| 6. Lane_Notify=True（実通知相当） | 188 | 27 | 34.6% | -0.314% | -0.545% | 低い |

### 確定した「劣化の起点」

```
候補全体 → AI通過 → ランク候補プール（段階1→2→3）は むしろ改善している
  WR 46.3% → 47.2% → 48.6%、avg -0.057% → -0.027% → -0.006%（ほぼ損益分岐まで上昇）

劣化は 段階4 = SHORT_AI_RANK が上位N件を選抜する瞬間 に起きている
  最良プール（4,059件 WR48.6%）から 最悪の181件（WR26.5%）を選び出している
  → 「入口が一様に弱い」だけではない。ランキング選抜が能動的に逆選別している
```

### 逆選別を裏付ける2証拠

```
証拠A（ランク選抜の逆転）:
  SHORT_AI_RANK（選抜前プール）  : WR48.6% / avg-0.006%
  SHORT_AI_RANK_REJECT（捨てた側）: WR50.6% / avg+0.091% / med+0.157%（プラス）
  → 捨てた方が選んだ方より良い = 完全な逆選別

証拠B（AI_Prob_Win 5分位が逆相関）:
  Q3中位 avg+0.105%（最良） vs Q5高スコア avg-0.017%（最悪）
  → AIが「勝ちそう」と高得点を付けたものほど負ける
  （CLAUDE.md 2026-06-12「AI_Prob_Winは逆方向に機能」を実EntryRecordで再確認）
```

### 解釈（入口 vs 選別の分離）

```
2つの問題が独立に存在する:
  (1) 入口候補プール自体が手動運用水準に未達
      （候補全体 median=-0.486%、平常日は負ける。暴落日依存）
  (2) その弱いプールから、ランキングが さらに悪い方を選んでいる（逆選別）

→ 「劣化はどこからか」の答え: 候補生成〜AI通過〜ランク候補プールではなく、
   SHORT_AI_RANK の最終選抜（ランキングモデル）が最大の劣化源。
→ ただし (1) があるため、逆選別を直しても手動運用シグナルにはならない
   （プールの天井が WR48.6%・avgほぼゼロ）。入口の天井が低いことは変わらない。
```

### 参考: Lane_Stage別・bypass別

```
Lane_Stage: reject avg-0.084% / research avg+0.015%(top3集中662%=暴落依存) /
            stable avg+0.107%(n=124,d=6薄い) / mid avg-0.191%
bypass short_a/b/c: 全てマイナス（short_a -0.107%, short_b -0.468%, short_c -0.208%）
  → bypass経路も救いにならない
```

### このデータでやらないこと（確定・変更なし）

```
SHORT_AI_RANK再開 / SHORT再開 / QS修正 / 逆選別を「直す」コード変更
  → プール天井が低い(WR48.6%/avgほぼ0)ため、逆選別を直しても手動運用水準に届かない
LONG_F停止 / rv_chg gate化 / TP/SL変更 / ENV変更
```

### この検死で確定したこと

```
1. SHORTの実通知が負けるのは「入口が弱い」と「ランクが逆選別」の二重構造
2. 劣化の最大の起点は SHORT_AI_RANK の最終選抜（証拠A・B）
3. 入口プールの天井自体が低い（WR48.6%/avgほぼゼロ・暴落日依存）ので、
   選別を直すだけでは手動運用シグナルにならない
4. → 本番選別ロジックを「直す」より、停止維持が正しい（逆選別を本番で動かさない）
5. 次に意味があるのは、別時期データでの入口の天井確認と、危険日ゲートの前向き検証
```

---

## 【2026-06-20 SHORT選別ファネル検死】劣化は入口でなく「ランク選抜の瞬間」で起きている

**データ:** EntryRecord_3（SHORT DONE 43,170行・5/1〜6/19）。実績 PnL_Net (= PnL_Pct - 0.15, ground truth)。シミュ不使用。全指標: n/WR/avg/median/p25/p75/合計/active_days/zero_days/max_day/top3日集中度。

### ファネル本線（候補全体 → AI → ランク → 通知）

| 段階 | n | d | WR | avg | median | 動き |
|---|---|---|---|---|---|---|
| 1. SHORT候補全体 | 43,170 | 50 | 46.3% | -0.057% | -0.486% | — |
| 2. AI_Pass=1 | 5,540 | 46 | 47.2% | -0.027% | -0.471% | **改善** |
| 3. AI_Band=SHORT_AI_RANK（選抜前プール） | 4,059 | 44 | 48.6% | -0.006% | -0.402% | **さらに改善（ほぼ分岐）** |
| 4. **short_ai_rank_selected（上位N選抜）** | 181 | 5 | **26.5%** | **-0.418%** | -0.807% | **崩壊** |
| 5. Notify_Pass=1 | 549 | 21 | 30.1% | -0.305% | -0.656% | 低い |
| 6. Lane_Notify=True（実通知相当） | 188 | 27 | 34.6% | -0.314% | -0.545% | 低い |

### 確定した「劣化の起点」

```
候補全体 → AI通過 → ランク候補プール（段階1→2→3）は むしろ改善している
  WR 46.3% → 47.2% → 48.6%、avg -0.057% → -0.027% → -0.006%（ほぼ損益分岐まで上昇）

劣化は 段階4 = SHORT_AI_RANK が上位N件を選抜する瞬間 に起きている
  最良プール（4,059件 WR48.6%）から 最悪の181件（WR26.5%）を選び出している
  → 「入口が一様に弱い」だけではない。ランキング選抜が能動的に逆選別している
```

### 逆選別を裏付ける2証拠

```
証拠A（ランク選抜の逆転）:
  SHORT_AI_RANK（選抜前プール）  : WR48.6% / avg-0.006%
  SHORT_AI_RANK_REJECT（捨てた側）: WR50.6% / avg+0.091% / med+0.157%（プラス）
  → 捨てた方が選んだ方より良い = 完全な逆選別

証拠B（AI_Prob_Win 5分位が逆相関）:
  Q3中位 avg+0.105%（最良） vs Q5高スコア avg-0.017%（最悪）
  → AIが「勝ちそう」と高得点を付けたものほど負ける
  （CLAUDE.md 2026-06-12「AI_Prob_Winは逆方向に機能」を実EntryRecordで再確認）
```

### 解釈（入口 vs 選別の分離）

```
2つの問題が独立に存在する:
  (1) 入口候補プール自体が手動運用水準に未達
      （候補全体 median=-0.486%、平常日は負ける。暴落日依存）
  (2) その弱いプールから、ランキングが さらに悪い方を選んでいる（逆選別）

→ 「劣化はどこからか」の答え: 候補生成〜AI通過〜ランク候補プールではなく、
   SHORT_AI_RANK の最終選抜（ランキングモデル）が最大の劣化源。
→ ただし (1) があるため、逆選別を直しても手動運用シグナルにはならない
   （プールの天井が WR48.6%・avgほぼゼロ）。入口の天井が低いことは変わらない。
```

### 参考: Lane_Stage別・bypass別

```
Lane_Stage: reject avg-0.084% / research avg+0.015%(top3集中662%=暴落依存) /
            stable avg+0.107%(n=124,d=6薄い) / mid avg-0.191%
bypass short_a/b/c: 全てマイナス（short_a -0.107%, short_b -0.468%, short_c -0.208%）
  → bypass経路も救いにならない
```

### このデータでやらないこと（確定・変更なし）

```
SHORT_AI_RANK再開 / SHORT再開 / QS修正 / 逆選別を「直す」コード変更
  → プール天井が低い(WR48.6%/avgほぼ0)ため、逆選別を直しても手動運用水準に届かない
LONG_F停止 / rv_chg gate化 / TP/SL変更 / ENV変更
```

### この検死で確定したこと

```
1. SHORTの実通知が負けるのは「入口が弱い」と「ランクが逆選別」の二重構造
2. 劣化の最大の起点は SHORT_AI_RANK の最終選抜（証拠A・B）
3. 入口プールの天井自体が低い（WR48.6%/avgほぼゼロ・暴落日依存）ので、
   選別を直すだけでは手動運用シグナルにならない
4. → 本番選別ロジックを「直す」より、停止維持が正しい（逆選別を本番で動かさない）
5. 次に意味があるのは、別時期データでの入口の天井確認と、危険日ゲートの前向き検証
```

---

## 【2026-06-20 逆選別原因の深掘り】selectedが崩壊した理由は「AIの逆選別」より「選抜時期の偶然」の可能性が高い

**追加分析:** rank_autopsy.pyで SHORT_AI_RANK内部構造、AI_Prob_Win分位full stats、仮想選択を検証。

### SHORT_AI_RANK プール内の構造

```
SHORT_AI_RANK プール(4,059件)の内訳:
  regime_policy_disabled: 3,864件 (95%) ← V2_SHORT_AI_NOTIFY_ENABLE=0でブロック
  short_ai_rank_selected:   181件 ( 4%) ← 実際に選ばれて通知候補になった
  その他:                    14件

→ プールの95%は「AIがランク候補と判定したが、レジームポリシーで止まった」
```

**selectedとregime_disabledの成績:**
| | n | d | WR | avg | med | leave-one-week-out |
|---|---|---|---|---|---|---|
| selected | 181 | **5** | 26.5% | -0.418% | -0.807% | n/a（d少なすぎ） |
| regime_disabled | 3,864 | 43 | 49.8% | +0.014% | -0.322% | 週23除外以外は全てavg+圏内 |

**selectedが崩壊した理由（決定的）:**

```
active_days = 5のみ（5/29〜6/1の5日間に集中）
この5日間はSHORTに最悪な相場（BTC上昇・SHORT全滅期）

特徴量の差（selected vs regime_disabled）:
  BTC_HTF_Strength: selected 0.176 vs regime 0.652 （大差）
    → "高BTC_HTF_Strength → SHORT WR51.4%"（CLAUDE.md既記載）の逆
    → selected は BTC_HTF_Strength が低い（SHORT不利）な時期に選ばれていた
  TotalScore:  selected 1.320 vs regime 1.764 （regime側が高い）
  P1_TrendScore: selected 1.404 vs regime 1.812 （regime側が高い）

結論: selectedの崩壊は「AIが逆選別した」より
     「選抜が行われた時期（d=5, 5/29-6/1）がSHORTに最悪な時期だったから」
      n=181/d=5では「AIの逆選別」と「時期の偶然」を区別できない
```

### SHORT_AI_RANK_REJECT (+0.091%/med+0.157%) が良い理由

```
日次推移で明確: 6/2(+116%), 6/4(+72%), 6/5(+259%), 6/6(+103%) の暴落4日で大勝ち
leave-one-week-out:
  週23(6/7-6/13)除外 → WR=42%/avg=-0.173%（プラス消える）
  top3日集中度 +217%（上位3日 = 6/5, 5/28, 6/2の暴落日）

→ REJECT側が良いのも暴落日エフェクト。安定して良いわけではない
→ 「捨てた側の方が良い」は偶然の暴落日集中と解釈すべき
```

### AI_Prob_Win 分位別（全SHORT・full stats）

| 分位 | 範囲 | WR | avg | median | 10/20/30日安定性 |
|---|---|---|---|---|---|
| Q1低 | 0.001-0.098 | 47.2% | +0.072% | -0.572% | 週23除外で avg-（不安定） |
| Q2 | 0.098-0.403 | 48.6% | +0.077% | -0.441% | 週23除外で avg-（不安定） |
| **Q3** | **0.403-0.604** | **50.6%** | **+0.105%** | **+0.159%** | **週23除外でもavg+（相対安定）** |
| Q4 | 0.605-0.748 | 49.4% | +0.025% | -0.344% | 週23除外でavg-（不安定） |
| Q5高 | 0.748-0.995 | 48.4% | -0.017% | -0.400% | 全週でavg-が多い（最悪） |

**Q3（中位帯 0.403-0.604）が最良。median+0.159%で唯一medianプラス。**
ただし手動運用基準（WR60%+/avg+0.20%+）には届かない（WR50.6%/avg+0.105%）。

### 仮想選択（ランク候補4,059件から181件選ぶ）

```
AI高スコアTOP181（現行相当仮想）: WR50.8%, avg+0.003%, d=23
AI低スコアBOTTOM181（逆選択）:   WR48.1%, avg-0.003%, d=34
SHORT_AI_RANK_REJECT上位181件:   WR17.1%, avg-0.496%, d=4  ← 最悪（最初の4日間が大崩壊）

→ どの仮想選択でも手動基準には届かない
→ プール天井（WR48.6%/avgほぼゼロ）を超えられない
```

### 確定見解（追加）

```
1. selectedの崩壊 = AIの逆選別 × 時期の偶然（d=5）の複合。分離不能
2. REJECT側の優位 = 暴落日エフェクト（leave-one-week-out で消える）
3. AI_Prob_Win Q3(中位)がmedian+0.159%で最も安定。ただし手動基準未達
4. 逆選別を修正しても手動運用シグナルにならない（プール天井が低い）
5. 「逆選別を直す研究価値あり」は本番実装では意味なし。ただしQ3中位帯は
   別時期データでも同傾向が維持されるかを前向き観察で確認する価値はある
```

### やらないこと（確定）

```
本番での逆選別修正（SHORT_AI_RANK_INVERTなど）
AI中位帯フィルターの本番投入
SHORT_AI_RANK再開・SHORT再開
```

### 次にデータが来た時の確認事項（追加）

```
AI_Prob_Win Q3（中位帯 0.4-0.6）の短期成績が次のEntryRecord でも維持されるか
BTC_HTF_Strength 別のSHORT成績（高BTC_HTF_StrengthでSHORT有利か）
```

---

## 【2026-06-20 合意・前向き観察フレーム確定】SHORTの到達点と次の観察タグ

**賢治さん合意の現状認識（断定を避けた最終整理）：**

```
SHORT入口プール：手動運用基準には未達（候補全体 median=-0.486%・暴落日依存）
SHORT selected崩壊：active_days=5（5/29-6/1）に集中。AIの逆選別か相場時期かは分離不可
SHORT_AI_RANK_REJECTのプラス：暴落日集中（top3集中+217%、週23除外でavg-0.173%）→採用不可
AI_Prob_Win Q3中位帯：相対的に良い（WR50.6%/avg+0.105%/med+0.159%）が手動基準未達
本番変更：なし
```

**前回までの「断定しすぎ」を2段階で修正した記録：**
```
段階1（誤）: 入口ロジックが主犯
段階2（誤）: SHORT_AI_RANKが完全に逆選別している（証拠A・Bで主張）
段階3（現）: selectedの崩壊は日数不足(d=5)で相場時期と分離不能。
            REJECT側の優位も暴落日依存。逆選別と断定できない。
            ただしAI_Prob_Win Q3中位 > Q5高スコア の構造は研究価値あり
```

### 次の観察タグ（次回EntryRecordから本格化・gate化しない）

| タグ | 現状のデータ状況 | 観察で確認すること |
|---|---|---|
| AI_Prob_Win Q3中位帯(0.4-0.6) | 既存データで最良・med+0.159% | 別日・別相場でも med>0 を維持するか。Q5高スコアより安定か |
| BTC_HTF_Strength 高/低 | 既存: 高→SHORT WR51.4% | 高BTCstrのSHORTが暴落日以外でもPnL_Netプラスか |
| rv_chg (BTC_RV_Chg等) | **EntryRecord_3に列なし**。次回から | long_f の rv_chg<5% PASS優位が前向きに継続するか |
| Short_Danger_Gate_Shadow | 同上（次回から） | 危険日BLOCKが暴落日に集中するか |
| 3反転列(BTC_RSI_15m他) | 6/19のみ150件＝初日 | 日数が溜まったら危険日ゲートのOOS検証 |
| G2_Gate | 6/12-6/19入っている | PASS>BLOCKを維持するか（既存検証では test で逆） |

### 確認の合言葉（次にデータが来たとき）

```
「暴落日依存ではなく、安定してPnL_Netが残るか」
  - top3日集中度が過大でないか（暴落日に利益が偏っていないか）
  - leave-one-week-out で符号が安定するか
  - median がプラスを維持するか
  - active_days が十分か（d<5は参考値）
→ これらを満たさない限り、Q3中位帯もrv_chgも本番gate化しない
```

### やらないこと（再確認・変更なし）

```
REST側採用 / rank反転 / AI閾値変更 / SHORT再開 / SHORT_AI_RANK再開
LONG_F停止 / rv_chg gate化 / TP/SL変更 / QS修正 / ENV変更
新条件の乱発探索（同一データへの過学習）
```

---

## 【2026-06-20 rv_chg ゲート付きSHORT検証】歴史データ5ヶ月で確定できる範囲

**データ:** 28銘柄 15m/1h OHLCV 1/1〜6/19（総候補113,540件）。train=1/1〜4/30（n=79,940）、test=5/1〜6/19（n=33,600）。TP=1.0%/SL=0.7%/COST=0.15%/最大96バー/同足両ヒット→SL優先。損益分岐WR=50.0%。

### rv_chg 閾値スイープ（0.00〜0.30）→ 全8閾値で全滅

| 閾値 | TEST WR | TEST avg | TEST median |
|---|---|---|---|
| >0.00 | 44.2% | -0.097% | -0.850% |
| >0.05 | 45.2% | -0.079% | -0.850% |
| >0.10 | 45.3% | -0.077% | -0.850% |
| >0.20 | 44.8% | -0.086% | -0.850% |
| >0.30 | 45.0% | -0.084% | -0.850% |

全閾値でmedian=-0.850%固定。WRが44〜45%台で横ばい、avg全てマイナス。

**確定：rv_chgをSHORT閾値ゲートとしても入口の問題は解決しない。** LONGでは効いたがSHORTでは逆（方向別ゲートが必要という既出結論と整合）。

### 暴落日限定SHORT（BTC日次<-2%）→ 唯一medianプラスに転換

| 区分 | n | d | WR | avg | median | top3集中 |
|---|---|---|---|---|---|---|
| TRAIN 暴落日 | 14,112 | 21日 | 54.9% | +0.084% | **+0.850%** | +50% |
| TEST 暴落日 | 5,376 | 8日 | 57.0% | +0.120% | **+0.850%** | **+90%** |
| TEST 平常日 | 28,224 | 42日 | 41.4% | -0.144% | -0.850% | -9% |

**median=+0.850% = TP勝ちが過半数。** 今回検証した全条件・全系統で唯一、medianがプラスに転換した断面。
TEST leave-one-week-out も全週プラス（w20除外:55%/+0.084% 〜 w25除外:59%/+0.160%）で比較的安定。

**ただし手動運用基準には届かない：**
- WR 57% → 基準60%（3ポイント不足）
- avg +0.120% → 基準+0.20%（0.08%不足）
- top3日集中度 +90%（TEST 8日のうち3日が利益の90%）

### 確定見解

```
1. rv_chg を SHORT 一般ゲートとして使うのは無効（全8閾値・全滅）
   SHORT方向ではrv_chg効果が逆（LONGと反対）=方向別設計不成立

2. 「BTC暴落日（<-2%）でのみSHORT」= 5ヶ月データで唯一medianプラスに転換した条件
   WR57%/avg+0.12%/median+0.850% → 手動運用基準(WR60%/avg+0.20%)には届かない

3. 暴落日SHORTのボトルネック:
   - TEST=8日/5ヶ月 → 月1.6回しか機会がない（手動運用としては稀）
   - top3日集中度=90%（3日で全利益）→ 1エピソード構造が残存
   - ただし leave-one-week-out で全週プラスは他条件より安定

4. 平常日（BTC≥-2%）のSHORTはWR41%/avg-0.144% = 明確な損失構造
   → 「平常日にSHORTを出してはいけない」は5ヶ月データで確定

5. 「rv_chg=危険日検知の本命仮説」は維持
   LONGで rv_chg<5% が平穏日を選ぶ方向性はSHORTとは別設計で評価する
   SHORTには「BTCが暴落中かどうか（方向量）」がrv_chgより直接的な変数
```

### このデータで確定できること・できないこと

```
確定できること（5ヶ月・歴史データで十分）:
- rv_chg threshold ゲートはSHORT方向で無効
- 暴落日SHORTはmedianプラスで方向性はある（ただし手動基準未達）
- 平常日SHORTは損失構造（確定）
- 月1.6回の暴落日では実用シグナルとして稀

確定できないこと（前向きデータが必要）:
- 今後の暴落日に同じWR57%/avg+0.12%が続くか
- top3日集中度90%が別の時期でも解消しないか
- 「暴落日を事前に知る」手段（rv_chg shadow列の前向き蓄積で確認中）
```

### やらないこと（確定・変更なし）

```
rv_chg を SHORT gate化（全閾値で無効・確定）
暴落日判定ゲートの本番実装（手動基準未達・BTC日次確定後にしか判定できない）
SHORT再開 / SHORT_AI_RANK再開 / TP/SL変更 / QS修正 / ENV変更
```

---

## 【2026-06-20 as-of暴落検知に置換】「暴落日SHORT median+0.850%」は未来情報のアーティファクトだった（確定）

**賢治さん指摘（重要・正しい）：** 直前の「暴落日（BTC日次<-2%）でのみSHORT → median+0.850%」は、**その日の終値（last）を見てから日を分類**している＝シグナル時点では未来情報。本番gateには使えない。検死としては「暴落日にSHORTは勝ちやすかった」で有効だが、「リアルタイムで暴落日を事前に見分けてSHORTできる」とは言えない。

→ BTC日次<-2%を、**シグナル時点で観測可能なas-of条件**に置き換えて再検証した。

**データ:** 28銘柄 15m/1h OHLCV 1/1〜6/19（総候補113,540）。train=1/1〜4/30、test=5/1〜6/19。TP=1.0%/SL=0.7%/COST=0.15%/最大96バー/同足両ヒット→SL優先。全BTC特徴量はエントリーバーまでの完了1h足のみ（as-of厳守・未来情報なし）。

### as-of 観測可能な7条件すべてで優位が消えた

| 条件（as-of・シグナル時点で観測可能） | TEST WR | TEST avg | TEST median | 判定 |
|---|---|---|---|---|
| ベースライン（無条件） | 43.9% | -0.102% | -0.850% | ❌ |
| c1: BTC 4h ret ≤ -1.5% | 34.1% | -0.269% | -0.850% | ❌最悪 |
| c2: BTC 24h ret ≤ -2.0% | 48.1% | -0.032% | -0.850% | ❌ |
| c3: BTC day-to-date ≤ -2.0% | 46.2% | -0.065% | -0.850% | ❌ |
| c4: BTC 1h ret ≤ -0.8% | 44.0% | -0.102% | -0.850% | ❌ |
| c5: BTC 24h安値更新中 | 41.1% | -0.150% | -0.850% | ❌ |
| c6: rv_chg>0.05 & BTC4h<0 | 42.2% | -0.132% | -0.850% | ❌ |
| c7: rv_chg>0.05 & BTC1h<0 | 42.6% | -0.124% | -0.850% | ❌ |

**7条件すべてで median が -0.850% に戻り、avg は全てマイナス。** 前回の終値ベース分類で出た median+0.850% / avg+0.120% は完全に消えた。

### 唯一プラスに見えた閾値も単一エピソード過学習

day-to-date 閾値を深くすると `≤ -3.0%` でTESTだけ僅かプラス（WR52.1%/avg+0.036%/median+0.850%、n=952/d=8）。だが：
```
- TRAIN は WR35.2%/avg-0.250%（train/test で符号逆転）
- top3集中度 +455%（利益が3日に極端集中）
- leave-one-week: w23除外で 29%/-0.364% に崩壊
→ 5〜6月の特定暴落エピソードへの後付け一致。汎化していない。採用不可
```

### 確定見解（最重要）

```
1. 前回の「暴落日SHORT median+0.850%」は終値ベース日分類＝未来情報のアーティファクト。
   シグナル時点で観測可能なas-of条件に置き換えると優位が消える（確定）。

2. as-of観測可能な7条件すべてでSHORTはavgプラスにも届かない（手動基準は論外）。

3. day-to-date≤-3%でtestだけ僅かプラスも、train逆符号・top3集中+455%・
   w23除外で崩壊 = 単一エピソード過学習。採用不可。

4. 「暴落日にSHORTが勝ちやすい」は"事後的に暴落日だったと判明した日"の事実であり、
   "シグナル時点でその日が暴落日になると見分ける"as-of条件では再現できない。
   → これがSHORT危険日ゲートの本質的な難しさ。
     CLAUDE.md既出「状態量だけでは6/7型を事前に区別できない」と同一メカニズム。

5. as-of暴落検知（4h/24h/day-to-date/1h ret・安値更新・rv_chg複合）は
   今回試した範囲では本番SHORT gateとして使えない。
   （"存在しない"とは断定しない。試したのは上記7条件＋day-to-date閾値スイープのみ）
```

### この検証で確定したこと

```
- SHORTを"いつでも出せるシグナル"にする道は、現データのas-of条件では見つからない
- 「平常日SHORTは損失構造」は維持（前回確定）
- ただし「暴落日だけ出せば勝てる」も、as-ofで暴落日を当てられないため実用不可
- SHORT停止維持が引き続き正しい
```

### やらないこと（確定・変更なし）

```
as-of暴落検知のSHORT gate化（今回試した7条件＋閾値で全滅）
BTC日次<-2% gate化（未来情報・本番不可）
SHORT再開 / SHORT_AI_RANK再開 / rv_chg gate化 / TP/SL変更 / QS修正 / ENV変更
```

### 次にデータ・検証で意味があること

```
- 前向きの rv_chg shadow列 / 3反転列の蓄積（暴落"前兆"のas-of検知は別の特徴量が要る）
- 別時期（6月以外）の暴落エピソードを含む新データでの再検証
  （現データは暴落がtest期間に集中＝どんなas-of条件もtest暴落に後付けで当たる構造）
- "暴落の最中"でなく"暴落の入口・前兆"をas-ofで捉える特徴量があるか
  （今回の7条件は全て"既に下げている"状態量。前兆量はまだ未検証）
```

---

## 【2026-06-20 順張り・スイング検証＋トラック分離合意】今回試した15分短期ロジック群でエッジ未発見／S4スイングは暴落依存

**経緯：** 賢治さん「もう待つな、勝てるシグナルを出せ」。これまで試した入口が全て"逆張り"だったと気づき、未検証の"順張り（トレンドフォロー）"と"時間軸を上げたスイング"を即検証した。本番変更なし・branch検証のみ。

### 事実1：今回試した15分短期ロジック群では手動運用基準を満たすエッジが見つからなかった

**表現の注意（賢治さん指摘・厳守）：** 「15分足はエッジなし（構造的・確定）」は言い過ぎ。正しくは「**今回の5ヶ月データ・今回試した15分短期ロジック群（逆張り＋順張り×決済7種）では、手動運用基準を満たすエッジは見つからなかった**」。15分足全般の否定ではない。ただし実務判断として「今の路線でさらに条件を足して探すのはやめる」は妥当。

```
順張り入口4種（ドンチャン20上抜けLONG / 下抜けSHORT / EMA順張りLONG / モメンタムLONG）
× 決済7種（固定対称/利大損小/トレーリング/時間）= 全て手動基準未達。
- 全てWR40%前後・avgマイナス・median=-0.850%固定
- 逆張り(既出)も順張りも今回の範囲では勝てなかった
→ 解釈: 15分足でTP1%級の小さい値幅を取りに行くと往復0.30%コストの影響が大きい。
  「入口・決済の設計」を変えても今回の範囲では超えられなかった。
  （15分足が原理的にダメと断定はしない。今回試した系統での結果）
```

### 事実2：S4（alt 55h下抜けSHORT・大TP/SL・スイング）は派手だが暴落依存

```
S4 = ドンチャン55h下抜けSHORT, TP12%/SL6%, 7日保有, 1日1回間引き, as-of厳守。
単純合計では TEST WR61.6%/avg+2.995%/median+2.76% と「初の好成績」に見える。
だが診断で暴落依存が確定:

月別avg: 01月+5.6% / 02月-1.5% / 03月-1.1% / 04月-3.3% / 05月+2.7% / 06月+3.2%
  → 暴落月(1/5/6)で大勝ち、平穏月(2/3/4)で3ヶ月連続負け
BTC日次帯別: BTC<-3%でavg+3.6% / BTC>+1%でavg-5.1%(WR8.8%壊滅)
  → 「BTCが落ちる時だけ勝ち、上がる時に壊滅」= 純粋な暴落捕獲ベット
as-of BTCトレンドフィルター(BTC<EMA50/EMA100/24h<0)を掛けても月別構造は不変
  → 平穏月のダマシ下抜けを除去できない（暴落検知の壁と同じ）
```

### 事実3：資金曲線レベルで見ると暴落除外でマイナス（賢治さん指摘で実施）

**「合計+1075%は1取引PnLの単純合計＝実運用と別物」という指摘を受け、ポートフォリオ制約付きで再評価：**

```
ポジション上限なし: 最大同時保有28・最大DD-718%・最大連敗36（単純合計は水増し）
上限5: 取引158 WR51.3% 実現+200% avg+1.27% 最大DD-144% 最大連敗12
上限3: 取引99  WR49.5% 実現+123% avg+1.24% 最大DD-72%  最大連敗8
銘柄集中: 最上位銘柄11%（分散OK・1銘柄の運ではない）

★決定的: 暴落3ヶ月(1/5/6)を除外すると（上限5）→ 取引82 実現-51% avg-0.623% WR39%
  → 暴落月を除くとS4はマイナス転落。暴落依存が資金曲線でも確定。
```

### S4の最終評価

```
全期間as-of・上限付きで実現プラス（+200%@上限5）かつ銘柄分散 = 研究価値はある
だが本質は暴落捕獲システム:
  - 平穏期はマイナス（2/3/4月）
  - WR48%/median負 = 高勝率型ではない（低勝率・高ペイオフのトレンドフォロー）
  - 最大連敗8〜12・数ヶ月DD = 手動運用には精神的に過酷
  - 暴落除外で負け = 次の半年に暴落が無ければ沈む
→ 手動の高勝率シグナルにはならない。
```

### 【合意・トラック分離】2026-06-20

```
本番・手動通知の主戦場 = 高勝率型(WR60%/median+)のみ。S4は混ぜない。
S4 = 別研究トラック。本番採用・SHORT再開・SHORT_AI_RANK再開の根拠にしない。
  - 評価は単純合計でなく資金曲線（DD/連敗/同時保有/ポジ上限/暴落依存除去）で行う ← 実施済み
  - 7日保有でDiscord短期通知Botとは性格が違う → 別シート・別評価で扱う
高勝率型探索とS4トレンド型研究は混ぜず別管理。
```

### この検証で確定したこと

```
1. 今回試した15分短期ロジック群（逆張り・順張り・全決済）では手動運用基準を満たすエッジ未発見
   （15分足全般の否定ではない。同一データへの条件足し探索は過学習なので一旦終了）
2. 唯一プラスに見えたS4スイングSHORTは暴落依存（暴落月除外で-51%）
3. 「勝てるシグナル」候補は現データでは:
   - 高勝率型: 未発見（5ヶ月データで存在を確認できていない）
   - トレンド型(S4): 期待値は出るが暴落依存・低勝率・手動基準外
4. 本番は全凍結が引き続き正しい
```

### 現在の固定（本番・変更なし）

```
SHORT再開なし / SHORT_AI_RANK再開なし / rv_chg gate化なし
TP/SL変更なし / QS修正なし / ENV変更なし / LONG_F停止なし
S4を本番通知に混ぜない / S4をSHORT再開・本番復帰の根拠にしない
```

### やらないこと（確定・変更なし）

```
S4の本番投入 / SHORT再開の根拠化 / SHORT_AI_RANK再開
高勝率型とトレンド型を同じ通知に混ぜる
同一5ヶ月データへの条件足し探索（過学習）
```

### 次にやること（2つに絞る・賢治さん指示 2026-06-20）

```
同じ5ヶ月データをさらに掘らない。次はどちらか:

1. [高勝率型トラック] rv_chg shadow・3反転列の前向き観察
   - 暴落"前兆"のas-of検知に必要な列を前向きに蓄積（gate化はGO条件まで保留）

2. [S4研究トラック] より長期OHLCVで別相場期間を含めた資金曲線検証
   - S4は暴落依存なので5ヶ月では不足。本物か判断するには最低限:
     2024年・2025年・2026年で月別 / 暴落が少ない期間 / 上昇トレンド期間 /
     レンジ期間 / 最大DD / 資金曲線 / 同時保有上限 まで見る
   - 暴落"前兆"as-of検知が出来れば、S4は優秀な"執行の器"として再評価可能
     （勝ち+7.8%/負け-5.4%のペイオフ非対称が大きく、弱い前兆予測でも黒字化余地）
```

### このセッションの最終結論（言い切らない整理）

```
× 「勝てるシグナルを発見」ではない
○ 短期高勝率型は今回の検証範囲（5ヶ月・今回のロジック群）では未発見
○ S4は別ジャンル（暴落捕獲・低勝率高ペイオフ）の研究枠。本番には混ぜない
○ 本番は凍結維持が引き続き正しい
```

---

## 【2026-06-20 クロスセクション・モメンタム発見】方向に賭けない市場ニュートラル＝初の頑健な候補

**経緯：** 賢治さん「新データを待つのは今のデータを使いこなせていないだけ」。正しい。これまでの全失敗（逆張り/順張り/rv_chg/暴落検知/S4）の共通原因は**全て「BTC方向への賭け」→ 暴落依存**だった。一度も試していなかった「方向に賭けない＝市場ニュートラル（相対強弱）」を同じ5ヶ月データで検証した。本番変更なし・branch検証のみ。

### シグナル定義（as-of厳守・1h足・欠損補完なし）

```
28銘柄を「過去L本のリターン」でランク付け（t時点で観測可能）
  → 上位n銘柄LONG / 下位n銘柄SHORT を同時保有（市場ニュートラル）
  → H本後に決済（非重複リバランス）。各レッグ往復コスト0.15%控除
最良: mom（強い物買い・弱い物売り）, L=168(7日), H=48(2日保有), n=5
```

### S4を殺した全検証を通過（頑健）

| 検証 | 結果 |
|---|---|
| train/test | TRAIN WR59.6%/avg+0.204%/med+0.335% ・ TEST WR62.5%/avg+0.626%/med+0.309% 両方+ |
| パラメータ近傍 | L=144〜192・H=48〜72が一様に+（単一セルの運でない） |
| コスト感応度 | 往復0.30%/legまで黒字維持（0.40%で train僅かマイナス） |
| leave-one-month-out | **全月除外で全体+維持**（5月除外でも+0.150%）＝単月依存でない |
| 月別 | 1月+0.21/2月+0.20/3月+0.19/4月+0.23/5月+1.12/6月-0.19（5/6ヶ月+） |
| mom vs rev | momは常に+、revは常に−（鏡像）＝本物の方向性 |
| 資金曲線 | 全81回 WR60.5% med+0.335% 累積+26.6% **最大DD-4.8%** 最大連敗3回 |

**S4と決定的に違う：暴落依存でない。** 平穏月(2/3/4月)が独立に+。BTC方向を打ち消し「強い銘柄は強いまま/弱い銘柄は弱いまま」の持続性だけで稼ぐ。

### レッグ分解（エッジの所在）

```
LONG leg（強い5銘柄買い）  : WR45.7% avg-0.120% med-0.558% → 負け（BTCベータ・corr+0.74）
SHORT leg（弱い5銘柄売り） : WR60.5% avg+0.778% med+1.129% → 強く+（ただしBTC下落ベータ込み）
SPREAD（市場ニュートラル）: WR60.5% avg+0.329% med+0.335% → BTC控除後の本体（防御的に正しい指標）
最強1銘柄だけLONG         : WR34.6% avg-0.975%             → 最悪（単一銘柄では機能しない）

→ エッジは「弱い銘柄を空売りする側」＝SHORT主軸と一致
→ ただし単一銘柄では不可。相対ランク/バスケットとして初めて機能
→ 生SHORT leg+0.778%はBTC下落ベータで水増し。SPREAD+0.329%が本物
```

### 正直な弱点（誇張しない）

```
- 2日ごと10建玉のバスケット戦略。Discord単発通知とは形が違う
- 6月-0.19%と直近で弱含み（cross-sectional momentum crash のregimeリスク）
- 1リバランス+0.33%は堅実だが派手でない・81サンプル
- 多重検定でbestを選んだ（ただし近傍安定・leave-one-month・mom/rev一貫で相当緩和）
```

### 位置づけ・次にやること（新データ不要）

```
これは「短期高勝率の単発手動シグナル」ではないが、研究全体で初めて
暴落非依存・train/test+・全頑健性通過の候補。トラック新設:

[クロスセクション・トラック]（最有望・本番候補の研究）
1. 採用銘柄の回転率・スリッページ・現実コストの精緻化（survives 0.30%/legは確認済）
2. 運用形態の決定: 市場ニュートラルbasket vs 弱モメンタムSHORT-leg
3. botにshadow実装: 7日リターンランクと選抜バスケットを記録だけ→前向き検証
   （実通知・本番選別に一切影響しない。rv_chg shadowと同じ方式）
4. momentum-crash regime耐性（6月弱含みの追跡・別時期データで再現性）

本番は引き続き全凍結（SHORT再開なし/SHORT_AI_RANK再開なし/rv_chg gate化なし/
TP/SL変更なし/QS修正なし/ENV変更なし/LONG_F停止なし）。
クロスセクションも shadow記録のみ・gate化や実通知化はGO条件確認まで行わない。
```

---

## 【2026-06-20 cs_mom_shadow Phase 1 配線確認合格】前向き観察フェーズ開始

**確認日: 2026-06-20。コミット: `594d5b5`（fix: run/rank_candle分離 + 固定48hグリッド）。**

### 確認結果

| 確認項目 | 結果 |
|---|---|
| cs_mom_shadow 初回記録 | ✅ 成功（rank_candle=2026-06-20 21:00:00 JST） |
| 2回目以降・同一48hスロット重複記録なし | ✅ OK（`recorded basket` が増えていない） |
| notify_candidates=0 / discord_sent=0 | ✅ 実通知なし維持 |
| AI-TRACE notify=1 の扱い | ✅ 内部trace表示のみ。Discord送信なし（想定通り） |
| SHORT通知停止の継続 | ✅ OK |

### 記録内容（初回）

```
rank_candle_time_jst = 2026-06-20 21:00:00
expected_eval_time_jst = 2026-06-22 21:00:00 相当
run_time_jst = Cloud Run実行時刻（別カラムで記録）
```

### 次の確認タイミング

| 時期 | 目的 |
|---|---|
| **6/22 21:00 JST以降** | cs_mom_shadowシートに2本目が1行だけ追加されているか確認 |
| 7日後（〜6/27） | 軽い傾向確認（バスケット3〜4回分） |
| 30日後 | 本格判断（leave-one-week-out相当・GO条件チェック） |

**それまでの間、何度Botが走っても `recorded basket` が増えないのが正常。**

### 現在の観察ルール（変更なし）

```
cs_mom_shadow は record-only・gate化しない
V2_CS_MOM_SHADOW_ENABLE=1（賢治さんがENVでON済み）
SHORT再開なし / SHORT_AI_RANK再開なし / rv_chg gate化なし
TP/SL変更なし / QS修正なし / LONG_F停止なし / 追加ENV変更なし
```

### GO条件（gate化・実通知化の前に必ず満たすこと）

```
1. leave-one-week-out で SPREAD avg+ が残る（単一週依存でない）
2. 全週でSPREAD medianプラスを維持
3. momentum-crash regimeで崩れていないか（6月弱含みが継続しないか）
4. 暴落依存でない（top3日集中度が過大でない）
→ これらが揃うまでは gate化・実通知化をしない
```

---

## 【2026-06-20 cs_mom 看板補正】「かなり頑健な候補」は楽観的すぎた。候補だが勝てるシグナル確定ではない

**前向き観察開始前の防御的オフライン分析（momentum-crash regime特性化）の結果、前回の「暴落非依存・全頑健性通過の候補」という表現を補正する。新条件探索ではなく、何がこの戦略を殺すかの確認。**

### 確定した補正（記載値定義 cost 0.15/leg・mom L168 H48 n5）

```
cs_mom はバックテスト上、他候補より筋が良いが、勝てるシグナル確定ではない。
- 5月の強いモメンタム月（avg+1.117%/WR73%）が全体を押し上げている（外れ値）
- 6月はマイナス（avg-0.192%/WR44%、直近6リバランスは avg-0.221%/WR33%）
- 現実的な往復コスト（0.30%/leg）前提では平常月（1〜4月）の地力は+0.054%まで低下し薄い
- バックテスト合格 ≠ 勝てるシグナル（S4の教訓と同じ）
→ 本番採用判断ではなく、前向きshadowで6月以降のmomentum-crashが
  一時的か、エッジ減衰かを見る段階。
```

### 月別 SPREAD（記載値定義 cost 0.15/leg）

| 月 | n | avg | WR | 評価 |
|---|---|---|---|---|
| 2026-01 | 12 | +0.207% | 58% | 基準ぎりぎり |
| 2026-02 | 14 | +0.195% | 64% | 基準ぎりぎり |
| 2026-03 | 16 | +0.188% | 56% | 基準やや下 |
| 2026-04 | 15 | +0.226% | 60% | 基準ぎりぎり |
| 2026-05 | 15 | **+1.117%** | 73% | **突出（外れ値）** |
| 2026-06 | 9 | **-0.192%** | 44% | **マイナス（直近）** |

### 5月依存（表現を正確に・「崩れる」は強すぎ）

```
全期間            avg=+0.329%
5月除外           avg=+0.150%  ← 5月を抜いてもプラスは残るが、
                                  手動運用基準(+0.20%)には届かない水準まで低下
1〜4月のみ        avg=+0.204%（記載値定義）/ +0.054%（現実的往復0.30%/leg）
```

**注意：** 記載値の +0.329% は cost 0.15/leg（片道寄り・楽観）。現実的な往復0.30/leg では全期間+0.179%、1〜4月地力+0.054%。両方ともコスト感応度表に既出で矛盾なし。

### 前向き観察の意味（今回だけ「待つ」に意味がある理由）

```
cs_mom は 48hで1サンプル の戦略。同じ5ヶ月データをこれ以上掘るのは過学習（禁止）。
よって前向きデータが唯一の有効な次の入力。今回の補正で観察の判定基準が明確になった：
  「6月のmomentum-crashが一時的に回復するのか、エッジ減衰の始まりなのか」を
  これから溜まるSPREAD符号で見分ける（机上では判定不能）。

次のテスト：
1. 6/22 21:00 JST以降に2本目が正しく1行だけ増えるか
2. 数本たまった時点でSPREADがプラス維持か
3. 6月の失速が続くか回復するか
```

### 本番状態（変更なし・全凍結）

```
cs_mom_shadow はONのまま前向き記録（record-only・本番通知に混ぜない）
SHORT再開なし / SHORT_AI_RANK再開なし / rv_chg gate化なし
TP/SL変更なし / QS修正なし / ENV追加変更なし / LONG_F停止なし
```

---

## 【2026-06-20〜22 SHORT_ML_SHADOW 不採用決定】OKX検証で全条件不合格

**記録日: 2026-06-22。不採用決定。コード変更・マージ・デプロイは一切なし。**

### 不採用の経緯

commit `052f508` で branch `claude/crypto-bot-assistant-QlA5G` に SHORT_ML_SHADOW（LightGBM walk-forward shadow実装・452行）を追加したが、以下の理由でそのまま凍結・不採用とした。

### 不採用の5理由（独立して各々が不採用根拠）

1. **OKX検証・全パラメータ不合格（決定的）**
   - `tf_thr_validate_okx.py`（23銘柄・walk-forward・PCT=98%・6基準）で検証
   - TP/SL 全組合せ（TPリスト×SLリスト）で6基準（WR≥60%/avg≥+0.15%/median>0/top3除外avg+/LOWO全週+/active_months≥6）を満たすものゼロ
   - 検証コードにバグ・方法論上の問題なし（compute_labels/gen_select/robustness 全て正しい）

2. **lightgbm が requirements.txt に存在しない → Cloud Run では自動無効化**
   - `try: import lightgbm` で guard されており、import失敗時は `_SHORT_ML_LGB_OK=False` で全機能スキップ
   - 本番に push しても実害ゼロだが、機能しないコードを本番に入れる意味もない

3. **入口候補プール（母集団）自体がエッジなし**
   - SHORT候補全体 median=-0.486%・暴落日依存（CLAUDE.md 2026-06-20 確定事実）
   - AIランキングをどう設計しても、プール天井（WR48.6%/avgほぼゼロ）を超えられない
   - ランキング改善より危険日ゲートが本筋（分析フレームが変わっている）

4. **現行SHORTモデル（AI_Prob_Win）は逆方向に機能**
   - Walk-forward AUC平均0.376、Top20% WR57.4% < Bottom80% WR69.7%（CLAUDE.md 2026-06-12確定）
   - 同じ設計思想でSHORT_ML_SHADOWを作り直しても同じ結果になる可能性が高い

5. **現時点でSHORTのAI再学習に進むフェーズでない**
   - Phase 1（defensive filter + QualityScore + shadow安定化）→ Phase 2（データ蓄積）→ Phase 3（AI再学習）の順序を厳守
   - 3反転列・rv_chg shadow・cs_mom shadow の前向き観察が先

### やらないこと（確定）

```
SHORT_ML_SHADOW の本番マージ・デプロイ
lightgbm の requirements.txt 追加
SHORT_ML_SHADOW の再最適化（TP/SL/PCTの再探索）
欠損銘柄をspot OHLCVで補完して再検証
23銘柄版での条件再最適化
「惜しい条件」の shadow 実装
```

### コードの扱い

```
branch: claude/crypto-bot-assistant-QlA5G に 052f508 として残存
main へのマージ: しない
Cloud Run デプロイ: しない
requirements.txt 変更: しない
コードの削除: しない（研究記録として保持）
実害: なし（lightgbm 未インストールで自動無効化）
```

---

## 【2026-06-22 EntryRecord_3 健全性診断】6/13〜6/22 前向き観察データ確認

**データ：** EntryRecord_3.xlsx（DONE 62,258行、5/17〜6/22）、MD5=ce54968e。全V2-Shadow/V2-FeatureProbe。

### 診断結果サマリー

| 項目 | 結果 | 判定 |
|---|---|---|
| SHORT通知 (6/19以降) | 0件 | ✅ 停止維持 |
| LONG_E通知 (6/12以降) | 0件 | ✅ 停止維持 |
| LONG_F (6/13〜6/22) | n=57, WR=52.6%, avg=+0.041%, med=+0.335% | ⚠️ 観察継続 |
| rv_chg shadow | 6/20以降 3,466/4,086件 非NaN記録中 | ✅ 正常 |
| 3反転列 | 6/19以降 4,874件 全列非NaN | ✅ 正常 |
| cs_mom_shadow | 2本記録（6/20 21:00, 6/21 09:00）OPEN | ✅ 正常 |
| lt_stable (6/20〜6/21) | n=15, WR=40%, avg=-0.123% | ❌ 基準未達・変更なし |

### LONG_F 日別（6/13〜6/22）

| 日 | n | WR | avg |
|---|---|---|---|
| 6/13 | 6 | 83% | +0.390% |
| 6/16 | 20 | 55% | +0.464% |
| 6/17 | 21 | 29% | -0.676% |
| 6/20 | 1 | 100% | +0.637% |
| 6/22 | 9 | 78% | +0.474% |

5日中4日プラス。停止基準（実通知8件+・WR35%未満・avgマイナス複数日）非該当。観察継続。

### cs_mom_shadow 確認

```
2本目（6/21 09:00スロット）: 正常追加 ✅ 重複なし
期待eval時刻: row0=6/22 21:00 / row1=6/23 09:00（いずれもOPEN）
PnL: まだ未確定（評価時刻未到達）
次の確認: 6/23 09:00以降に row1 が DONE になるか
```

### 現状維持（変更なし）

```
SHORT停止維持 / LONG_E停止維持 / LONG_F観察継続
rv_chg gate化しない / cs_mom gate化しない / lt_stable変更なし
SHORT再開なし / LONG_F停止なし / QS修正なし / ENV変更なし
```

---

## 【2026-06-22 ブランチ防御原則の確定】不採用コードは本番にも main にも入れない

**賢治さん指摘（2026-06-22・厳守）：**

### 誤った安全網認識（禁止）

```
✗「lightgbm が requirements.txt にないから自動無効 → 実害なし → deployしてもいい」
→ これは安全網として頼ってはいけない。
  将来 requirements.txt に lightgbm が追加されたり、別修正で有効化されると
  不採用の SHORT_ML が本番で動く。fragile な防御線に依存するな。
```

### 正しい防御原則（絶対厳守）

```
不採用コードは、本番（Cloud Run）にも main ブランチにも絶対に入れない。
「自動無効になるから大丈夫」を根拠に deploy / merge することは禁止。
```

### 現ブランチの構造（確認済み・2026-06-22）

```
branch: claude/crypto-bot-assistant-QlA5G
main.py 内の混在状態:
  採用済み（本番稼働中）: rv_chg shadow（dab5a28）/ cs_mom shadow（6c7757f）
  不採用（本番未投入）:  SHORT_ML_SHADOW（052f508）
  → Excelの列（74列）にSHORT_ML系の列がないことで本番未投入を確認済み

守るべき防御線:
  1. このブランチを Cloud Run に deploy しない
  2. このブランチを main に丸ごと merge しない
```

### CLAUDE.md だけ残したい場合の正しい方法

```
NG: このブランチ HEAD から docs/CLAUDE.md だけ抜いてマージ
    → 万が一の操作ミスで main.py も混入するリスクあり

OK: main 相当から新しいブランチを作り、CLAUDE.md 部分だけ cherry-pick または手動適用
    → SHORT_ML が正史コードになるリスクがない
    → 現時点では実施不要。必要になったときに実施する
```

### 次の監視ポイント（追加開発なし）

```
1. 6/23 09:00以降: cs_mom_shadow row0 の eval_status が DONE になるか / PnL符号確認
2. LONG_F: あと数日の実通知を観察（改善継続 or 悪化で判断を更新）
```

---

## 【2026-06-23 OKX長期再検証】cs_mom / rv_chg / S4 を2024-2026で正式に「切る／残す」判定

**目的：** 研究候補に残っていた cs_mom / rv_chg / S4 を、OKX長期OHLCVで正式に採否決定する（"正式に切る／残す作業"）。本番変更・deploy・merge は一切なし。研究用スクリプトのみ。

**スクリプト：** `okx_offline_verify.py`（branch `claude/crypto-bot-assistant-QlA5G`、commit `2917cdc`、562行・pure pandas/numpy・LightGBM非依存）。

### データの正確な記録（重要・誤記訂正）

```
使用ディレクトリ: /tmp/ohlcv_long/ohlcv_2024_2026/ （23銘柄版 /tmp/ohlcv_okx ではない）
  - /tmp/ohlcv_okx は本セッションでは空だった
  - 使ったのは「long」歴史データ。OKX swap形式（source_symbol=*/USDT:USDT）
  - 28銘柄すべてが 2024-01-01〜2026-06-21・各21,649行で揃っている（=23銘柄版より広い）

注意（誠実に記録）:
  - 28銘柄全部が「同一開始日・同一行数(21649)」で綺麗に揃っている
  - 新規上場トークン(POL=旧MATIC/SUI/SEI/BONK等)の2024年初頭足はバックフィル/合成の可能性
  - ただし銘柄構成・多少の合成に関わらず「不採用」の結論は不変（全指標が大きく基準割れ）
評価条件: TP/SL/cost各セクション記載、as-of厳守、損益分岐は各戦略の幾何に依存
判定基準: WR≥60% / avg≥+0.20% / median>0 / top3日除外+ / LOMO全月+ / zero_months≤2 / maxDD許容
```

### Section 1: cs_mom（L=168h rank / H=48h hold / n=5 / market-neutral SPREAD）→ **不採用確定**

| 指標 | 値(0.15%/leg) | 基準 | 判定 |
|---|---|---|---|
| avg | **-0.121%** | ≥+0.20% | ✗ |
| median | -0.074% | >0 | ✗ |
| WR | 47.7% | ≥60% | ✗ |
| LOMO | **全30ヶ月マイナス** | 全月+ | ✗ |
| zero_months | 20/30 | ≤2 | ✗ |
| maxDD | -59.9% | 許容 | ✗ |
| top3日除外 | -0.185% | + | ✗ |
| コスト0.08%/legでも | -0.051% | + | ✗ |

```
- mom > rev（avg-0.121% > -0.179%）= 方向性は弱く存在するが、全コストでマイナス
- 5ヶ月Bybitの avg+0.329% は期間の偶然。2.5年・28銘柄では全コスト・全月で赤字
- 前回「かなり頑健な候補」評価は楽観すぎ。本検証で確定不採用
- 今後 cs_mom_shadow が1〜2本勝っても期待しない（唯一の残り候補ではなくなった）
```

### Section 2: rv_chg（BTCボラ変化ゲート・LONG方向）→ **gate化不可だが環境認識特徴量として価値は残す**

```
[市場全体LONGリターン（ランキングなし）で検証] → 全閾値で PASS > BLOCK 一貫:
  thr=3%: PASS+0.089% vs BLOCK+0.039%  ✓
  thr=5%: PASS+0.182% vs BLOCK-0.119%  ✓
  thr=7%: PASS+0.139% vs BLOCK-0.066%  ✓
  thr=10%: PASS+0.106% vs BLOCK-0.025% ✓
  → rv_chg<5%（平穏）が市場全体LONG方向を弱く正しく分離する（環境認識として実在）

[cs_mom LONGレッグに適用] → PASS+0.048% > BLOCK+0.012% だが手動基準未達:
  - top3日除外 -0.236% ✗ / LOMO 5ヶ月マイナス ✗ / LOWO 9週マイナス ✗
  - ランキング+コストが乗るとエッジが消える

確定: 単体gate化不可 / cs_momとの組み合わせ不可 / 手動通知シグナルにもならない
      → record-only で蓄積継続が正解（環境特徴量としての価値は否定しない）
```

### Section 3: S4（Donchian 55h下抜けSHORT / TP12% / SL6% / 7日保有 / ポジ上限付き）→ **不採用確定**

| 指標 | pos_limit=5 | 基準 |
|---|---|---|
| avg | +0.304% | △（平均だけプラス） |
| **median** | **-3.953%** | ✗ |
| WR | 42.4% | ✗ |
| zero_months | 12/30 | ✗ |
| **maxDD** | **-98.8%** | ✗（許容不可） |
| 最大連敗 | 17 | ✗ |
| LOMO全月+ | ✓ | （暴落が各年分散のため） |
| top3日除外 avg | +0.110% | ✓ |
| **暴落月除外後 avg** | **-0.093%** | ✗（暴落依存確定） |

```
暴落月(BTC月次<-10%): avg+1.966% / 暴落月除外: avg-0.093%
→ 「暴落時だけ当たり、平常時は削られる」型。平均プラスでもmedian-4%・maxDD-99%で危険すぎる
→ 5ヶ月分析の暴落依存結論が2.5年で確定。本番候補から除外
```

### この検証で確定したこと（研究ルートの棚卸し）

```
cs_mom    : 不採用確定（全指標基準割れ・LOMO全30ヶ月マイナス）
rv_chg    : 方向感はあるがgate化不可。環境認識特徴量としてrecord-only継続
S4        : 不採用確定（median-4%・maxDD-99%・暴落依存）
SHORT_ML  : 不採用済み（OKX全パラメータ未達・2026-06-22）
→ 候補だった研究ルートはほぼ全て本番採用不可。残念だが判断としては前進
```

### 現在の本番状態（変更なし・全凍結）

```
SHORT停止維持 / LONG_E停止維持 / LONG_F観察継続
rv_chg shadow・3反転列・cs_mom_shadow は record-only で蓄積継続
cs_mom 本番採用しない / S4 本番採用しない / rv_chg gate化しない
QS修正なし / TP/SL変更なし / ENV変更なし
```

### ブランチ防御（再確認・厳守）

```
branch claude/crypto-bot-assistant-QlA5G には main.py 変更が依然含まれる（不採用コード混在）
このブランチを: PR化しない / merge しない / Cloud Run deploy しない
okx_offline_verify.py を残したいだけなら、後で main相当からクリーンブランチを作り
検証スクリプトだけ移すのが安全（現時点では実施不要）
```

### 次にやること（追加探索は止める）

```
1. 最新EntryRecord で本番健康診断（SHORT通知ゼロ / LONG_E ゼロ / LONG_F悪化なし）
2. rv_chg / 3反転列 / cs_mom_shadow は前向き蓄積継続（gate化しない）
3. 新しい本番ロジックは作らない
4. 同一データへの条件足し探索は過学習なので行わない
```

---

## 【2026-06-23 探索フェーズ終了・監視フェーズ移行】暴走防止メモ

**記録日: 2026-06-23。main.py変更・deploy・mergeは一切不要。方針メモとして記録のみ。**

---

### 1. 探索フェーズ終了（確定）

2026-06-23時点で、既存OHLCV・EntryRecord・OKX 2024〜2026データを使った勝ち筋探索は終了とする。以下は不採用または本番採用不可として扱う。

- **cs_mom**：OKX 2.5年検証で全指標基準割れ、LOMO全30ヶ月マイナス。不採用。
- **rv_chg**：gate化不可。環境認識・record-only用途に限定。
- **S4**：median大幅マイナス、maxDD過大、暴落依存。不採用。
- **SHORT_ML**：OKX全パラメータ未達。不採用。
- **Funding/OI/LSR**：直近無料データでは煙あり。ただしTardis無料サンプル30ヶ月OOSではFunding効果が安定せず、有料データ購入根拠不足。前向きrecord-only観察に限定。

**同一データに対して、条件・閾値・時間帯・RSI・AI確率・feature条件を追加して勝ち筋を探す行為は禁止する。**
理由は、過学習リスクが高く、過去検証で再現性のあるエッジが確認できていないため。

---

### 2. 監視フェーズ開始

今後のEntryRecord分析は、勝ち筋探索ではなく**本番健康診断に限定**する。確認対象は以下のみ：

- SHORT通知が0で維持されているか
- LONG_E停止が維持されているか
- LONG_Fが急悪化していないか
- notify_sent=1の実通知が異常増加していないか
- Discord通知過多が発生していないか
- rv_chg shadow、3反転列、cs_mom_shadow、derivs_logger等のrecord-only配線が壊れていないか
- 本番設定が意図せず変更されていないか

**監視フェーズでは、新しいprofile候補、SHORT再開条件、AI閾値変更、gate化条件を探さない。**

---

### 3. 再検証フェーズに進む条件

以下のどちらかが満たされるまで、再度の探索は行わない：

1. 別ソース・別時期の本当に新しいデータが入手できた場合
2. rv_chg / 3反転列 / cs_mom_shadow / derivs_logger などの前向きrecord-onlyデータが、事前に定めたGO条件を満たす程度に蓄積した場合

前向きデータが蓄積されるまでの期間は「待ち」ではなく、**探索フェーズが完了し、監視フェーズに移行した状態**とする。

---

### 4. 禁止事項（このメモ追記以降）

```
main.py変更
Cloud Run deploy
危険ブランチのmerge
本番ロジック追加
SHORT再開
LONG_E再開
AI閾値変更
同一過去データへの条件足し探索
有料データ購入の即断
```

Funding/OI/LSRロガーは、Macローカルでのrecord-only観察に限定し、本番Botには接続しない。

---

### このメモの目的（次セッションで読む自分へ）

「もう一度EntryRecordを分析して勝ち筋を探そう」「条件を少し足せば勝てるかも」「SHORT再開条件を探そう」というループに戻らないためにこのメモを残す。

EntryRecord分析を始めようとしたとき、必ず問うこと：**「これは健康診断か、勝ち筋探しか」。後者ならやらない。**

---

## 【2026-06-24 Binance公開データ LSR/taker 路線 不採用】BTC全65ヶ月OOSで一貫性が崩壊

**記録日: 2026-06-24。本番・ENV・通知設定・main.py への変更は一切なし。研究用スクリプトのみ（branch push）。**

### 経緯

「探索フェーズ終了・監視フェーズ移行」後、前向きロガーの3ヶ月待ちを本命にせず、**別の無料データ**を探した。data.binance.vision（Binance公式の無料公開データ、2020-09〜）が futures/um の OI / LSR / taker比率 / fundingRate / klines を複数年分提供しており、3ヶ月待たずに歴史OOS検証ができることが判明。形式チェック（probe_binance_public_data.py・7項目全✅）→ 小規模OOS（代表11ヶ月）→ BTC全月（65ヶ月）の順で検証した。

### 検証設計（事前固定・閾値探索なし）

```
データ: data.binance.vision BTCUSDT, 2021-01〜2026-05 全65ヶ月, 47,453行
特徴量5種(as-of・完了足のみ): lsr_all / lsr_top / oi_chg / taker / funding
目的変数: 8h先・24h先の価格リターン
分位: 月内3分位(low/mid/high)固定
判定基準(事前固定): lsr系 %pos≤30%(DOWN方向) / taker24h %pos≥70%(UP方向)
スクリプト: verify_binance_metrics_oos.py（branch claude/crypto-bot-assistant-QlA5G）
  ※ローカル実行（リモートはプロキシでdata.binance.visionがブロック）
```

### 代表11ヶ月 → 全65ヶ月で一貫性が崩壊（確定）

| signal | 代表11ヶ月 %pos | 全65ヶ月 %pos | 全期間effect | 判定 |
|---|---|---|---|---|
| lsr_all 8h | 18%（強い） | **48%**（ランダム） | -0.1895% | ❌崩壊 |
| lsr_all 24h | 27%（強い） | **52%**（ランダム） | -0.4340% | ❌崩壊 |
| lsr_top 8h | 10%（強い） | **40%**（弱い） | -0.0659% | ❌崩壊 |
| lsr_top 24h | 30% | **37%** | -0.2251% | ❌届かず |
| taker 24h | 18%→UP期待で+side少 | **48%**（ランダム） | +0.0459% | ❌崩壊 |
| funding 8h/24h | — | 26%/22% | -0.0528%/-0.1165% | ⚠️n分布不良(high13%のみ)・本命外 |

### 確定見解

```
1. 代表月(1月・7月)で見えたLSR/takerの強い一貫性は「月選択バイアス」だった。
   全65ヶ月にすると %pos がほぼ50%前後に収束＝月によって効いたり効かなかったり。
2. 全期間effectの方向（高LSR→下落）自体は符号として実在するが、
   月別一貫性がない（lsr_all 8hは34/65ヶ月が逆方向）→ 実運用では信頼できない。
3. 事前固定の判定基準（lsr系≤30% / taker24h≥70%）を1つも満たさない。
   → 横展開(ETH/SOL/DOGE/BNB)は実行しない。magnitude確認にも進まない。
4. funding(26%/22%)は閾値を一見通過するが、month内3分位が機能していない
   （high群が全体の13%のみ・n分布 low15813/mid25605/high6001）。本命にしない。
```

### この検証で得たもの・得なかったもの

```
得たもの（プロセスの前進）:
- Binance公開データの取得・結合ルートが確立（OI/LSR/taker/funding/klines, 2021〜）
- 無料で複数年OOSができる道具（verify_binance_metrics_oos.py）
- 「代表月で良く見えても全月で消える」= 月選択バイアスの実例を確認
得なかったもの:
- 採用できるシグナルは見つからなかった（LSR/taker は採用基準未達）
```

### スクリプトの扱い（branchに残す・本番非投入）

```
branch claude/crypto-bot-assistant-QlA5G:
  verify_binance_metrics_oos.py    (504e85b→6f72f3a, 65ヶ月版・実行済み)
  verify_binance_multisym_oos.py   (bb5188a, 横展開・準備のみ・未実行で確定)
  probe_binance_public_data.py     (形式チェック)
→ いずれも研究用。main.pyとは独立。PR化/merge/deployしない（ブランチ防御原則）。
→ 横展開スクリプトはBTC全月崩壊により実行しない（準備のみで凍結）。
```

### やらないこと（確定・変更なし）

```
横展開(多銘柄)の実行 / magnitude確認 / LSR・taker・funding のgate化や本番投入
SHORT再開 / SHORT_AI_RANK再開 / rv_chg gate化 / TP/SL変更 / QS修正 / ENV変更
有料データ購入 / main.py変更 / deploy / merge
同一データへの閾値探索・条件足し（過学習）
```

### この路線の再検証条件

```
別ソース・別時期の本当に新しいデータが出たときのみ、
同じ事前固定ルール（特徴量固定・分位固定・8h/24h固定・%pos閾値固定）で再検証する。
今のBTC 2021-2026データへの条件足し・閾値スイープはしない（月選択バイアスの逆＝過学習）。
```

### 監視フェーズ継続（変更なし）

```
探索フェーズは引き続き終了。LSR/taker路線も探索の一部として完了・不採用。
前向きrecord-only（rv_chg / 3反転列 / cs_mom_shadow）の蓄積を待つ状態は維持。
次の入力: 別ソースの新データ、または前向きデータがGO条件に達したとき。
```

---

## 【2026-06-24 Funding Carry検証メモ】監視対象として棚上げ確定

**記録日: 2026-06-24。本番・ENV・通知設定・main.py への変更は一切なし。**

Binance fundingRate 2020-01〜2026-05で、BTC/ETH/SOL/DOGE/BNBを検証。
BTC/ETHは全期間では構造収益が確認できたが、直近Fundingは大幅に低下。

### 2026-05末時点の現在地スナップショット（verify_funding_carry.py Section 5）

```
BTC/ETHともに 30d/60d/90d/180d/12m の pass_030/pass_060 は全てNO

BTC 12m net_060 = 2.94%
ETH 12m net_060 = 2.56%
BTC 90d net_060 = -0.77%
ETH 90d net_060 = -0.31%
```

### 確定結論

```
Funding Carryは失敗トラックではない。
BTC/ETHについては構造収益として監視対象に残す。
ただし現在地は薄すぎる。今すぐ資金投入しない。
発火ルール最適化もしない。閾値変更もしない。

実運用しない
本番Botに接続しない
main.py変更なし / deployなし / mergeなし
DOGE：参考のみ（全期間では機能するが現在薄い）
SOL/BNB：監視終了
```

### 監視ルール（月1回程度）

```
cd ~/Downloads
curl -L "https://raw.githubusercontent.com/kenji888knymec/ocho/claude/crypto-bot-assistant-QlA5G/verify_funding_carry.py" -o verify_funding_carry.py
python3.14 verify_funding_carry.py
```

BTC/ETHの 90d/180d/12m pass_060 を確認する。
pass_060 が YES になっても即運用GOではなく、取引所/税務/清算/実手数料の確認を開始するラインとする。

### やらないこと（確定）

```
Basis検証 / クロス取引所比較 / 発火ルール検証 / 閾値最適化
SOL/BNBの追加調査
Funding Carry関連のBot組み込み・main.py変更
```

---

## 【2026-06-24 日次トレンドフォロー仮説 棄却】Donchian 20日ブレイクアウト LONG（事前固定・一発検証）

**記録日: 2026-06-24。本番・ENV・通知設定・main.py への変更は一切なし。オフライン検証のみ（branch `claude/crypto-bot-assistant-QlA5G`、`verify_daily_trend.py`）。**

### 経緯・狙い

これまでの検証が「15分足・短期反転・短期モメンタム・SHORT寄り・AI判定・細かい条件」に偏っていた反省から、**まだ試していない別系統＝「時間軸を上げた日足の中期トレンドフォロー LONG」を、事前に条件・合格基準を全固定して一発だけ検証**した（条件を後から都合よく変える泥沼探索をしないため）。

### 検証設計（事前固定・変更禁止）

```
仮説    : 暗号資産は15分足ではノイズ過大。日足の中期トレンドは持続するか
入口    : 過去20日高値(Donchian 20)を終値で上抜けた翌日の始値でLONG
対象    : BTC / ETH のみ判定。SOL / DOGE は参考（合否に使わない）
保有    : 10日 または SL 5% のどちらか早い方
コスト  : 往復0.15%
件数基準: test trades >= 10（件数不足は勝率が良くても保留）
train   : 2024-01-01〜2025-03-31 / test : 2025-04-01〜2026-06-21
合格基準: test avg>0 / median>-3% / active_months≥5 / maxDD>-35% / top3集中<60%
         BTC・ETH両方合格（片方だけはNG）/ train合格・test符号逆転は不合格
```

**約定ルールの現実化（検証前に修正・閾値変更ではない）：**
```
- エントリー当日も intraday SL の判定対象（前日終値ブレイク→当日始値エントリーのため、
  当日中に Low<=entry*0.95 ならSL到達）。当初コードは翌日からしか見ておらず甘かった→修正済み。
- ギャップダウン（翌日以降に始値<=SL）は当日Openでexit（保守的）。
- SLと10日保有終了が同日ならSL優先。
```

### 結果（test期間・実績PnL_Net）

| 銘柄 | test n | WR | avg | median | maxDD | top3集中 | 判定 |
|---|---|---|---|---|---|---|---|
| **BTC** | 15 | 40.0% | +0.478% | -0.975% | -23.3% | **354.3%** | ❌ top3集中 |
| **ETH** | 16 | 37.5% | +1.765% | **-5.150%** | -24.6% | **172.8%** | ❌ median & top3集中 |

参考（判定外）: SOL test avg-0.358%（マイナス）/ DOGE test WR41.7% だが median-5.150%。

### 確定見解

```
1. 件数基準は両方クリア（BTC15・ETH16件）。PASS_MIN_N=10 に救われた合格ではなく、
   中身で純粋に落ちた（件数水増しの誤判定ではない）。

2. avgはプラス（BTC+0.478% / ETH+1.765%）だが median がマイナス。
   特にETHは median=-5.150%（=SL5%+コスト0.15%）に張りつく＝トレード過半数がSL直撃。
   勝ちは少数の大勝ち（例: ETH 2025-07 に+11.5%級が集中し月+34.5%）。WR37〜40%で
   損益分岐50%に未達。

3. top3集中が大きい（BTC354% / ETH172%）＝利益が数ヶ月に極端集中。
   安定した月次エッジではなく、少数の大ブレイク月に依存している。

4. → S4（Donchian 55h SHORT・暴落捕獲）と「同様に」、低勝率・少数イベント依存の
   ペイオフ構造が見られる。安定した手動運用シグナルとは判断しない。
   （"S4と完全に同じ"とは断定しない。方向・時間軸・銘柄は別だが構造が似ている）

5. 手動運用基準（WR≥60% / avg≥+0.20% / median>0）には median・top3 で明確に未達。
   avgプラスだけでは「掛けていい」水準にならない。
```

### 結論

```
日次トレンドフォロー仮説（Donchian 20日ブレイクアウト LONG）は棄却。
合格していれば前向き観察候補になる予定だったが、棄却なので観察候補にも入れない。
この仮説の検証はここで終了。
```

### やらないこと（確定・厳守）

```
20日→18/22日への変更 / SL幅(5%)変更 / 保有日数(10日)変更
SOL/DOGEで合格を探す / 「少し変えると合格」の再探索
verify_funding_extreme.py の実行（Funding極値逆張りは保留のまま）
本番Bot接続 / main.py変更 / deploy / merge / 通知ロジック変更
```

### スクリプトの扱い

```
branch claude/crypto-bot-assistant-QlA5G:
  verify_daily_trend.py     (研究用・実行済み・棄却)
  verify_funding_extreme.py (存在のみ・今回実行しない・保留)
→ いずれも research only。PR化/merge/deployしない（ブランチ防御原則）。
```

---

## 【2026-06-24 路線転換・確定】公開データ方向当てBot探索をクローズ → 判断補助・監視へ目的変更

**記録日: 2026-06-24。本番・ENV・通知設定・main.py への変更は一切なし。これは「新しい開発の開始」ではなく、探索フレームを閉じて目的を再定義する方針記録のみ。実装・ダッシュボード作成にはまだ進まない。**

### この記録の目的（次セッションの自分へ）

数週間後に「RSIを変えたら / SLを変えたら / 4時間足なら / Funding極値なら / CTA式なら」と条件探索ループに戻るのを防ぐためのガードレール。EntryRecordや過去OHLCVを開こうとしたとき、必ず問う：**「これは健康診断・監視か、勝ち筋探しか。後者ならやらない。」**

### なぜ路線転換するのか（言い切らない整理）

```
「撤退」= 仮想通貨やBot全体を諦めるのではない。
正確には「公開データで方向を当てる自動Botを作る路線」を一旦クローズする、という意味。

理由:
- 公開OHLCV・公開指標（funding/LSR/taker）の方向当ては、多系統・2.5年で全滅。
  これは腕の問題ではなく、誰でも触れる公開データは取り尽くされてエッジが残りにくい
  （market efficiency）という構造。RSI・期間・SL・銘柄・時間足をいじって
  「勝者のロジック」に届く可能性は低い（世界中が既に試している）。
- 勝っている人は、単純なテクニカル方向当てではなく、速度・インフラ・別データ
  （板/フロー/清算/オンチェーン）・低手数料/リベート・裁定・資金管理・構造収益
  など「別の武器・別のゲーム」で勝っている可能性が高い。
  「勝者が存在する」ことは「公開OHLCVと簡単なシグナルで勝てる」証明ではない。
```

### これまでに残った/棄却したものの正確な区別（表現を強くしない）

```
Funding Carry      : 構造収益が確認され、監視対象として残った（BTC/ETH）。
                     ただし現在地は薄い（pass_060 全NO）。今は張らない。
日次トレンドフォロー : avg はプラスの兆候があったが、低勝率・中央値マイナス・
                     top3集中過多により安定性基準を満たさず棄却（2026-06-24）。
                     ※「本物のエッジ」とは書かない。avgプラスの兆候止まり。
SHORT系/cs_mom/S4/LSR・taker : いずれも棄却済み（再探索しない）。
```

### 記録方針（4項目・確定）

**1. 公開OHLCV・公開指標で方向を当てる自動Bot探索は一旦クローズ**
```
- RSI・期間・SL・保有日数・銘柄・時間足を変えて勝ち条件を探す探索はしない
- Donchian日次トレンド / SHORT系 / cs_mom / S4 / LSR・taker 等の再探索はしない
- Funding極値逆張り（verify_funding_extreme.py）も現時点では実行しない
```

**2. 低コストで残すもの（新規開発なし）**
```
- Funding Carry : 月1監視のみ。pass_060 が YES になって初めて執行条件の検討に進む
- 古いMacBook derivs_logger : 3ヶ月だけ期限付きで継続。勝てる前提ではなく監査用
                              （負担が大きければ止めてよいレベル）
- 本番Bot : 健康診断のみ。SHORT停止 / LONG_E停止維持 / LONG_F急悪化チェック
```

**3. ゴールを「勝てる自動売買Bot」から「判断補助Bot・監視ダッシュボード」に置き換える**
```
- 既存の shadow基盤・rv_chg・3反転列・cs_mom_shadow・derivs_logger・Funding監視・
  LONG_F健康状態を「見える化」する方向
- BUY/SELLの売買指示ではなく、危険日・過熱・構造収益の現在地・既存Bot健康状態を
  表示する補助ツールとする
- これは新しい勝ちシグナル探索ではなく、既存材料の整理・監視用途とする
- ※今回は方針記録のみ。実装・通知連携・Discord連携・ダッシュボード作成には進まない
```

**4. 保留**
```
- CTA式・常時ポジ型 / 多市場ボラ調整トレンドのポートフォリオ検証 / 弱シグナル×資金管理
  は今は実行しない
- やる場合は別途、事前固定・1本のみ・完全オフラインで、明示的に依頼されたときだけ
```

### やらないこと（確定・厳守）

```
本番Bot接続 / main.py変更 / deploy / merge / 通知ロジック変更
SHORT再開 / LONG_E再開 / AI閾値変更
公開データ方向当ての条件探索（RSI/期間/SL/銘柄/時間足の振り直し）
判断補助化の実装・ダッシュボード作成（今回は方針記録のみ・実装には進まない）
```

---

## 【2026-06-25 健康診断レポート運用 開始】手動レポートが本当に役立つか確認する段階

**記録日: 2026-06-25。本番Bot動作・main.py・deploy・mergeへの変更は一切なし。運用フレームの確定記録のみ。**

### 確定した運用フレーム

```
本番Botの動作: 凍結維持
  SHORT停止 / LONG_E停止 / 通知ゲートOFF / 再学習停止 / deployなし

プロジェクトの目的: 変更
  旧: 公開データで方向を当てる「勝てる自動売買Bot」を探す
  新: 判断補助・監視（勝ち筋探しではなくBot健康状態の確認）
```

### 今後のEntryRecord診断の見方（厳守）

```
やらないこと:
  - 勝ち筋を探さない
  - 条件変更を探さない
  - 買い・売りを出さない

見るのはこれだけ:
  - Botが正常に記録しているか（shadow列が溜まっているか）
  - notify_sent=1相当（Lane_Notify=True & Notify_Pass=1）の実績が悪化していないか
  - LONG_Fが急悪化していないか
  - SHORT停止・LONG_E停止が維持されているか
  - rv_chg / 3反転列 / cs_mom_shadow の記録が継続しているか
  - 今日触るべきか、触らないべきか
```

### 出す結論はこの4つだけ

```
通常観察  : 異常なし。何もしない
慎重      : 気になる変化あり。次回確認時に再チェック
触らない  : 悪化あり。それでも変更はしない。観察継続
記録異常  : shadow列が止まっている・配線が壊れているなど技術的問題
```

### 現在の状態（2026-06-25 時点）

```
スケジューラー:
  有効: crypto-alert-judge-v2-every-15m / crypto-alert-run / v2-report-daily / ops-health-daily
  停止: train-*-once 4本 / reload-model / train-every-hour / label-market

shadow記録（6/20以降）:
  BTC_RV_Chg: 92.0% 記録中
  3反転列（BTC_RSI_15m等）: 91.2% 記録中
  cs_mom_shadow: 別シート・継続中

LONG_F（最新 6/23-24）: 2日連続悪化（WR16-18%）。Lane_Notify=False（実Discord送信なし）。停止基準非該当。観察継続。

結論: 「触らない」（LONG_F悪化あり。ただし実送信なし・停止基準非該当のため変更なし）
```

### 次に実装・開発に進む条件

```
この手動レポートが実際に判断の助けになると確認できたとき、
その後にオフライン集計スクリプトや簡易ビューを検討する。
今は実装しない。まず手動レポート運用で確認する。
```

### 絶対にやらないこと（追加・厳守）

```
EntryRecordを見ながら勝ち筋を探す行為
「この条件を変えたら良くなりそう」という提案
「まだデータが足りない」を理由に待ちを無限に続けること（→ 期限を明示する）
Dashboard実装・Discord連携・v2_report拡張・main.py変更・deploy・merge
```

---

## 【2026-06-27 検証A（VRP）結果】BTC VRPは構造収益として監視トラック候補・ETHは不採用・総合不合格

**記録日: 2026-06-27。本番・ENV・通知設定・main.py への変更は一切なし。研究用スクリプトのみ（branch `claude/crypto-bot-assistant-QlA5G`）。**

**経緯：** 公開OHLCV方向当ての全滅（C/C'/C''含む）を受け、別ゲーム＝「ボラの保険料（VRP=ボラティリティリスクプレミアム）」を検証。Deribit DVOL（IV指数・BTC版VIX）を賢治さんのMacで取得（`fetch_deribit_dvol.py`、BTC/ETH各1921日 2021-03〜2026-06）。重複期間は比較対象OHLCVが2024-01〜のため **2024-01-30〜2026-06-20（873日）**。

### 仮説・事前固定基準（後出し変更なし）

```
仮説: BTC/ETHのIV(DVOL)は、その後30日の実現ボラ(RV_fwd)より構造的に高い。
      = VRP = DVOL - RV_forward > 0 が平均的に成立。
合格基準（5つ・BTC/ETH両方が満たすこと）:
  ① 全期間 VRP平均>0
  ② VRP>0が60%以上
  ③ train/test 両方で平均>0（符号一貫）
  ④ BTC・ETH 両方で①成立
  ⑤ 簡易ボラ売りPnL（コスト年率2%控除後）がプラス
スクリプト: verify_vrp.py / verify_vrp_tail.py
注意: RV_forwardは事後値=構造存在の確認用。実運用signalではない。
```

### 結果（verify_vrp.py）

| 基準 | BTC | ETH |
|---|---|---|
| ① 全期間VRP平均>0 | ✅ +4.81%pt | ✅ +0.26%pt（辛うじて） |
| ② VRP>0が60%以上 | ✅ 77.0% | ❌ 58.6% |
| ③ train/test両方>0 | ✅ +6.35 / +3.34 | ❌ +1.48 / **-0.91**（test負） |
| ⑤ ボラ売りPnL>0 | ✅ +2.81%/年 | ❌ -1.74%/年 |
| 月次プラス | 24/30ヶ月 | 19/30ヶ月 |

**総合判定: ❌ 不合格（BTC/ETH両方が5基準＝事前固定条件）。この判定は固定。ETHを外してBTC単独で合格化はしない。**

### 確定見解

```
1. BTC VRPは本物。5ヶ月の研究で初めて、事前固定の頑健性（train/test符号一貫・
   77%恒常割高・24/30ヶ月プラス）を単独で全通過した構造。株のVIXプレミアム
   （約3-4 vol pt）と同水準の約4.8 vol pt がBTCに存在する。
2. ETHは薄すぎ・不安定で落ちた（test期間マイナス転落）。プレミアムが最も流動性の
   高いBTCオプション市場に集中し、ETHでは消える＝構造として整合的。不採用。
3. ただし「構造が存在する」≠「勝てる」。下記テール参照。
```

### テール可視化（verify_vrp_tail.py・1回限り・採否判定ではなくリスク確認）

**閾値探索・期間変更・ETH除外合格化・銘柄追加・実装は一切せず、BTC VRP最大の弱点のみ可視化。**

```
VRP分布: avg+4.8 / median+5.7 / min-31.1 / max+27.8（vol pt）= 明確な非対称（左に長い裾）
負け日頻度: VRP<0は23%だけ・VRP<-10は8.2%だけ = たまにしか負けないが負けると巨大
最悪日ワースト15: 全て2026-02〜03に集中（単一エピソード）。IV≈52%で売った後
                  実現ボラが80〜83%に爆発（30 vol pt の読み違い）
最悪月: 2026-02 = -22.1 vol pt（次点2025-03 -12.6を大きく引き離す）
簡易PnL最大DD: -2.30%（※線形近似の楽観下限・最長水中161日）

★最重要発見（⑤ DVOL変化5分割）:
  危険は「IVが高い時に売ること」ではない。DVOL急騰局面に売った翌VRPはむしろ最良(+6.0)。
  実際に踏まれたのはIVが平穏(≈52%)な時に売った後、相場が静→乱へレジーム転換した
  2026年2月。→ IVの水準からは危険を事前に見分けられない。これがショートボラ本質の怖さ。

正直な注記: ③のPnLは VRP/365 の線形近似で、vega損・gamma損・証拠金増加・強制
            ロスカットを未計上。2026年2月（IV52→RV82）は実際には口座を脅かす規模の
            損失になり得る。最大DD-2.30%は『下限の楽観値』。
```

### Aの最終結論（CLAUDE.md確定記録）

```
総合判定: 不合格（事前固定・BTC/ETH両方の条件）
BTC VRP: 構造収益として「監視トラック」に残す候補
ETH VRP: 不採用（薄い・test負・PnL負）
実運用 / Bot接続 / 通知化 / gate化 / 実装: しない
位置づけ: Funding Carryと同枠＝「構造は実在するが別ゲーム・要資本・テールリスク有・薄利」
          Discord通知シグナルにはならない。+2.81%/年はFunding Carryの「旨味ほぼなし」と同水準。
```

### Aの再検証・本採用に進む条件（満たすまでgate化・実装しない）

```
1. 本物の暴落レジーム（2022 LUNA/FTX級）を含む期間でVRPプレミアムが残るか
   （現データはtest側に2026-02の単一ボラスパイクのみ。本格暴落が重複期間に無い）
2. 線形近似でなく実オプション評価（vega/gamma/テールヘッジ）でネットプラスが残るか
3. テールヘッジ後でも年率リターンが資本コスト・要証拠金に見合うか
```

### やらないこと（確定・変更なし）

```
ETHを外してBTC単独で合格扱いにする / 期間変更で合格化 / 閾値・条件探索
VRPの本番gate化・Bot接続・Discord通知化・実装
SHORT再開 / SHORT_AI_RANK再開 / LONG_F停止 / rv_chg gate化 / TP/SL変更 / QS修正 / ENV変更
main.py変更 / deploy / merge
```

### スクリプト・データの扱い（branchに残す・本番非投入）

```
branch claude/crypto-bot-assistant-QlA5G:
  fetch_deribit_dvol.py / verify_vrp.py / verify_vrp_tail.py / dvol_BTC.csv / dvol_ETH.csv
→ 研究用。main.pyとは独立。PR化/merge/deployしない（ブランチ防御原則）。
```

### 次（B：トークンアンロック）に進む前の確認事項

```
Bはアンロック日・供給イベントという別データ。公開OHLCV方向当てではない。
ただしBもいきなり売買・Bot接続・通知化はしない。まず以下の「見るだけ」から:
  - 過去のアンロック日と価格反応の確認
  - イベント前後の平均リターン
  - BTC地合いを除いた影響
  - 回数不足かどうか
データ取得方法（アンロックスケジュール源）と取得環境（リモートはプロキシ制約・
Mac-local fetchが要る可能性）を賢治さんと決めてから着手する。
```

---

## 【2026-06-27 検証B（トークンアンロック）結果】一般エッジ不成立・APT単独依存・総合不合格

**記録日: 2026-06-27。本番・ENV・通知設定・main.py への変更は一切なし。研究用スクリプトのみ（branch `claude/crypto-bot-assistant-QlA5G`）。**

**狙い：** 公開OHLCV方向当ての全滅を受け、別データ＝供給イベント（トークンアンロック）への価格反応を検証。「アンロック前後でBTC地合いを差し引いても負の超過リターンが出るか」。

### データ源の試行履歴（無料で取れず転戦）

```
① DeFiLlama emissions（無料アンロックAPI）→ HTTP 402（有料化）。Pro購入せず。
② CoinGecko キーレス → 401/429。Demoキー作成も有料Basic $29/mo($348/yr)契約画面に
   誘導され、無料・カードなしで取れず → 停止（購入しない・CLAUDE.md遵守）。
③ Coinpaprika 無料API（キー不要・カード不要）→ 採用。/v1/coins でID自動解決、
   /v1/tickers/{id}/historical で price+market_cap を取得（無料枠=約1年）。
```

### 事前登録（verify_unlock_PREREG.md・データを見る前に固定）

```
イベント = 単日で循環供給(=mcap/price)が ≥1.0% 増え持続（偽ジャンプ除外）、±5日統合。
超過リターン = market-model AR = r_token − β·r_BTC（β窓[-90,-11]・α=0固定）。
CAR_pre[-10,-2] / CAR_event[-1,+1] / CAR_post[+2,+10]。
合格5基準（全て）: ①n≥30 ②pre/event平均が負 ③その窓で|t|≥2かつ中央値負
                  ④train/test両方で負 ⑤leave-one-out(1件)で符号維持。
データ: Coinpaprika 28銘柄×365日（2025-06-28〜2026-06-27, 10,220行）+ 既存OHLCV。
分析コード verify_unlock_event_study.py は結果を見る前にロック・合成データでスモークテスト済み。
```

### 結果（verify_unlock_event_study.py・実データ）

```
有効イベント33件（APT13/ARB10/SUI7/STX2/DOT1/SEI1）。≥5%大型は4件。
  pre[-10,-2] : 平均-2.55% 中央-3.28% 負67% t=-1.75
  event[-1,+1]: 平均+0.68% 中央-0.56% 負55% t=+0.83
  post[+2,+10]: 平均-1.43% 中央-0.92% 負58% t=-1.12

合格判定: ①✅(n=33) ②✅(pre負) ③❌(t=-1.75<2) ④✅(train-1.05/test-4.15) ⑤✅(LOO-2.10)
→ 4/5通過。総合 ❌不合格（③＝統計的有意性に届かず）。判定は固定。
```

### 決定的診断：pre下落ドリフトはAPT単独依存（leave-one-token-out）

```
銘柄別 pre[-10,-2]:
  APT n=12 平均-5.97% 中央-8.11% 負75%  ← これだけが強い
  ARB n=10 平均-0.36% 中央+0.21% 負50%  ← ほぼゼロ
  SUI n= 7 平均-2.07% 中央-1.63% 負86%
  STX n= 2 平均-3.44%（n小）/ DOT・SEI 各1件は正

leave-one-TOKEN-out:
  APT除外 → 全体pre平均が -2.55% → -0.60% に崩壊（効果消失）
  → 全体の「アンロック前下落」は ほぼAPT1銘柄が作っていた。一般化しない。
```

### 確定見解

```
1. 「供給イベント一般のエッジ」は不成立。pre窓の負ドリフトはAPT単独依存で、
   2番目に多いARBはほぼゼロ。leave-one-token-outで効果が消える。
2. ③（|t|≥2）未達に加え、単一銘柄依存という研究全体で繰り返した失敗パターンと同型。
3. 供給データの報告遅れでイベント日が±数日ずれ得る（pre/eventの分離が曖昧）という
   構造的弱点もある（無料データ由来）。
4. APT単独の月次アンロック前ドリフトは、薄く・単名で・既に裁定されている可能性が高く、
   当初狙った優位性ではない。B5（実運用設計）には進まない。
```

### やらないこと（確定・変更なし）

```
B5（実運用設計）への前進 / アンロックgate化・Bot接続・通知化・実装
有料データ購入（DeFiLlama Pro / CoinGecko Basic等）/ APT単名への条件足し探索
SHORT再開 / SHORT_AI_RANK再開 / LONG_F停止 / rv_chg gate化 / TP/SL変更 / QS修正 / ENV変更
main.py変更 / deploy / merge
```

### Bの再検証に進む条件（満たすまで保留）

```
1. 複数年・多数イベント（n≫33）の供給/アンロック履歴が無料 or 既存コストで得られる
   （現状は無料枠1年が上限。これ以上は有料 → 購入しない）
2. APT以外の複数銘柄でも pre窓の負ドリフトが独立に出る（単名依存でない）
3. 報告遅れの無い正確なアンロック日（cliff確定日）で pre/event/post を分離できる
→ いずれも現状の無料データでは満たせない。Bは「データ取得コストあり・単名依存の弱い候補」として保留。
```

### スクリプト・データの扱い（branchに残す・本番非投入）

```
branch claude/crypto-bot-assistant-QlA5G:
  verify_unlock_PREREG.md / probe_unlock_data.py(DeFiLlama用・402で無効) /
  fetch_supply_coingecko.py(CoinGecko用・有料化で不使用) /
  fetch_supply_coinpaprika.py(採用) / verify_unlock_event_study.py /
  supply_coingecko.csv(Coinpaprika出力・28銘柄×365日)
→ 研究用。main.pyとは独立。PR化/merge/deployしない（ブランチ防御原則）。
```

### 研究トラックの棚卸し（2026-06-27時点）

```
A（VRP/ボラ売り）: BTC構造あり＝監視トラック候補（実運用不可・テールリスク・薄利）。ETH不採用。
B（トークンアンロック）: 一般エッジ不成立・APT単独依存。保留（無料データの限界・購入しない）。
C系（公開OHLCV方向当て）: 全滅・閉鎖。
→ 別データでも「単名・単一エピソード依存」「データが有料の壁」で実運用エッジに届かず。
   本番は引き続き全凍結が正しい。
```

---

## 【2026-06-27 探索フェーズ一区切り・監視フェーズ再確定】A/B/C完了 → 新探索しない

**記録日: 2026-06-27。本番・ENV・通知設定・main.py への変更は一切なし。方針メモとして記録のみ。**

### この記録の目的（次セッションの自分へ）

A（VRP）・B（アンロック）・C系（公開OHLCV方向当て）を一通り検証し終えた。
ここで「別データを探せばまだ何か出るかも（D系）」という探索ループに戻らないための区切り。
次にEntryRecordや新データを開こうとしたとき必ず問う：**「これは健康診断・月1監視か、新しい勝ち筋探しか。後者ならやらない。」**

### 探索の棚卸し（確定）

```
C系（公開OHLCV方向当て）: 全滅・閉鎖（RSI/時間足/SL/順張り/逆張り/モメンタム/rv_chg/暴落検知/S4/cs_mom/LSR・taker）
A（VRP/ボラ売り）       : 事前固定では総合不合格。BTCのみ構造あり＝監視候補（実運用不可・テールリスク・薄利）。ETH不採用
B（トークンアンロック） : 事前固定で総合不合格。pre下落ドリフトはAPT単独依存で一般化せず。保留（無料データ1年の限界・購入しない）
Funding Carry          : 構造はあるが現在薄い（pass_060全NO）。月1監視継続
→ 共通の壁: 別データでも「単名・単一エピソード依存」「有料データの壁」「小資金では薄い・テールリスク」で実運用エッジに届かない
```

### 今後残すもの（新規開発なし・低コスト監視のみ）

```
1. Funding Carry  : 月1監視（verify_funding_carry.py）。pass_060がYESで初めて執行条件検討
2. BTC VRP        : 月1監視（Macでdvol再取得→verify_vrp.py）。Funding Carryと同枠＝監視候補。
                    実運用しない/Bot接続しない/通知化しない/gate化しない。現在地だけ見る
3. record-only観察: rv_chg / 3反転列 / cs_mom_shadow（前向き蓄積・GO条件まではgate化しない）
4. 本番健康診断   : EntryRecordで SHORT停止/LONG_E停止/LONG_F急悪化/notify_sent異常増/
                    Discord過多/shadow配線/本番設定の意図せぬ変更 を確認するだけ
```

### やらないこと（確定・厳守）

```
D系の新探索（別データを探せば何か出るかも、のループに戻らない）
有料データ購入（DeFiLlama Pro / CoinGecko Basic / その他）
A/Bの条件足し・閾値スイープ・単名（APT等）への後付け救済
B5（アンロック実運用設計）への前進
SHORT再開 / SHORT_AI_RANK再開 / LONG_E再開 / LONG_F停止
rv_chg gate化 / cs_mom gate化 / VRP gate化 / TP/SL変更 / QS修正 / AI閾値変更
ENV変更 / main.py変更 / Bot接続 / 通知化 / deploy / merge
```

### 再探索フェーズに進む条件（どちらか満たすまで新探索しない）

```
1. 別ソース・別時期の本当に新しいデータが「無料 or 既存コスト」で入手できた場合
   （有料の壁に当たったら進まない・購入しない）
2. record-only（rv_chg / 3反転列 / cs_mom_shadow）または月1監視（Funding Carry / BTC VRP）が
   事前に定めたGO条件を満たす程度に蓄積した場合
→ それまでは「待ち」ではなく、探索完了・監視フェーズに移行した状態とする
```

### BTC VRP 月1監視の手順（参考・実運用ではない）

```
Mac（リモートはDeribit 403）:
  python3 fetch_deribit_dvol.py        # dvol_BTC.csv / dvol_ETH.csv 更新
  → 2ファイルをチャットに貼る
リモート:
  python3 verify_vrp.py                # ①〜⑤の現在地を確認（合格化が目的ではない）
確認するのは「BTCのVRPプレミアムが残っているか／テールが悪化していないか」だけ。
GO条件（本物の暴落レジームを含む期間で残る・実オプション評価でネット＋）を満たすまで実運用しない。
```

---

## 【2026-06-27 ランキング残り候補の消化】⑦レバレッジ・ペアトレード = 実データで不採用確定

**記録日: 2026-06-27。本番・ENV・通知・main.py 変更なし。研究用スクリプトのみ。**
**経緯：** 「7候補を順に全部見る」約束に従い、A/B/C(=①②④)完了後の残り（③ベーシス/⑤DeFi貸出/⑥DEX LP/⑦レバ・ペア）を消化中。⑦は新データ不要で既存OHLCVで完結するため先に実証（verify_leverage_pair.py）。

### ⑦結果（BTC/ETH 市場ニュートラル・ペア、既存OHLCV）

```
ベース無レバ: 全期間合計-68.4% / maxDD-76.4%（=#4現物持ち替えと同系統で既に負け）
レバ別（全期間 / 強制ロスカット）:
  1x -68.4%  なし
  2x -94.8%  ★退場 2025-04-21
  3x -99.4%  ★退場 2024-10-29
  5x -100.0% ★退場 2024-08-07
TRAIN/TESTでも2x以上は全て退場。
→ レバレッジは期待値の乗数。ベース≤0なら倍率↑で必ず悪化＋DDが1/Lを超えた時点で破産。
  ⑦は不採用確定（順位表②退場リスク=1点を実データが裏付け）。
```

### 7候補スコアボード（2026-06-27時点）

```
①ボラ売り(VRP)        : ✅完了 — BTC構造あり=監視候補 / ETH不採用 / 総合不合格
②アンロック           : ✅完了 — APT単独依存 / 不合格
③キャリー(Funding/ベーシス): △ Funding=検証済み月1監視 / ベーシス=未検証(同系統見込み・要先物データ)
④現物持ち替え         : ✅完了 — C/C'/C'' 全滅
⑤DeFi貸出             : ⏳未検証(無料DeFiLlama yieldsで確認可・要Macフェッチ)
⑥DEX LP               : ⏳未検証(同上・IL込み採算)
⑦レバレッジ・ペア     : ✅完了 — 不採用確定(上記)
```

---

## 【2026-06-27 ⑤DeFi貸出・⑥DEX LP 確認】⑤=預金(別枠)・⑥=IL/報酬の幻で不採用

**記録日: 2026-06-27。本番・ENV・通知・main.py 変更なし。DeFiLlama yields(無料・キー不要)の現在地スナップショット確認。**

### ⑤ DeFi貸出（ステーブル lending・71件中 大TVL代表）

```
Sky SUSDS 3.60%(TVL$5844M) / Aave USDC 3.16%($213M) / Aave USDE 4.84%($192M)
Fluid USDC 6.72%($131M) / Morpho USDC 4.32%($432M)
→ 安全大型で 3〜5%、やや小型で6%台。30d平均≒現在値＝安定・持続的。
判定: 本物の利回りだが本質は「預金」（年3〜5%・スキル不要・資金力比例・SC/デペッグリスク）。
      「勝てるシグナル」ではない。遊休資金の利回りフロア=別枠。順位表「※ただの預金」を裏付け。
```

### ⑥ DEX LP（multi-asset・TVL≥$50M）

```
高APY上位は全てILリスク有りのボラペア:
  Uniswap-v3 WETH-USDC 43.6%(base43.6/ilRisk=yes) / WETH-USDT 39.3%(同)
安定ペアは base ほぼゼロ:
  Curve USDC-RLUSD 5.57%(base0.09=報酬トークン依存) / DAI-USDC-USDT 0.00% / ETH-STETH 1.35%
判定: 高APY=IL(持たされ損)＋報酬トークン依存の幻。手数料APYは高いがトレンド相場(2024-26)では
      ILが手数料を侵食。本質はショートガンマでVRPと同族だが綺麗なプレミアム構造がなく
      レンジ相場でしか勝てない。明確なエッジにならない → 不採用。
```

### 7候補スコアボード（2026-06-27・更新）

```
①ボラ売り(VRP)   : ✅完了 — BTC構造あり=監視候補 / 総合不合格
②アンロック       : ✅完了 — APT単独依存 / 不合格
③キャリー         : △ Funding=検証済み月1監視 / ベーシス=未検証(同系統見込み・要先物データ)
④現物持ち替え     : ✅完了 — 全滅
⑤DeFi貸出         : ✅完了 — 本物の利回りだが「預金」(年3〜5%)。エッジでなく別枠
⑥DEX LP           : ✅完了 — 高APYはIL/報酬の幻・トレンドでIL負け。不採用
⑦レバレッジ・ペア : ✅完了 — 不採用(無レバで負け・レバで退場)
→ 残るは ③ベーシスのみ。Fundingでカバー済み(構造あり・薄利・要資金)。
```

### やらないこと（変更なし）
```
⑤⑥のBot接続・実装・通知化 / 有料データ購入 / main.py変更 / deploy / merge
SHORT再開 / LONG_E再開 / rv_chg・cs_mom・VRP gate化 / ENV変更
```

---

## 【2026-06-27 ③ベーシス=Fundingで代表して閉じる → 7候補探索 完了】最終スコアボードと監視項目

**記録日: 2026-06-27。本番・ENV・通知・main.py 変更なし。賢治さん合意で③ベーシスは追加フェッチせず閉じる。**

### ③の締め方（合意）

```
③キャリーは Funding（現物ロング+無期限先物ショートで資金調達料を収穫）で検証済み・月1監視中。
ベーシス（現物vs四半期先物）は同じキャリー構造（現物ロング+先物ショートで金利差/プレミアム収穫）。
→ 四半期先物データを追加取得しても結論は同系統（構造あり・薄利・要資金・月1監視）になる見込み。
  手間の割に実運用判断が変わらないため、③はFundingで代表して閉じる（ベーシス追加検証しない）。
```

### 7候補 最終スコアボード（全完了）

```
①ボラ売り(VRP)    : BTCに構造あり → 月1監視候補（実運用不可・テールリスク・薄利）。ETH不採用。総合不合格
②アンロック        : APT単独依存 → 不合格・保留（無料データ1年の限界・購入しない）
③キャリー          : 構造あり・現在薄い(pass_060全NO)・要資金 → 月1監視（Fundingで代表・ベーシスは閉じる）
④現物持ち替え      : C/C'/C'' 全滅 → 閉鎖
⑤DeFi貸出          : ステーブル貸出 年3〜5%（本物だが「預金」）→ エッジでなく別枠（遊休資金の利回りフロア）
⑥DEX LP            : 高APYはIL/報酬トークンの幻・トレンドでIL負け → 不採用
⑦レバレッジ・ペア  : 無レバで負け・2x以上で強制ロスカット退場 → 不採用
```

### 全体結論（言い切らない整理）

```
公開・無料データの範囲では「掛けていい」手動売買シグナルは見つからなかった（A〜⑦全て）。
構造収益として残るのは ①BTC VRP と ③キャリー の2つだけ。ただし両方とも:
  - 別ゲーム（オプション売り / 現物+先物キャリー）
  - 要資本・小資金では薄利
  - ①はテールリスク大（暴落で一発）
  → Discord通知シグナルにはならない。実運用しない。月1で現在地を見る監視候補。
本番Botは引き続き全凍結が正しい。
```

### 今後残す監視・観察（新規開発なし）

```
1. BTC VRP        : 月1監視（Macでdvol再取得→verify_vrp.py）。実運用/Bot/通知/gate化しない
2. キャリー(Funding): 月1監視（verify_funding_carry.py）。pass_060がYESで初めて執行条件検討
3. record-only観察 : rv_chg / 3反転列 / cs_mom_shadow（前向き蓄積・GO条件までgate化しない）
4. 本番健康診断    : EntryRecordで SHORT停止/LONG_E停止/LONG_F急悪化/notify_sent異常増/
                     Discord過多/shadow配線/本番設定の意図せぬ変更 を確認するだけ
⑤DeFi貸出は「遊休資金の置き場」として頭の片隅に（Bot/シグナルではない・別枠）
```

### やらないこと（確定・厳守）

```
③ベーシスの追加フェッチ / D系の新探索 / 有料データ購入
①〜⑦の条件足し・閾値スイープ・単名(APT等)救済 / B5実運用設計への前進
①VRP・③キャリーの実運用化・Bot接続・通知化・gate化
SHORT再開 / SHORT_AI_RANK再開 / LONG_E再開 / LONG_F停止
rv_chg gate化 / cs_mom gate化 / TP/SL変更 / QS修正 / AI閾値変更
ENV変更 / main.py変更 / deploy / merge
```

### 再探索フェーズに進む条件（どちらか満たすまで新探索しない）

```
1. 別ソース・別時期の本当に新しいデータが「無料 or 既存コスト」で入手できた場合（有料の壁なら進まない）
2. 月1監視（BTC VRP / Funding）または record-only（rv_chg/3反転列/cs_mom_shadow）が
   事前に定めたGO条件を満たす程度に蓄積した場合
→ それまでは探索完了・監視フェーズに移行した状態とする。EntryRecordを開くときは必ず
  「健康診断・月1監視か、勝ち筋探しか。後者ならやらない」を自問する。
```

### branch上の研究スクリプト（本番非投入・ブランチ防御）

```
A:  fetch_deribit_dvol.py / verify_vrp.py / verify_vrp_tail.py / dvol_*.csv
B:  verify_unlock_PREREG.md / probe_unlock_data.py / fetch_supply_coingecko.py /
    fetch_supply_coinpaprika.py / verify_unlock_event_study.py / supply_coingecko.csv
⑤⑥: fetch_defillama_yields.py
⑦:  verify_leverage_pair.py
C系: verify_ratio_rotation.py / verify_ratio_momentum.py / verify_xsec_momentum.py
→ いずれも研究用。main.pyと独立。PR化/merge/deployしない（ブランチ防御原則）。
```

---

## 【2026-06-27 撤回・訂正】「7候補完了・探索終了」は早すぎた → 新規ローンチ先回りが未検証

**記録日: 2026-06-27。賢治さん指摘により、直前の「探索完了・監視フェーズのみ」結論を撤回する。本番・ENV・通知・main.py 変更なし。**

### 撤回する結論（根拠不足だった）

```
✗ 「7候補すべて検証完了」
✗ 「探索終了 / 開発終了 / 監視フェーズのみに戻す」
✗ 「公開・無料データではもう全部ない」
→ 理由: ②アンロック検証(=既存上場銘柄の mcap/price 供給ジャンプ)を、
  「新規ローンチ先回り」と混同して完了扱いにした。後者は一度も検証していない。
  さらに各候補も限定範囲の検証であり「全部」ではない（下表）。
```

### 各候補の検証範囲と限界（正確版）

```
① VRP        : 今回実行。Deribit DVOL vs 将来30日RV の構造検査(873日,BTC/ETH)。
               →実オプション売買(ストライク/証拠金/清算/ヘッジ/税)は未検証。
② アンロック : 今回実行。Coinpaprika mcap/price の供給≥1%ジャンプ(365日,28銘柄)。
               →【重要】既存上場銘柄のみ。新規ローンチ/TGE/IDO/IEO/CEX上場/
                 エアドロ/プレマーケット/DEX初期流動性は未検証。
③ Funding    : 今回は再実行していない（過去session記録に依拠）。再確認が必要。
③ ベーシス   : 未検証。四半期先物データ未取得。「Fundingと同構造」で推論で閉じた(省略判断)。
④ 現物持ち替え: 過去session。BTC/ETH比率ローテ＋28銘柄xsecモメンタムのみ。他ペア未検証。
⑤ DeFi貸出   : 現在利回りスナップショットのみ。バックテストではない。
⑥ DEX LP     : 現在利回りスナップショットのみ。バックテストではない。
⑦ レバ・ペア : 今回実行。BTC/ETHのみ。他ペア/cointegration/実清算は未検証。
```

### 未検証テーマ（別枠・最優先で棚卸し対象）

```
A. 新規ローンチトークン先回り（最優先）
B. CEX新規上場告知イベント（Binance/Bybit/OKX/Coinbase）
C. TGE / IDO / IEO 後リターン
D. エアドロップ上場後リターン
E. プレマーケット価格と上場後価格の乖離
F. DEX初期流動性イベント
→ これらは②アンロック(既存銘柄の供給増)とは別物。データもイベント定義も異なる。
  必要データ: 上場/ローンチ日・初値・1h/24h/3d/7d/30dリターン・出来高・流動性・
              FDV/初期流通量/VC/上場取引所・実際に買えたか(KYC/地域/スリッページ)。
```

### 現在の正しい状態（確定）

```
探索は終了していない。監視フェーズ「のみ」ではない。
本番Bot凍結(SHORT停止/通知OFF/deployなし)は安全策として継続するが、
これは「探索完了」を意味しない。
次にやること: 新規ローンチ先回りを最優先の未検証テーマとして、
  データ源(無料で取れるか)・取得可能期間・イベント定義・合格/不合格基準を事前登録してから着手。
やらないこと(変更なし): Bot接続/通知化/main.py変更/deploy/merge/有料データ購入。
```

---

## 【2026-06-27 CEX新規上場後リターン検証】事前登録 全基準通過＝研究アーク初。ただし"ショート側"エッジで実行可能性は未確認

**記録日: 2026-06-27。本番・ENV・通知・main.py 変更なし。研究用スクリプトのみ。**
**重要: ②アンロック(既存銘柄の供給ジャンプ)とは別物の新テーマ。前回の「探索完了」撤回後の本筋。**

### データ・設計（事前登録 verify_newlisting_PREREG.md・データ前にロック）

```
データ: Binance Spot USDTペアで 2024-01-04〜2026-05-22 に新規上場した177銘柄、
        各上場後31日の1h kline(131,688行)。fetch_binance_newlistings.py(無料・キー不要)。
        上場日=最古klineのopen time。初日quote_vol<$1M・レバトークン・ステーブルは除外。
エントリー: E0=初値(参考・非現実) / E1=上場1h後close / E4=4h後close（採用判断はE1/E4）。
保有: +1h/+24h/+3d/+7d/+30d。コスト0.5/1/2%往復控除。市場調整=btc_excess(BTC同区間)。
合格(全8): n≥30 / 平均と中央値が同符号 / |t|≥2 / train-test同符号 /
          leave-one-token-out / 上位3銘柄除外 / btc_excess同符号 / コスト1%でも符号維持。
分析コード verify_newlisting_event_study.py は結果前にロック・合成データでスモークテスト済み。
```

### 結果（raw・コスト控除前）

```
E0_open(参考): +1h平均+635%/中央+110% … 初足openは0.03→2.58等の異常値で約定不能。採用に使わない。
E1_1hC: +24h -5.0%/-7.3%  +3d -7.6%/-14.3%  +7d -11.5%/-19.6%  +30d -17.7%/-32.4%（負76%）
E4_4hC: +24h -5.8%/-8.7%  +3d -7.4%/-13.6%  +7d -11.7%/-18.9%  +30d -16.1%/-34.1%（負73%）

合格判定: E1/E4 の +24h/+3d/+7d/+30d 全セルが ①〜⑧ を通過（t=-2.8〜-5.1）。
  → 研究アーク全体で初めて、事前登録の全頑健性基準を通過した断面。
  → btc_excess(⑦)通過=市場ベータでなく新規上場固有の下落。train/test・LOO・top3除外も通過。
```

### 確定見解（誇張しない）

```
1. 「新規上場を買って持つ」は robustに負ける（確定）。avoid signalとして堅い。
   中央値(-32%)<平均(-18%)=典型上場は大きく下げ、少数の暴騰銘柄が平均を上げる(負76%)。
2. エッジは"ショート側"。利益化は「新規上場を空売り」が必要 → 実行可能性が別問題:
   (a) 執行未検証: 上場直後にショート可能か(perp/先物の有無・借入・ファンディング)
   (b) テールリスク: 少数銘柄が暴騰=ショートの右裾リスク。"全部ショート"は1銘柄の暴騰で吹く。
       建玉上限・ポートフォリオ管理・1銘柄リスクキャップが必須。
3. 統計優位は本物。だが「使えるエッジ」確定にはあと2つ必要(下記)。即実装しない(§8・§9)。
```

### 次のステップ（まだ実装しない・研究のみ）

```
Step-1 リスク特性化(オフライン・既存データで可能):
  E1/E4ショートの等金額バスケットを時系列で組み、建玉上限・1銘柄キャップ付きで
  資金曲線/最大DD/最悪銘柄/暴騰での損失を測る。mean<median(右裾)が実際に致命的か。
Step-2 執行可能性(要Macフェッチ):
  各上場銘柄に 上場時点で Binance perp(USDM)が存在したか、ファンディング、借入可否。
  ショートが現実に建てられたか・コストはいくらか。
→ Step-1で資金曲線が耐え、Step-2で執行可能なら、初めて"使えるショート/回避戦略"候補。
  それでも Bot通知化・本番実装・SHORT再開・deploy はしない（別途判断）。
```

### やらないこと（変更なし）
```
即実装 / Bot接続 / 通知化 / SHORT本番再開 / main.py変更 / deploy / merge / 有料データ購入
初値(E0)で買えた前提での採用 / 統計優位だけでの実運用化
```

### スクリプト・データ（branch・本番非投入）
```
verify_newlisting_PREREG.md / fetch_binance_newlistings.py / verify_newlisting_event_study.py
newlisting_universe.csv / newlisting_klines_1h.csv
→ 研究用。main.pyと独立。PR化/merge/deployしない（ブランチ防御原則）。
```

---

## 【2026-06-27 新規上場ショート・リスク特性化】右裾は実在するがバスケットを壊さない＝最強候補。残る関門は執行

**記録日: 2026-06-27。本番・ENV・通知・main.py 変更なし。研究用スクリプトのみ（verify_newlisting_shortrisk.py）。既存データのみ・新規取得なし。**

### データ監査（先に確定）

```
生存者バイアス: universeは status=TRADING の現存ペアのみ(fetch_binance_newlistings.py)。
  上場廃止銘柄は欠落 → ロング回避結論は保守的に強まる / ショート成績は過小評価(保守的)。
「上場日」= 最古klineのopen time = "Binance Spot 新規取引開始イベント"(新規ローンチではない)。
  RENDER/POL/ONDO/VIRTUAL等、他所で取引後のBinance上場も混在。
```

### ショート・リスク特性化（等金額1ユニット/銘柄・コスト往復1.0%）

```
ショート勝率(=トークン下落): E1+30d 74% / E4+30d 71% / E1+7d 71%（中央 +18〜33%/トレード）
右裾(最悪トレード=暴騰): NEIRO -508%(MAE+604%) / USUAL -404% / VIRTUAL -267% / MMT MAE+932%
  → 暴騰込みでもバスケットは大きくプラス(E1+30d 合計+2960%, 上位3暴騰除外+4017%)。
    勝率74%が少数暴騰を吸収＝「9勝1敗で1敗が致命傷」では壊れていない。
損切り(逆行stop)で右裾を安く制御:
  E1+30d: stopなし+2960% → +100%stop +2544%(最悪-101%/手仕舞い13%) → +50%stop +2152%(28%)
  → ストップで右裾を切っても優位がほぼ残る。
```

### 確定見解（誇張しない）

```
1. これは研究アーク全体で最強の結果。事前登録の統計優位(①〜⑧)＋リスク特性化の両方を
   通過した初めての候補。ロング回避シグナルとしては既に堅い。
2. ただし「使える戦略」確定ではない。残る重大ポイント:
   (a) MAE凶悪(+932%等)→損切りなしで30日naked保有は非現実的(途中清算)。ストップ必須(幸い安く効く)。
   (b) 「合計+2960%」は実リターンでない。1トレード=資本1/177の単純合計。実益は建玉サイズ・
       同時保有・資金管理次第。資金曲線maxDD-1166%(2024-11ミーム相場でNEIRO/PNUT/USUAL同時暴騰=
       相関クラスター)。適切なサイズ/同時保有上限が要る。
   (c) 執行未検証(最大の関門): 上場直後にショート建てられたか=perp(USDM先物)有無/ファンディング/
       板/借入。新規上場perpはfundingが極端なことが多い(ヘッジに不利or有利)。要確認。
3. 生存者バイアスはショートに保守的(消えた暴落銘柄が欠落)=本来の短期ショート優位はもっと大きい可能性。
```

### 次のステップ（執行可能性・要Macフェッチ・まだ実装しない）

```
Step-2 執行確認: 177銘柄について Binance USDM perp が 上場時点(E1/E4)で存在したか、
  最初の数日のファンディングレート、清算が起きうるMAE水準を確認。
  データ源: Binance futures public API(fapi, 無料・キー不要)。Mac実行。
  → perpが上場時にあり、funding控除後もショート優位が残るなら、初めて"執行可能なショート候補"。
Step-3 資金曲線の現実化: 同時保有上限・1銘柄キャップ・ストップ込みで、単純合計でなく
  実際の口座リターン/最大DD/最悪月を測る。
→ ここまで通っても Bot通知化・本番実装・SHORT本番再開・deploy はしない(別途判断)。
```

### やらないこと（変更なし）
```
即実装 / Bot接続 / 通知化 / SHORT本番再開 / main.py変更 / deploy / merge / 有料データ購入
「合計%」を実リターンと誤認すること / 損切りなしnaked保有前提での採用
```

---

## 【2026-07-01 全体ロードマップ確定】以後のセッションはこの優先順位・GO/NO-GO に従う

**記録日: 2026-07-01。全トラックの方向性を一本化した指示書。次セッション以降（Opus/Sonnet）はこれを読んでから作業する。本番・ENV・通知・main.py 変更なし。**

### 現在地の一行サマリ

```
5ヶ月の探索で、公開OHLCV方向当ては全滅（恒久クローズ）。
構造収益系で残ったのは BTC VRP / Funding Carry（薄利・月1監視）。
唯一、統計＋リスク特性の両方を通過した本命候補が「新規上場ショート（listing fade）」。
残る関門は執行可能性（perp有無・funding）のみ。ここが主戦場。
```

### 進行原則（この5ヶ月で確定した教訓・全トラック共通）

```
1. 同一データへの条件足し探索は必ず過学習で死ぬ → 禁止。前進は常に「新しい種類のデータ」から。
2. 事前登録 → 分析コードをデータ前にロック → 実行 → LOO/top3/train-test、の型を崩さない。
3. 「統計優位」と「実行可能性」と「実リターン」は別物。3つ揃うまで採用と言わない。
4. 勝ち筋のテンプレは「イベント × 構造的売り圧 × 皆が見ない断面」。方向当てには戻らない。
5. 1セッション=1ステップ。重い計算はスクリプト化してオフライン。データ取得はMacフェッチ。
```

---

### Track A【主戦場】新規上場ショート（Binance Spot 新規取引開始→下落の収穫）

**確定済み:** 統計(8基準全通過・177銘柄・t=-2.8〜-5.1・btc_excess/LOO/top3/コスト2%通過) + リスク特性(勝率71-74%・暴騰込みでもバスケット+・stop+50%で右裾制御可・生存者バイアスはショートに保守的)。

**Step-2: 執行可能性確認（次セッションの最初のタスク・Mac1回）**

```
スクリプト: fetch_binance_perp_check.py を新規作成（無料・キー不要・fapi）
取得内容（177銘柄それぞれ）:
  a. USDM perp の存在と上場日: GET /fapi/v1/exchangeInfo の onboardDate(ms) が直接取れる。
     symbol名はSpotと同じ {BASE}USDT が原則（1000系リネーム: 1000BONK等に注意。
     BASEで前方一致マッチし、1000プレフィクスは価格1/1000として扱う）。
  b. funding履歴: GET /fapi/v1/fundingRate?symbol=X&startTime=上場時刻&limit=1000（8h毎・無料）。
     上場後30日分を保存。
出力: newlisting_perp_check.csv (symbol, perp_symbol, perp_onboard_utc, spot_listing_utc,
      perp_minus_spot_hours, funding履歴は別CSV)
※Binanceは話題銘柄でperpをSpotと同日か先に上場することが多い＝執行可能性は悲観不要。
  逆に新規上場のfundingは深いマイナス（ショートが払う）になりがち＝これが最大の敵。
  funding符号の規約: rate>0 はロングがショートに払う（ショートは受取り）。
  ショートのfunding調整後リターン = -raw - コスト + Σ(rate_i)（保有期間中の8hごと）。
```

**Step-2 GO/NO-GO（事前登録・データを見る前に固定）**

```
GO: E4エントリー時点で perp が既に取引可能だった銘柄が n≥30 あり、かつ
    その部分集合で funding調整後ショートリターン（+24h と +7d のどちらか）が
    平均・中央値同符号プラス / |t|≥2 / LOO / top3暴騰除外 を全て通過。
NO-GO: perp即時上場が30銘柄未満、または funding控除で +24h/+7d 両方の優位が消える。
NO-GO時の扱い: Track Aは「新規上場を買わない」ルール（確定済み・実行コストゼロ）だけ残して
    ショート化は閉じる。Bybit/OKXの新規上場で同構造をクロス検証するかだけ判断（Step-5参照）。
※+30dはfunding累積で死にやすいので参考値。主戦場は+24h/+7d。
```

**Step-3: 資金曲線の現実化（Step-2 GO後・オフラインのみ）**

```
funding込み・E4・stop+50%固定で、実際の口座を想定:
  1銘柄=資本のX%（5%/10%を比較）/ 同時保有上限（3/5）/ 月次リターン / maxDD / 最悪月 / 最長水中。
判定基準（事前固定）: 月次中央値プラス / maxDDが資本の-25%以内 / 最悪月-15%以内 /
  2024・2025・2026の各年でプラス（単一年依存でない）。
```

**Step-4: 手動小口トライアル設計（Step-3合格後・実装ではない）**

```
紙上または最小サイズで、次の新規上場5〜10件を事前登録ルール通りに記録（E4・stop+50%・サイズ固定）。
バックテストと実測の乖離（スリッページ・funding・約定可否）を確認して初めて「使える戦略」確定。
この段階でも Bot自動売買はしない。将来Botの役割は「新規上場イベントの検知・通知」への
転換が自然（15分足シグナルより単純で誤作動しにくい）が、それはStep-4合格後の別判断。
```

**Step-5（任意・頑健性強化）: Bybit/OKX の新規上場で同一事前登録のクロス検証（無料kline）。
イベント数も増える。Step-2がNO-GOでも、他取引所でperp事情が違えば復活の余地あり。**

---

### Track B【月1監視】BTC VRP / Funding Carry（構造は本物・時を待つ）

```
毎月1回（月初推奨）: fetch_deribit_dvol.py(Mac)→verify_vrp.py / verify_funding_carry.py。
見るのは現在地のみ（合格化目的の再分析はしない）。
特記: 大きな暴落の直後はVRPプレミアムが太る傾向 → 暴落発生時は臨時チェック可。
GO条件は既記載（VRP: 暴落レジーム込みで残存+実オプション評価ネット+ / Funding: pass_060 YES）。
```

### Track C【record-only】rv_chg / 3反転列 / cs_mom_shadow

```
記録継続・工数ゼロ運用（EntryRecord健康診断のついでに配線確認のみ）。
cs_mom_shadow への期待値は低く固定（OKX 2.5年で不採用確定済み。数バスケット勝っても復活させない）。
rv_chg は「環境フラグ」としてのみ将来価値（Track Aの暴騰クラスター回避に使える可能性はメモに留める）。
```

### Track D【本番Bot】凍結維持・健康診断のみ

```
SHORT停止 / LONG_E停止 / 通知ゲートOFF / deploy・merge なし（変更なし）。
EntryRecord が来たら4択（通常観察/慎重/触らない/記録異常）の健康診断のみ。
Botインフラの将来像: Track A が Step-4 まで通った場合に「上場イベント検知・通知Bot」へ
転換するのが最有力（別途判断・現時点では何もしない）。15分足シグナルBotには戻らない。
```

### Track E【保留】残りの新規ローンチ系（TGE/IDO/プレマーケット/エアドロ/DEX初期流動性/上場告知）

```
データが有料・ゲートのため保留。再開条件: 無料or既存コストで体系的データが取れるようになった時、
または Track A が Step-4 合格して「上場イベント族」への投資判断が固まった時のみ。
```

### Track F【恒久クローズ・再発防止】

```
公開OHLCV方向当て（15分逆張り/順張り/TP・SL調整/QS修正/AI閾値変更）
cs_mom本番化 / S4 / 日次トレンドフォロー / LSR・taker / レバペア / DEX LP
既存銘柄アンロック（APT単独依存） / SHORT_AI_RANK再開 / LONG_E再開
→ 「少し条件を変えれば」の誘惑が最大の敵。再開するには本ロードマップの改訂を先に行うこと。
```

---

### セッション運用（クレジット効率・全セッション共通）

```
1セッション=1ステップ（例: Step-2スクリプト作成→Mac実行依頼→結果分析、で1セッション）。
重い分析はスクリプトに書いてローカル/リモートで実行。チャットで長い生データを往復しない。
データ取得は全てMacフェッチ（リモートはプロキシ403）。キーや認証情報はチャット・gitに出さない。
新しい探索アイデアが出たら、まず本ロードマップとFトラック（クローズ一覧）に照合してから着手。
```

### 次セッションの最初のタスク（明確に1つだけ）

```
Track A Step-2: fetch_binance_perp_check.py を書く（上記仕様）→ Mac実行を依頼 →
結果CSVで funding調整後のGO/NO-GO判定（判定基準は上記の事前登録に従い、変更しない）。
```

---

## 【2026-07-02 Cloud Scheduler 全停止】Bot巡回・shadow前向き蓄積を終了

**記録日: 2026-07-02。賢治さんがGCPコンソールで実施（deploy/merge/main.py変更なし・一時停止のみ）。**

```
停止済み（4本とも一時停止・削除はしていない）:
  crypto-alert-judge-v2-every-15m / crypto-alert-run / ops-health-daily / v2-report-daily
→ 動いているスケジュールはゼロ。Cloud Runは呼ばれないため自動ゼロスケール＝ランニングコストほぼゼロ。

これに伴い終了するもの:
  - v2_shadow / rv_chg / 3反転列 / cs_mom_shadow の前向き蓄積（Track C 実質終了）
  - EntryRecord健康診断（見る対象が止まったため実質終了）
  - 一部OPEN行はDONE化されないまま残るが実害なし

影響を受けないもの:
  - Track A（新規上場ショート）: Botと無関係（Macフェッチ＋オフライン分析）
  - Track B（BTC VRP / Funding Carry 月1監視）: Macスクリプトで実施
  - 本番凍結の防御線: ブランチ防御原則は継続（このbranchをdeploy/mergeしない）

再開方法: GCPコンソールで各ジョブを「再開」するだけ（ワンクリック・可逆）。
再開する場合の候補理由: Track AがStep-4合格し「上場イベント検知Bot」へ転換する時（別途判断）。
```

---

## 【2026-07-02 Track A Step-2/Step-3 結果】執行可能性=🟢GO、資金曲線=🔴不合格（2024年の薄さ）→ Step-5クロス検証へ

**記録日: 2026-07-02。本番変更なし。データ: newlisting_perp_check.csv(168perp) + newlisting_funding.csv(34,441行・403分は再取得済み)。判定は事前登録どおり・後出し変更なし。**

### Step-2 執行可能性 = 🟢GO（研究アーク初のGO）

```
perpあり168/177。E4時点(Spot+4h以内)でperp取引可能 = 68銘柄 ≥30 ✅
  → この68はほぼ全て「perp先行上場」コホート(lag<0)。Binanceは話題銘柄の先物を先に出す。
funding実態: 平均-0.0254%/8h・負の時間帯33%のみ = 恐れたほど深くない。
funding調整後ショート(E4・コスト1.0%・n=68):
  +24h: 平均+4.45% 中央+7.41% t+1.86（t僅か未達）
  +7d : 平均+11.17% 中央+15.08% t+2.96 funding負担-2.63% → 全基準✅ GOセル
  +30d(参考): 平均+14.57% 中央+30.18% t+2.06（funding-6.78%でも生存）
stop+50%でも +7d 平均+8.08%。判定器は verify_newlisting_execution.py（ロック済み・
funding未取得銘柄は0扱いせず除外する保守仕様）。
```

### Step-3 資金曲線の現実化 = 🔴不合格（0/4構成・基準④で落ちた）

```
設定: E4 / +7d / stop+50% / コスト1.0% / funding実績込み / 複利 / verify_newlisting_step3.py
1トレードnet: 平均+8.45% 中央+15.08% 勝率74% 最悪-60.1% / stop手仕舞い16%
グリッド結果（size5-10% × cap3-5）:
  最終+31.6%〜+78.8% / maxDD -7.5%〜-18.0% / 月次中央+0.30〜+0.60% / 最悪月-2.6〜-9.1%
  → ①月次中央>0✅ ②maxDD≥-25%✅ ③最悪月≥-15%✅ は全構成クリア
  → ④「2024/2025/2026各年プラス」で全構成❌（2024が -3.4〜-6.7%）
```

### 診断（基準変更ではない・事実の分解）

```
年別: 2024 n=4 計-67.2%（WIF -50.7% stop / TURBO -35.6% が主因）
      2025 n=49 計+583.3%（勝率76%） / 2026 n=15 計+58.6%（勝率73%）
解釈:
 (a) 2024年はperp先行上場コホート自体が希少（n=4）で、④を統計的に判定できる年ではない。
 (b) ただし内容は示唆的: WIF/TURBOは2024強気相場のミーム＝上場後も上がり続けた。
     → 本戦略は「アルト弱気・横ばいで刈れて、狂乱強気では逆風」というレジーム依存の可能性。
     これは無視してよいリスクではない（次の強気相場で弱る前提で扱う）。
```

### 事前登録どおりの帰結と次の一手

```
Step-4（手動小口トライアル）には進まない（Step-3不合格のため）。
確定済みの持ち帰り: 「Binance新規上場は上場後1ヶ月買わない」は全結果を通じ不変・即使用可。

次の一手 = Step-5（ロードマップに事前登録済みのクロス検証）:
  Bybit（次点でOKX）の新規上場で同一パイプライン（E4/+7d/stop50%/funding込み）を再現し、
  イベント数（特に2024年）を増やして④問題を新データで白黒つける。
  【事前登録・Bybitデータを見る前に固定】
    Step-5判定: Binance+Bybit結合の資金曲線で、
      ①〜③は同一基準 /
      ④' 年間イベント n≥10 の年が全てプラス（n<10の年は報告のみ・ゲートにしない）
    ※④'は「Binance 2024 n=4を見た後」の基準精緻化であることを明示的に開示する。
      したがってBybitデータはこの精緻化基準のアウトオブサンプル検定として機能する。
      Bybit側でも「n≥10の年にマイナス」が出れば、レジーム依存＝不採用（後付け救済しない）。
  NO-GO時: Track Aショート化は closed。「買わない」ルールだけ残す。
```

### やらないこと（変更なし）
```
Step-4先行着手 / Bot接続 / 通知化 / SHORT本番再開 / main.py変更 / deploy / merge / 有料データ
④を「2025-26だけ見れば合格」と読み替える後出し / レジームフィルタの後付け追加
```

### スクリプト・データ（branch・本番非投入）
```
fetch_binance_perp_check.py / fetch_binance_funding_retry.py /
verify_newlisting_execution.py / verify_newlisting_step3.py /
newlisting_perp_check.csv / newlisting_funding.csv
次セッションのタスク: fetch_bybit_newlistings.py（Bybit版ユニバース+kline+perp+funding、無料・キー不要）
```

---

## 【2026-07-02 Track A Step-5 結果】🔴NO-GO確定 → 新規上場ショートは closed（事前登録どおり・救済なし）

**記録日: 2026-07-02。データ: Bybit 224新規上場（2024-01〜2026-05・4CSV）。判定コードは分析前にロック（verify_newlisting_step5.py）。本番変更なし。**

### 判定結果（事前登録基準・変更なし）

```
S（Bybit単独・アウトオブサンプル）: ❌ 不合格【決定打】
  funding調整後+7dショート（適格n=52）: 平均-7.64% 中央-1.08% t-1.55 勝率48%
  → Binanceで見えたエッジがBybitに存在しない = Binance特殊性

結合資金曲線（Binance+Bybit 100件・重複除去20件）: 0/4構成合格
  2024: -5.0〜-16.1%（n=21・ゲート対象）❌ / 2025: +24.7〜+59.0%（n=61）✅ / 2026: -6.2〜-12.3%（n=18）❌
  → ④'「n≥10の年すべてプラス」で全構成落ち。2025年（Binanceアルト弱気）だけの現象と確定。

→ 🔴 NO-GO。Track A ショート化は closed。レジームフィルタ後付け・再最適化・救済はしない。
```

### Bybitロング側の複製実行（ロック済みコードの再利用・基準変更なし）

```
Bybit全224銘柄の「買って持つ」: E1+30d 中央-23.2%・負69%（典型上場はBinance同様に沈む）
ただし 平均+1.8%（+24hは平均+8.9%）= 少数の大暴騰が平均を押し上げる宝くじ構造。
初日出来高 中央$13M（Binance $124M）= Bybitはより小型・早期段階を上場し右裾が極端に太い。
→ これがショートがBybitで壊れた理由と整合（暴騰の刈り取られ）。
```

### 解釈（仮説として記録・行動根拠にしない）

```
Binanceの「上場後下落」は、(a)2025年アルト弱気相場と、(b)Binance上場=プロジェクト後期の
出口流動性イベント（Bybit等で先に取引済みの銘柄が最後にBinanceに来る）という2要因の
複合だった可能性。Bybit上場はより早期＝宝くじ段階で、同じ力学が働かない。
→ 検証で反証されたため、これ以上この仮説を掘らない（同一データ再掘削の禁止）。
```

### Track A 最終確定

```
ショート戦略: closed（統計→リスク→執行まで通ったが、クロス取引所で消えた=2025年Binance固有）
残る持ち帰り:
  1. 「Binance新規上場は上場後1ヶ月買わない」= 8基準全通過の頑健な回避ルール（不変）
  2. Bybit新規上場も典型は沈む（中央-23%/負69%）が宝くじ右裾あり = 買いも短絡ショートも不可
Step-4（手動小口トライアル）には進まない。Bot化・通知化もしない。
```

### 研究プログラム全体の現在地（2026-07-02・最終）

```
5ヶ月+の全探索が完結: C系方向当て（全滅・閉鎖）→ 7候補（VRP/アンロック/キャリー/現物/
DeFi/LP/レバペア）→ 新規上場ファミリー（統計は本物だったが取引所固有・closed）。
事前登録→ロック→OOS検証の規律により、実資金を失う前に全て机上で決着した。

残るもの:
  - Track B: BTC VRP / Funding Carry 月1監視（構造は実在・薄利・時を待つ）
  - 回避ルール: Binance新規上場を買わない
  - 本番Bot: 全凍結・スケジューラ停止済み・ブランチ防御原則維持
再探索の条件（変更なし）: 本当に新しい種類のデータが無料/既存コストで得られた時のみ。
「少し条件を変えれば」には戻らない。
```
