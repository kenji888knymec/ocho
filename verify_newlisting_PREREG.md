# 【検証 新規テーマ 事前登録】CEX新規上場後リターン検証（Binance Spot USDTペア）

**記録日: 2026-06-27。データを見る前に固定する事前登録。後出しで変更しない。**
**本番・ENV・通知・main.py・deploy・merge・有料データ購入には一切関与しない。完全オフライン・分析のみ。**

---

## 0. 名前と範囲（正確に）

```
これは「上場前の先回り（IDO/IEO/プレマーケット/エアドロ/DEX初期流動性）」ではない。
無料データで取れる「CEX新規上場"後"の初動リターン」だけを検証する。
  上場前に買う戦略 ≠ 上場後の足を見て 買う/避ける/(可能なら)売る 戦略
段階方針:
  Step1: Binance Spot USDTペアだけで検証（本事前登録）
  Step2: 形が見えたら OKX / Bybit に拡張
  Step3: 上場後に優位が出た場合のみ 上場告知時刻/TGE/IDO/IEO/エアドロ/プレマーケットへ拡張
※今やるのは Step1 のみ。最初から全部やらない。
```

---

## 1. 仮説（事前固定）

```
Binance Spot に新規上場したトークンは、上場後に系統的なリターンパターンを示す。
  対立仮説A（経験則）: 上場直後にポンプ→その後失速/下落（"上場ポンプ後の下落"）
  対立仮説B: 上場後しばらく上昇が続く（モメンタム）
→ どちらでもなく「ランダム/コスト負け」なら不採用。
  方向は事前に決め打ちせず、買い・売り両方向を対称に測る（どちらかに robust な符号が出るか）。
```

---

## 2. データ源（事前固定）

```
Binance 公開API（無料・キー不要・カード不要）:
  - exchangeInfo: 全Spotシンボル一覧
  - klines (GET /api/v3/klines): symbol/interval/startTime/limit でローソク足
上場日の定義（as-of・観測可能）: その銘柄の "最初に存在するklineのopen time"。
  （Binanceに上場日APIは無いため、最古klineを上場開始とみなす。再上場/名称変更は誤差源として注記）
取得環境: 賢治さんのMac（リモートはBinance 403）。標準ライブラリのみ・売買なし。
取得スクリプト: fetch_binance_newlistings.py（acquisition）。
```

---

## 3. 対象ユニバース（事前固定・チェリーピック禁止）

```
対象: 「最初のklineが 2024-01-01 以降」かつ「上場から30日以上経過済み(=上場日 ≤ now-30d)」の
      Binance Spot USDTペア 全件（個別選別しない）。
除外:
  - レバレッジトークン（symbolに UP/DOWN/BULL/BEAR を含む 例: BTCUPUSDT）
  - ステーブル/法定as-base（USDC/FDUSD/TUSD/DAI/EUR/...USDT 等のステーブル同士・通貨ペア）
  - 上場初日のquote出来高が極端に薄いもの（初日 quote volume < $1,000,000 を除外＝死に上場/流動性無し）
  - 明らかな再上場・名称変更で最古klineが2024前のもの（最古klineで自動的に対象外になる）
注記: 再上場/リブランドの完全検出は無料データでは困難 → 限界として明示。
```

---

## 4. エントリー前提（最重要・甘くしない）

```
"最初の足の始値で買えた" 前提にしない（上場直後の初値は現実に取れない）。
3つのエントリー時点を全て測る:
  E0: 最初の1h足の OPEN（=上場初値。参考・非現実的として併記するが採用判断には使わない）
  E1: 最初の1h足の CLOSE（上場1時間後に入る）
  E4: 4本目の1h足の CLOSE（上場4時間後に入る）
採用判断は E1 / E4（現実的に約定しうる時点）で行う。E0は参考値。
```

---

## 5. 測る指標（事前固定）

```
各エントリー時点(E0/E1/E4)から、保有 +1h / +24h / +3d / +7d / +30d の リターン。
  raw_ret = entry→horizon の価格変化
  btc_excess = raw_ret − BTC同区間リターン（市場ベータ調整・"市場が上げただけ"を除く）
記述用: 初日pop幅（最初の1h open → 初日high / 初日close）。
コスト控除（スリッページ+手数料）: 0.5%(基準) / 1.0% / 2.0% の3水準で net を見る。
  → 新規上場は初期スプレッドが広いので 0.5% でも楽観。2.0%まで耐えるか確認。
集計: セル =(エントリー時点 × 保有期間)ごとに n/平均/中央値/負割合/t統計量。
```

---

## 6. 合格基準（事前固定・全て満たすこと）

```
ある (エントリー E1 or E4 × 保有期間) のセルで、net(コスト控除後) リターンが:
  ① n ≥ 30 上場銘柄
  ② 平均 と 中央値 が同符号（外れ値・少数大勝ち依存でない）
  ③ |t| ≥ 2.0
  ④ train/test（上場日でソートし前半/後半に分割）両方で同符号
  ⑤ leave-one-token-out で符号反転しない（単一銘柄依存でない）
  ⑥ 上位3銘柄を除いても符号維持（数銘柄依存でない）
  ⑦ btc_excess でも同符号（市場ベータだけの見かけでない）
  ⑧ コスト0.5%だけでなく 1.0% でも符号維持（スプレッドに殺されない）
方向: 上記を満たす符号が
  - プラス → "上場後ロングで取れる" 候補（実際に買える前提の確認に進む）
  - マイナス → "上場後は下落＝買うな/(先物があれば)ショート" 候補（執行可否を別途確認）
```

## 7. 不合格基準（いずれか該当で不採用）

```
A. n < 30
B. 平均と中央値が逆符号（少数大勝ち/大負け依存）
C. |t| < 2.0
D. train/test で符号反転
E. ≤2銘柄 or 上位数銘柄に依存（leave-one-out / top3除外で消える）
F. btc_excess で消える（市場ベータだけ）
G. コスト0.5%→1.0%で消える（初期スプレッドに殺される）
```

---

## 8. 進め方（この順・各段で止めて確認）

```
Step B-new-0: 本事前登録を固定 → 賢治さん確認【今ここ】
Step B-new-1: fetch_binance_newlistings.py を Mac 実行 → 新規上場ユニバース＋上場後1h klines取得
Step B-new-2: 分析コードを「データを見る前に」本事前登録で固定（窓・基準ロック）→ オフライン実行
Step B-new-3: 結果をCLAUDE.mdに記録（合格でも即実装しない）
Step B-new-4: 合格時のみ OKX/Bybit へ拡張（Step2）。さらに優位ならStep3（上場前系）の可否検討
```

**Step B-new-2 の分析コードは、データ投入後に窓・閾値・基準を動かさない。**

---

## 9. 実行可能性の正直な注記（統計の合否とは別に必ず併記）

```
- 上場直後の初値(E0)は現実に約定困難 → 採用判断はE1/E4のみ。
- 初期スプレッド/スリッページが大きい → コスト2%でも耐えるかを必ず見る。
- 日本から実際に買えるか（取引所/KYC/地域制限）は統計の外。優位が出ても別途確認。
- 上場後すぐ先物が無い銘柄が多い → "マイナス符号=ショート" は執行不能のことが多い。
- 結論は「統計的優位」と「実際に取れるか」を分けて記録する。
```

---

## やらないこと（確定・厳守）

```
有料データ購入 / Bot接続 / 通知化 / main.py変更 / deploy / merge
最初の足の始値で買えた前提での採用判断
OKX/Bybit/上場前系(IDO/IEO/プレマーケット/エアドロ/DEX)へStep1前に拡張すること
データを見てから窓・閾値・基準を変えること
```
