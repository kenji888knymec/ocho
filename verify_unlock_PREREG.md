# 【検証B 事前登録】トークンアンロック（供給イベント）への価格反応

**記録日: 2026-06-27。これは「実データを見る前」に固定する事前登録。後出しで変更しない。**
**本番・ENV・通知・main.py・deploy・merge には一切関与しない。完全オフライン・反応分析のみ。**

---

## 0. このBで「やること / やらないこと」

```
やること（これだけ）:
  - アンロックスケジュールのデータ取得（Mac-local、リモートはプロキシ制約）
  - 過去アンロック日 前後の価格反応分析
  - BTC地合いを差し引いた超過反応（market-model）の確認
  - イベント前に下がるか / 当日か / 後で戻るか の時系列分解
  - サンプル数・再現性（train/test・leave-one-out）の確認

やらないこと（禁止）:
  - いきなりショート戦略化 / Bot接続 / Discord通知 / gate化
  - main.py変更 / deploy / merge
  - 条件・閾値の後出し探索 / 負けた後の条件追加での救済
  - ETH除外のような「外して合格化」
```

---

## 1. 仮説（事前固定）

```
大型トークンアンロック（vesting cliffで循環供給の相応割合が一度に解放される日）の
前後で、その銘柄は BTC地合いを差し引いても 負の超過リターン（価格の弱さ）を示す。

効率市場仮説の対立仮説も同時に検証する:
  「アンロック日は既知 → 事前に織り込まれ、イベント前に下落し、イベント後はむしろ戻る
   （sell the rumor, buy the news）」
→ イベント前ドリフト / 当日 / イベント後ドリフト を分けて見る。
```

---

## 2. データ源（事前固定・優先順）

```
【2026-06-27 更新】DeFiLlama無料emissions APIは有料化（HTTP 402 Payment Required）。
  → 有料Proには進まない（CLAUDE.md「有料データ購入の即断はしない」）。
  → Token Unlocksも無料APIゲート＋ToS懸念で不使用。

採用データ源（差し替え後・事前固定）:
  循環供給ジャンプ検出方式（CoinGecko無料データから供給を復元）
    circ_supply[t] = market_cap[t] / price[t]   （CoinGecko market_chart, days=max）
    単日で circ_supply が ≥1.0% 増えた日 = アンロックイベント（経済定義は不変）
  → 第三者のアンロックラベルでなく「実際に観測された供給増」を使う。
    チーム/エコシステム/emission の区別なく、市場に出た供給増を捉える（むしろ筋が良い）。
取得環境: 賢治さんのMac（リモートはCoinGecko 403）。標準ライブラリのみ・売買なし。
価格データ: 既存 OHLCV /tmp/ohlcv_long/ohlcv_2024_2026/*_1h.csv（28銘柄・日次終値に集約）

正直な注意（誇張しない）:
  - CoinGeckoの供給更新は数日遅れ・平滑化あり → イベント日が±数日ずれ得る（±10日窓で吸収）。
  - ごく小さなアンロックは見えないことがある（≥1%cliffは検出可能の想定）。
  - mcap/price 復元は丸め誤差あり → 1日スパイクで戻る偽ジャンプは「持続ステップ」条件で除外。

★イベント定義（§4）の窓・指標・合格基準（§5〜§7）は一切変えない。データ源のみ差し替え。
```

**取得は fetch_supply_coingecko.py（Mac実行）。生の price/mcap も保存し、供給復元と
ジャンプ検出はリモート側（オフライン）で行う（スキーマ推測でハードコードしない）。**
**旧 probe_unlock_data.py（DeFiLlama用）は402で無効化・研究記録として残置。**

---

## 3. 対象銘柄（universe・事前固定ルール）

```
ルール（チェリーピック禁止）:
  「アンロックデータが取得でき、かつ 日次OHLCV が揃う」全銘柄を対象にする。
  個別に銘柄を選んで残す/外すことはしない。
  古参（BTC/ETH/DOGE/BNB/SOL等・継続的な大型cliffが無い）は自然に対象外になりうる。
注記: 対象が少なすぎる場合は、OHLCV側を Mac で補充して件数を確保する
      （universeルール自体は変えない＝「unlock×OHLCV が揃う全銘柄」を維持）。
```

---

## 4. イベント定義（事前固定）

```
イベント = 単日で 循環供給の ≥1.0% を解放する discrete cliff unlock。
  - linear（毎日少しずつ）の連続解放はイベントスタディに不向き → 除外
  - 同一銘柄で近接（±5日以内）する複数cliffは最大のもの1件に統合（重複カウント防止）
サブ集計: 「大型」= ≥5.0% を別途集計（規模依存の確認用・合否は ≥1.0% 全体で判定）
as-of: アンロック日・解放量は事前に公表される既知情報。価格は日次終値のみ使用。
```

---

## 5. 検証窓・指標（事前固定）

```
超過リターン = market-model の abnormal return:
  各銘柄 r_token,t に対し β を推定窓 [-90, -11]日 で OLS 推定（vs BTC日次リターン）
  AR_t = r_token,t − β · r_BTC,t        （α は0固定＝保守的）
  欠損は補完しない（np.nan のまま・該当日はその銘柄を集計から除外）

累積超過リターン CAR を3窓で測定:
  CAR_pre   = Σ AR over [-10, -2]   （イベント前ドリフト）
  CAR_event = Σ AR over [-1, +1]    （当日±1）
  CAR_post  = Σ AR over [+2, +10]   （イベント後ドリフト）

各窓について: 平均CAR / 中央値CAR / 勝敗（負の方向に出た割合） / t統計量 / イベント数
```

---

## 6. 合格基準（5つ・全て満たすこと・事前固定）

```
① イベント数 n ≥ 30（discrete cliff ≥1.0%・統合後）
② CAR_pre または CAR_event の平均が 明確に負（経済的に意味ある大きさ）
③ その負の窓で |t統計量| ≥ 2.0（統計的に偶然でない）かつ 中央値も負（外れ値依存でない）
④ train/test 時系列分割（前半/後半）両方で同じ符号（負）が残る
⑤ leave-one-out（最悪1イベント除外）で符号が反転しない（単一イベント依存でない）
```

## 7. 失敗基準（いずれか該当で不採用・事前固定）

```
A. n < 30（サンプル不足）
B. 全窓で平均CAR ≈ 0 または 正（反応が無い／逆）
C. BTC地合い調整後に効果が消える（生リターンでは負でもARで消える）
D. 効果が ≤2 イベントに集中（leave-one-out で反転）
E. train/test で符号逆転
```

---

## 8. 進め方（この順で・各段で止めて確認）

```
Step B0: 事前登録（本ファイル）を固定 → 賢治さん確認【完了 2026-06-27】
Step B1: 【次】probe_unlock_data.py を Mac 実行 → DeFiLlama実スキーマ確認（生JSON保存）
Step B2: スキーマ確定後、本取得スクリプトでアンロックCSV作成（Mac）
Step B3: 既存OHLCVと結合し、上記5基準でイベントスタディ（オフライン）
Step B4: 結果をCLAUDE.mdに記録
Step B5: B3で明確にエッジが出た場合のみ → 小さな実運用「設計」へ進む
         （いきなり実装/Bot接続/通知ではない。設計から段階的に）
```

**「検証で止める」の正しい意味（賢治さん確認 2026-06-27）：**
```
永遠に検証だけして実運用に進まない、という意味ではない。
B3で明確なエッジが見えれば B5（小さな実運用設計）に進んでよい。
ただし B3 でエッジが見えないまま、いきなりショート戦略化・Bot接続・通知化に
飛ぶのを禁じる、という意味。供給イベントは別データなので進む価値はある。
```

**Step B3 の分析コードは、結果を見る前に本事前登録の窓・指標・基準で固定する。データ投入後に窓や閾値を動かさない。**

**閾値の確定（賢治さん確認 2026-06-27）：** イベント定義は **≥1.0%** のまま。**≥5% はサブ集計**として規模依存だけ見る。最初から5%に絞るとイベント数が30に届かず検証不能になりうるため、1%で全体を判定する。
